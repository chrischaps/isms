# Open questions and provisional answers

Where the GDD and TDD are silent or contradict each other, the executing agent records the question and a provisional answer here and proceeds on it. Each answer is in force until Chris overturns it; overturning one is an edit here plus, if code changed, an ADR. Structural deviations from the TDD are ADRs in `docs/decisions/` (referenced below), not entries here.

Numbering is stable; do not renumber. Add new entries at the end.

| # | Question | Provisional answer | Where it bites | Status |
|---|---|---|---|---|
| Q1 | TDD 9.3 legacy-manager script never buys inputs, so Mills, Foundries and Workshops would starve (production consumes from the owning org's inventory, TDD 5.5 phase 4). | Each tick the legacy manager bids for the shortfall between org inventory and one cycle's recipe demand at `last_price x (1 + legacy_markup)`. Amends TDD 9.3. | S0.12b | provisional |
| Q2 | Legacy hiring is uncapped: 40 householders at `legacy_wage` x 8 h is ~2560/cycle payroll against ~1900/cycle household spend at start prices, so treasuries drain and the machine-buy trigger never fires. | Script rule: a legacy firm posts no new job offers while inventory exceeds `legacy_hire_inventory_cycles_cap` cycles of last-cycle sales. S0.14 may still need an engine session; pre-authorised as S0.14b. | S0.12b, S0.14 | provisional |
| Q3 | T20 fatigue debt: is "high effort" per citizen or per allocation? Does hardship debt recur? When is `budget` set (TDD 6 says cycle start, 5.5 says 8h at cycle end)? | A cycle is high-effort if any allocation with hours > 0 was High at cycle end. Hardship debt recurs each hardship cycle. Step 8h at tick 23 sets next cycle's `budget`; that is the same moment as "cycle start". | S0.5, S0.6a | provisional |
| Q4 | What happens when the new budget is below the sum of existing allocations, and is the D6 constraint on the rate or the cycle total? | Allocations are a rate. `SetLabor` validates sum(hours) <= budget at submission. Phase 3 uses `min(sum(hours), budget)` scaled pro rata without mutating allocations, so no synthetic `LaborSet` is emitted. | S0.6a | provisional |
| Q5 | Hours per tick are fractional (8/24) but TDD 5.9 forbids accumulating f64 across ticks; `hours_in_family`, `cycle_output` and piece-rate attribution all accumulate. | Labor is stored as integer tick-hours (`u32`, each tick adds `hours`); real hours = tick_hours / 24. Hourly pay = `wage_cents x tick_hours / 24`, floored; the remainder stays in the treasury. f64 only in `output_remainder` and per-cycle attribution that resets at 8a. Transcendentals go through `libm` so MSVC and glibc agree. | S0.6a, S0.10a | provisional |
| Q6 | Are standing-plan internal commands persisted, with what `client_kind`, and do they count as presence? | Persisted in the tick's event vector with a new `ClientKind::Plan`. Only Web/ApiKey commands and `Seen` update `last_seen_tick`. Householders are exempt from dormancy (they emigrate instead). | S0.9 | provisional |
| Q7 | `tick()` cannot refuse to run after `EpochEnded` (S0.4 gate says tick 1008 is never produced). | `tick` returns `Result<Vec<Event>, TickError>`; `TickError::EpochEnded` when the epoch has ended and no new epoch was started. | S0.4 | provisional |
| Q8 | "Last price" before any trade, and for shares and dwellings; `TickResolved.last_prices` duplicates state that `Trade` already moves. | `last_price(instr)` = last `Trade`, else `start_prices[good]`, else book value for shares, else None. `apply(Trade)` is authoritative; `TickResolved` carries only VWAP and index statistics. | S0.8 | provisional |
| Q9 | `founding_cost_money` is debited but Freeport has no treasury (A6 = none). Where does it go? | `Burned`, with `RuleId::founding_fee`, so conservation holds. Householders never found. | S0.10a | provisional |
| Q10 | How does a human `Join` a simulated society when the actor is not yet a citizen? | `Envelope.actor = System`, `client_kind = Sim`, `Join { handle, kind: Human }`; `CitizenJoined` assigns the next id. Sim humans never `Seen`, so they go dormant after 7 cycles and the fill rule refills; tests account for that. | S0.12a, S0.13 | provisional |
| Q11 | Starting state of a joiner beyond "equal money and nothing else". | Meters start at `meter_start` = 100, pantry empty, unhoused. Shelter decays until a lease, so legacy Builders must let the 40 `initial_dwellings` in cycle 0. | S0.5, S0.12 | provisional |
| Q12 | GDD gives a pantry cap of 48 for Food only; S0.7 enforces "pantry caps" and citizens may buy Machines and Materials. | `pantry_cap` is per good: 48 for Food and Wares, unlimited otherwise. A bid is rejected if `pantry + open bid qty + qty > cap`. | S0.7, S0.8 | provisional |
| Q13 | Is the hardship test (Food < 20 for a full cycle) measured before or after eating? | The Food meter at the end of phase 5, at every tick 0..23 of the cycle. | S0.5 | provisional |
| Q14 | S0.2 needs numbers that exist nowhere: Republic tax defaults, Commonwealth levy, the cross-axis constraint list, the lexicon key set. | `tax_rate` 0.20 flat, `need_floor_food` 24, `capital_levy` 0.01 per cycle (all `[new tunable] S0.2`). Constraints: need => pricing=none and redistribution=total; pricing=none => compensation=need; capital=open => ownership=private; ownership=collective => capital=none; labor=assigned => governance=committee; compensation=share => ownership=cooperative; governance=none => redistribution=none and pricing != administered. Lexicon keys are listed in `presets/lexicon/KEYS.txt`. | S0.2 | provisional |
| Q15 | Householder "living cost" and legacy job-offer terms are undefined. | Living cost = 24 Food + 4 Wares at last price + current rent. Legacy offers: max 8 h, open-ended term, 1-cycle notice. | S0.12b | provisional |
| Q16 | S0.6 builds `SetLabor` to any workplace; S0.10 rejects hours above the contract's max. What links them? | From S0.6, `SetLabor` requires the citizen to be in `workplace.workers` (an assignment). S0.6 fixtures insert assignments directly; from S0.10 `AcceptEmployment` is the only producer of assignments in Freeport. | S0.6a, S0.10a | provisional |
| Q17 | Builders produce Dwelling assets, but dwellings arrive in S0.11. | The Builder arm of production is a tracked no-op in S0.6 (listed in the unimplemented registry) and is completed in S0.11a. | S0.6b | provisional |
| Q18 | The away digest needs history and `World` holds none. | `digest(citizen, events: &[Event], since_tick) -> Digest`; the caller (server or sim) supplies the event slice. | S0.9 | provisional |
| Q19 | GDD 4.2 scales Food decay "by effort", but a citizen may hold two allocations with different efforts, or none. | The decay effort is the highest effort among allocations with hours > 0; Normal when idle. Meters are stored in integer tenths so the 0.8/1.0/1.3 rates are exact. | S0.5 | provisional |
| Q20 | With `food_meter_per_unit` = 4 and decay 4 per tick at normal effort, auto-eating one Food per tick only holds the meter; a citizen whose meter has reached 0 cannot leave hardship by eating (even at low effort +0.8 per tick is under 20 points per cycle), so hardship is sticky. GDD Q3 says "eat, and it recovers next tick". | Decided 2026-09-13 (S1.0): `food_meter_per_unit` = 6, so one Food per tick recovers +2 at normal effort and a citizen at 0 climbs out of hardship in one cycle. Config only (`presets/_base.toml`); goldens regenerated; `make sim-check PRESET=freeport` unchanged (freeport-01.md showed 6 changes nothing for householders). Earlier history: implemented at the GDD value of 4 through S0.14; a second-unit rule was rejected as an engine change. | S0.5, S0.14, S1.0 | decided |
| Q21 | When production is capped by inputs, what happens to the surplus fraction and the carried remainder? | The capped units are produced; the remainder is reset to 0 (nothing carries while starved of inputs), so an input-starved workplace cannot bank output. | S0.6a | provisional |
| Q22 | Assignments (`workplace.workers`) are created by employment contracts in free-labor systems and by the Committee in assigned systems; the TDD has no event for the second case. | New events `Assigned { workplace, citizen, contract }` and `Unassigned`. `Unassigned` also drops the citizen's allocation to that workplace. `EmploymentAccepted`/`Terminated` (S0.10) emit them alongside. | S0.6a, S0.10, S0.16 | provisional |
| Q23 | The reference basket includes Dwellings, which have no market price. | The price index is computed over basket goods that have both a start price and a current price; Dwellings count in real output only. | S0.8, S0.13 | provisional |
| Q24 | The Wares rule says when to buy but not how many. | The plan bids for enough Wares to refill Comfort (`ceil((100 - comfort) / comfort_per_wares)`), capped by the pantry room and by what the saving floor allows, and only when no Wares bid is already resting. Standing orders with `EachTick` refresh rest for one tick (cancelled and re-placed each tick); `EachCycle` orders are re-placed on the first tick of a cycle and expire at its end. | S0.9 | provisional |
| Q25 | Dormancy is measured from `last_seen_tick`: only `Seen` (issued by the API on any authenticated request) and Web/ApiKey commands count as presence. Householders never go dormant. On dormancy the citizen's open orders are cancelled and their contracts are suspended; `Seen` from a dormant citizen emits `CitizenReturned`, which reactivates suspended contracts. | Implemented as stated; dwelling release in collective systems is S0.15. | S0.9 | provisional |
| Q26 | GDD Q3 says a destitute citizen cannot sign long contracts, without defining long. | `long_contract_cycles` = 5 [new tunable]. A contract's commitment is its notice: an employment offer is long when its term or its notice exceeds the threshold (open-ended with one cycle's notice is short); a lease is long when its term exceeds it; a loan when its term does. Revised in S0.13b: the earlier reading (open-ended = long) locked every destitute householder out of the legacy firms' open-ended jobs. | S0.10a, S0.13b | provisional |
| Q27 | T18 notice pay for piece-rate contracts, and whether termination is immediate. | Termination is immediate in both directions. An employer pays the accrued pay of the current cycle plus, for hourly contracts, `wage x max_hours x notice_cycles`; piece-rate contracts have no notice component. A worker leaving forfeits the current cycle's accrued pay. Notice pay is capped by the treasury; a shortfall is a `PaymentMissed`. | S0.10a | provisional |
| Q28 | Term expiry: a contract with `term_cycles` = n ends at the end of the cycle in which it has run n cycles, after payroll. | Implemented as stated (`cycle + 1 >= created_cycle + n` at 8a). | S0.10a | provisional |
| Q29 | When a share transfer gives a citizen control (> 50% of issued), does the manager change? | Yes, automatically: the fill or sale that crosses 50% emits `ManagerAppointed` for the new controlling owner unless they already manage the org (TDD S0.12: the householder manager steps down). | S0.10b | provisional |
| Q30 | Declared dividends: when are they paid and what if payroll leaves the treasury short? | Paid at step 8e of the cycle in which they were declared, pro rata to the treasury; the declaration is cleared at `CycleClosed`. One declaration per org per cycle. | S0.10b | provisional |
| Q31 | TDD 5.3 lists workplace leases alongside dwelling leases. | Not in Phase 0a: `OfferLease { Workplace }` is `NotImplemented`; nothing in the Freeport householder economy needs it. | S0.11a | provisional |
| Q32 | Can a lease be ended before its term, and with what notice? | Either party may end it at once; there is no notice period for leases (the GDD gives none). | S0.11a | provisional |
| Q33 | Partial rent: does a tenant who can pay some of the rent pay it? | No: rent is paid in full or missed; a miss is recorded and the tenant keeps their money. Eviction follows `lease_grace_cycles` misses, on the first tick of the next cycle. | S0.11a | provisional |
| Q34 | The GDD says dwellings can be bought, but only leases put a citizen in one. | `MoveIn`/`MoveOut` let an owner occupy their own dwelling; an occupied dwelling cannot be let; selling an occupied dwelling keeps the tenancy (rent flows to the new owner). | S0.11a | provisional |
| Q35 | Credit: what happens when the borrower cannot pay an installment in full? | Nothing is paid; `CreditMissed` is recorded and the default resolves in phase 7 of the next tick (TDD 5.5): collateral to the lender (a dwelling changes owner; escrowed shares move), the contract ends, the borrower's public `defaulted` flag is set. Unpaid balance is not pursued further (no bankruptcy, T9). | S0.11b | provisional |
| Q36 | Who may lend and borrow? | Any citizen or org (via its manager) may offer or accept credit where the constitution enables the contract; a destitute citizen may not sign a loan longer than `long_contract_cycles`. Shares pledged as collateral sit in escrow for the life of the loan. | S0.11b | provisional |
| Q37 | Which householders emigrate when humans arrive? | The most recently joined non-dormant householders leave first. Emigration cancels their orders and offers, ends their employment (forfeiting accrued pay) and tenancy, hands any managed org to another householder (or leaves it vacant), then burns balance and pantry (T19); the citizen record stays, dormant. | S0.12a | provisional |
| Q38 | Seeding shape: one legacy org per seeded workplace, named `Legacy <Kind> No. n`, each with `legacy_treasury_credits`; initial dwellings round-robin among the Builders; a householder manager per org round-robin. | Implemented as stated in `seeding::start_epoch`. | S0.12a | provisional |
| Q39 | The householder saves 10% of cumulative income and rents within 25% of last cycle's income; the engine had no income counters. | `Citizen.wages_total`, `cycle_wages` and `last_cycle_wages` count `Paid` amounts (wages and notice pay); dividends and sales are not income for these rules. Rolled at `CycleClosed`. | S0.12b | provisional |
| Q40 | Legacy cost-plus pricing needs last cycle's payroll and inputs per unit; the TDD does not define the bookkeeping. | Cost per unit = legacy wage / base rate + inputs at last price; the ask is that times the markup, never below the start price, refreshed when it moves. Hiring stops while output stock exceeds `legacy_hire_inventory_cycles_cap` cycles of full production (Q2). `decide` takes `&World` rather than the TDD's `CitizenView`/`PublicView`; the views are an API concern (S1.4). | S0.12b | provisional |
| Q43 | With `legacy_machine_buy_payroll_mult` = 3 no legacy firm ever reaches the machine-buy threshold in the first cycles, so the Machine Shop has no revenue, misses payroll and fires its workers. | Set to 1.0 in S0.12b (buy a machine whenever the treasury exceeds one cycle of payroll plus the price); S0.14 raised it to 1.5 because at 1.0 several firms crossed the threshold in the same cycle and one epoch per seed showed a 0.65 investment share (`docs/tuning/freeport-01.md`). | S0.12b, S0.14 | provisional |
| Q41 | What exactly carries across an epoch rollover? | Constitution, params, id counters and the human roster. Every material thing is cleared (orgs, workplaces, dwellings, land, books, contracts, offers, escrow, ledger); humans start dormant with a fresh endowment and default state; householders are dropped and re-seeded by `start_epoch`. | S0.13a | provisional |
| Q42 | The API share metric needs command counts by client kind, which the engine never sees for rejected commands. | Deferred to the server (S1.4): `Citizen.api_share` stays in state for it; `CycleAggregates` carries no API share in Phase 0. | S0.13a | provisional |
| Q44 | The first 42-cycle simulation exposed three price-formation traps: legacy asks priced on the last price compounded with bids at last x markup (index 78-2228); plan bids at the citizen's ceiling let every incoming ask fill at the ceiling; and a plan bid resting one cent below a cost-plus ask never re-priced. | Legacy firms price on `reference_price` (labor at the legacy wage plus reference-priced inputs, times the markup: the TDD 5.7 derivation), and bid inputs and machines at reference x markup. Plan bids are placed at the best resting ask (capped at the citizen's limit) or at the last price, and a resting plan bid is cancelled and re-placed when a crossable ask appears above it. | S0.13b | provisional |
| Q45 | Day one has no Food: households cannot buy until the Mills have bought Grain and milled it, so most citizens fall below 20 in cycle 0 and (with Q20) never recover. No config knob could seed stock. | `params.seeding.legacy_inventory` [new tunable, ADR-0004]: goods seeded into each legacy org by workplace kind at `start_epoch`, counted in `seeded`. Chosen over seeding household pantries because GDD 6.1 says citizens join with money and nothing else; legacy firms exist precisely so day one works. | S0.14b | provisional |
| Q46 | The householder Freeport runs with 15–22 of 40 citizens unemployed: six legacy firms hiring only while inventory is under two cycles of sales (Q2) need about half the workforce to feed and clothe forty scripted households at the base rates. | Left as is for Phase 0a (not a GDD 17 target; every target passes). It is a recipe-constants fact, and a design question once humans arrive: what does an unemployed human with a full pantry do? Candidates: lower `base_rate`s, more consumption per household, or founding as the intended outlet (GDD 7.1). | S0.14 | provisional |
| Q47 | T19 burns an emigrant's balance and pantry, but in a Common Store or state-stock society those goods were the society's to begin with and burning them is a leak the system never chose. | Where a society stock exists (`world.store` or `world.state_stock`) an emigrant's pantry and balance return to it (`StoreReturned`, goods to the Store or state stock, money to the till) and `HouseholderEmigrated` carries zero burned; T19 burning stays where neither exists (Freeport, Republic, Commonwealth). | S0.15a | provisional |
| Q48 | Where do a collective or state enterprise's inputs, outputs, machines and building Materials live? TDD 5.5 phase 4 says "the Common Store / state stock in collective systems" for inputs only. | One helper, `ledger::stock_holder`, decides: a Society-owned org's goods are the Store (moneyless) or the state stock (administered); production output, recipe inputs, Machines installed or uninstalled, and the Materials for a workplace or a dwelling all move through it, and a dwelling built by a Society-owned org is `Owner::Society`. Firms and coops keep their own inventory; the Freeport paths are unchanged. | S0.15a | provisional |
| Q49 | GDD 6.2 gives a draw entitlement per tick ("whatever brings the meters to full") but no command shape; the plan's `keep_food_at_least` is a market rule. | `RequestStoreDraw { good, qty }` is capped at the entitlement: `ceil((full - meter) / meter_per_unit)` less the pantry and this tick's pending requests, within the pantry cap; Food and Wares only in Phase 0. The standing plan files it every tick; `keep_food_at_least` is inert in store systems. Phase 6 serves everyone when the stock suffices, else need-first (largest request first, ties by the tick RNG); one `Drew` per served request with an Explain. | S0.15a | provisional |
| Q50 | Adding an `Event` or `Command` variant in the middle of its enum changes every later variant's postcard tag and invalidates every golden. | New variants are appended at the end of the enum (and of `Event::ALL_KINDS` and `kind()`), under a `Phase 0b` comment. | S0.15a | provisional |
| Q51 | A citizen joining a collective society whose dwellings are all occupied has no way to be housed until a dwelling frees up. | Society dwellings are assigned at join, at step 8b to every active unhoused citizen, and again after the 8l fill so a dwelling an emigrant released goes to whoever is waiting the same cycle end; dormancy and emigration release a society dwelling (GDD 9.3). | S0.15a | provisional |
| Q52 | Event payloads are JSONB (TDD 8), and Postgres re-emits numbers from its own `numeric`; do f64 fields (price index, remainders, aggregates) survive the trip byte-for-byte, as replay requires? | Yes, with `serde_json` built with `float_roundtrip`: without it the parser is best-effort and the S1.1 3-cycle replay diverged by one ulp in `TickResolved.price_index`. Enabled workspace-wide. Remaining caveat: `numeric` has no negative zero, so an engine `-0.0` would replay as `0.0` (equal under `PartialEq`, different bytes); no engine path produces one today, and the S1.1 snapshot+tail test would catch it. | S1.1 | provisional |
| Q53 | GDD 20 Q4 names `equal_shortfall` and `lottery` as rationing options without defining them; T8 wants both real in Phase 0b. | Equal shortfall: every requester gets `floor(stock x requested / total requested)`, and the leftover units go one each to requesters (who still have an unmet request) in tick-RNG order. Lottery: requests are served whole in tick-RNG order until the stock runs out; the request that hits the bottom takes the remainder. Both, like need-first, apply only when the tick's requests exceed the stock. | S0.15b | provisional |
| Q54 | GDD 6.2 says surplus beyond the draw entitlements is distributed as equal shares, without saying when, of which goods, or how the pantry cap of 48 and indivisible units are handled. | At cycle end (step 8b, after the tick's draws) each active citizen receives `floor(stock / active citizens)` of Food and of Wares from the Common Store, capped by their pantry room; the indivisible remainder and whatever the caps refused stay in the store. Other goods are not shared out (they are inputs and capital, governed by the Materials split). Each share is a `Drew` with `RuleId::StoreSurplusShare`. | S0.15b | provisional |
| Q55 | The Ledger of Contribution records output "as attributed under the society's monitoring"; the headless sim has no API to apply the sigma filter. | The ledger sums the phase-4 `attributed_output` figures, which already carry the monitoring noise; hours are the exact tick-hours. No second noise is applied anywhere. | S0.15b | provisional |
| Q56 | The rationing rule, the Materials split and the norm are assembly votes (Phase 2); Phase 0b needs them settable in the sim. | `SetPolicy { policy }` is accepted from `Actor::System` only (the sim's stand-in for the assembly, committee or legislature) and validated by `Policy::validate_against`, which rejects any field the constitution's axes do not use; the engine reads `world.policy` live at every step (rationing at phase 6, the split at phase 4, the norm at 8i), while `Rules` stays derived once per epoch and carries only the monitoring override. | S0.15b | provisional |
| Q57 | GDD 7.2 lets a pledge go to an org or to the assembly; contracts have two parties. | `Pledge` carries `to: Option<OrgId>`; `None` pledges to the assembly and the contract's parties are the citizen twice. Nothing is escrowed (non-enforced, recorded only); the ledger closes the pledge when its term ends with `met` for the hours half (this cycle's tick-hours at or above the pledged hours) and `None` for a goods-only pledge. | S0.15b | provisional |
| Q58 | Step 8g reset the per-cycle workplace accumulators (`cycle_output`, `cycle_tick_hours`, `cycle_attributed`) before 8i could read them for the ledger. | The reset moves to step 8m (`labor::reset_cycle_accumulators`, called by `metrics::cycle_end_8m_aggregates` after the aggregates are taken); nothing between 8g and 8m read them before, so every event stream is unchanged. Skill idle detection still resets `tick_hours_this_cycle` at 8g. | S0.15b | provisional |
| Q59 | The Materials split is a vote on where Materials go, but production is a per-tick pull from the society's stock. | The split is a consumption cap: each tick, each sink kind (Workshop for wares, Machine Shop for machines, Builders for dwellings) fed from a society stock may consume at most `floor(Materials on hand at tick start x its share)`, shared among that kind's workplaces in id order; the rest waits. Inputs other than Materials are uncapped, and orgs with their own inventory are unaffected. | S0.15b | provisional |
| Q60 | GDD 11.3 describes the householder only in market terms (best offer, buy needs, sell surplus); the Commune and Directorate have no offers, no prices and nothing to sell. | One script with a branch per labor axis: under a work norm the householder joins the least-staffed workplace and works the published norm at normal effort; under assignment (S0.16c) it accepts its assignment; in free-labor systems it takes the best offer as before. Draws and purchases come from the standing plan; nothing is sold where there is no market. `SCRIPT.md` documents each branch. | S0.15c | provisional |
| Q61 | Who manages a seeded collective or state enterprise, and what does its manager do without prices, wages or hiring? | The householder round-robin manager stays (coordinators and the Committee are Phase 2). Its script has one action: each round, install one Machine from the society's stock at each of its workplaces except a Machine Shop, so the society's Machines spread across the orgs in citizen order. | S0.15c | provisional |
| Q62 | Norm systems have no employment offers, so nothing places a citizen at a workplace; GDD 6.3 wants an algorithm "balancing the Plan" for assignments too. | `JoinWorkplace`/`LeaveWorkplace` (norm systems only) create and drop contract-less positions, within `max_workers_per_workplace` and `max_workplaces`. The householder joins `orgs::least_staffed`: the workplace with room whose workers-per-weight is lowest under `params.labor.balance_weights` [new tunable], ties to the lowest id. The same rule is the Committee's assignment at join in S0.16a. | S0.15c | provisional |
| Q63 | `unemployed` counted citizens without an employment contract, which is everyone in a norm or assigned system. | `unemployed` = active citizens holding no position at any workplace (`labor::has_position`). Identical in Freeport, where assignments exist only through contracts (Q16). | S0.15c | provisional |
| Q64 | "No persistent stock-out" was read from resting Food asks, which never exist in a store society; in the Commune the surplus share empties the store every cycle end by design. | The simulator flags a cycle as stocked out by what stock-out means in that system: no Food asks resting at cycle end (market), or Food requested from the store and not served during the cycle (Common Store and, from S0.16, the state stock). The `food_unfilled` and `stocked_out` columns carry it. | S0.15c | provisional |
| Q65 | GDD 6.3 guarantees a "dwelling assignment"; TDD 5.2 gives the Directorate zero-rent state leases. | Directorate dwellings are assigned exactly as the Commune's (Q51): built by state Builders they are `Owner::Society`, handed out at join, at 8b and after the 8l fill, no lease contract, no rent. Leases stay enabled by the constitution for anything private a human might build later. | S0.16a | provisional |
| Q66 | A ration card is "an equal cap per citizen" with no value, period or good named. | `policy.ration_caps: Option<BTreeMap<Good, u32>>` (administered societies only, set by `SetPolicy`): units per citizen per cycle, tracked in `StateStock.issued_this_cycle` and cleared at `CycleClosed`. None by default: cards are the Committee's response to a shortage, not the normal state. | S0.16a | provisional |
| Q67 | State enterprises have treasuries but nobody to pay from them and no sales of their own; the state store has a till. | The state is one purse: the till receives the seeded legacy budget (one `legacy_treasury` per seeded enterprise) and every sale, and pays state wages (S0.16b) through `ledger::payer_of`. State enterprise treasuries stay at zero. | S0.16a | provisional |
| Q68 | "A minimum Food ration at zero price" has no value, period or mechanism. | At step 8b every active citizen's pantry is topped up to `policy.minimum_food_ration` (24) from the state stock at zero price, in citizen order while the stock lasts (`RationIssued`, `RuleId::ProvisionRation`). At 24 a citizen who buys nothing still eats: the provision floor is, as configured, the whole of a day's Food. The S0.16c tuning pass decides whether the Committee's policy keeps it there. | S0.16a | provisional |
| Q69 | The standing plan's `max_food_price` and standing orders have no meaning where prices are administered. | The administered executor files `RequestStateStore` for the plan's Food target and Wares rule at the list price, within the saving floor and pantry room; price limits are ignored (nobody bargains with the state store) and standing orders are already rejected by `SetStandingPlan` where there are no books. | S0.16a | provisional |
| Q70 | GDD 6.3 says overfulfilment raises next cycle's target with the ratchet on, without saying by how much; TDD's 1.05 is the sim planner's growth, not the ratchet. | At step 8a, after payroll, every workplace whose output exceeded its target gets `TargetSet { target: max(target, output x ratchet_mult) }` from the System; `params.governance.ratchet_mult` = 1.0 [new tunable]: the ratchet fixes what was achieved, and growth is the planner's job. | S0.16b | provisional |
| Q71 | TDD 5.3 gives `SetPlan { targets, materials_split, price_list, wage_grades }`; GDD 6.3 also lets the Committee impose ration cards. | `SetPlan` carries `ration_caps` too. Targets are `PlanPublished` (per workplace, `None` untouched); any policy part present is applied as one validated `PolicyChanged`. `System` only until Phase 2. | S0.16b | provisional |
| Q72 | Wage-scale pay has no contract, so the T18 breach rules (pro rata plus `PaymentMissed`, the contract ends, the org flagged) do not apply. | A short till pays every position pro rata, wages and bonuses alike, with the till in the Explain; nothing ends, nothing is flagged: the state cannot fire its own citizens for its own shortfall. | S0.16b | provisional |
| Q73 | "Grade by job family and skill band" (GDD 5 A3) and a grade table "by skill band of 20" (directorate.toml) name no formula. | Grade index = `floor(skill in the workplace's job family / params.labor.skill_band)` [new tunable, 20], capped at the top grade; pay = grade x tick-hours / ticks per cycle, floored to the cent (Q5), one `Paid` per position per cycle with `RuleId::PayScale`, and a second with `RuleId::PlanBonus` = floor(pay x plan_bonus_fraction) when the workplace's output reached its target. | S0.16b | provisional |
| Q74 | The planner and the scoreboard need each workplace's last cycle output and fulfilment, but the live world's `cycle_output` lags a tick behind the scratch world at cycle end. | `Workplace.{last_cycle_output, last_fulfillment}` are set at 8m on the scratch world before the reset and carried by `WorkplaceDelta`; `CycleAggregates.plan_fulfillment` is their mean over targeted workplaces, computed at 8m. | S0.16b | provisional |
| Q75 | Q68 read the minimum ration as a top-up "to 24", which makes buying Food pointless (whoever buys holds 24 anyway) and turns the whole of a day's Food into a gift. | A ration is a fixed issue: every active citizen receives `minimum_food_ration` units at zero price at each cycle end, within their pantry room and while the stock lasts; the Committee sets its size in policy (`directorate.toml`: 8, a third of a day), so citizens buy the rest at the list price and the state has takings to pay wages from. Q68 is superseded. | S0.16c | provisional |
| Q76 | Who plays the Planning Committee before Phase 4, and how are its rejections treated? | `planner::decide_system` (T10): on the first tick of each cycle it publishes targets = last cycle's output x `sim_planner_target_growth` for every workplace that produced, and approves every pending transfer; the simulator runs it before the householders each round and counts its rejections with theirs (a rejection is a script bug). Prices, grades, the split and ration cards stay at the preset's policy. | S0.16c | provisional |
| Q77 | The Directorate's levers (price list, wage grades, ration) are policy, which `--param` could not reach, so its tuning pass could not sweep them. | `load_preset_with_overrides` accepts `policy.<key>` overrides alongside `params.<section>.<key>`; the sim's `--param` passes both through. | S0.16c | provisional |
| Q78 | TDD 14 says argon2 for any secret at rest; the S1.3 secrets (magic-link tokens, session tokens, API keys) are random 256-bit strings, and argon2 on every API-key request would cost tens of milliseconds against a 50 ms p99 budget (TDD 17). | Random tokens are stored as SHA-256 hashes (a slow hash protects low-entropy passwords, which none of these are); the key itself is shown once and never stored. If a password ever exists it gets argon2. | S1.3 | provisional |
| Q79 | GDD 6.4 taxes "income" without saying what counts, or when. | The base is money a citizen received as `Paid` (wages, notice pay), `DividendPaid` and, later, coop share-outs and strike pay, accumulated in `Citizen.taxable_income` since the last assessment; sales, rent received, transfers and the need floor are not income. Step 8b assesses every non-dormant citizen with a positive base (a `TaxAssessed` even at zero tax, so the base resets); a dividend paid at 8e is taxed at the next cycle's 8b. | S0.17a | provisional |
| Q80 | Bracket semantics: `tax_brackets = []` in the Republic preset, and "rate and brackets" in the GDD. | Marginal: income up to the lowest bracket's threshold at `tax_rate`, each slice above a threshold at that bracket's rate, brackets sorted ascending, every slice floored to the cent; an empty or absent list is a flat tax. | S0.17a | provisional |
| Q81 | The need floor covers "Food and Shelter" (GDD 6.4) but the preset has only `need_floor_food` and `public_dwellings = 0`; is the floor money or goods? | Money: at 8b, after the assessment, every active citizen whose balance plus pantry Food at the last Food price falls short of `need_floor_food` units at that price is topped up from the treasury, largest shortfall first, until it runs dry (`NeedFloorPaid`, `RuleId::NeedFloorTransfer`). Shelter is `policy.public_dwellings` society-owned dwellings (seeded as Builder-built and transferred to the society) assigned rent-free to the unhoused by the shared step 8b rule (Q51); the treasury does not buy dwellings yet. | S0.17a | provisional |
| Q82 | Minimum wage enforcement on piece rates, and what 0 means. | `OfferEmployment` is rejected (`BelowMinimumWage`) when an hourly rate is below `policy.minimum_wage` or a piece rate times the workplace's base rate is; 0 or absent means no enforcement. `republic.toml` sets 6.00, real but below the legacy wage of 8.00, so the householder economy is unaffected. | S0.17a | provisional |
| Q83 | Event payloads are JSONB and JSON maps can only have string keys; `Ownership::Shares.holdings` is keyed by `ShareHolder`, a newtype variant, so `OrgFounded` for a human founder failed to serialize (S1.4 found it: the first human-founded firm returned 503). | `ShareHolder` serializes as a string (`citizen:7`, `org_self`) with a custom serde impl; goldens regenerated. Rule for every track: a `BTreeMap` inside an event may only be keyed by a type whose serde form is a string or an integer (unit enums, ids, `String`); anything else needs a string form or a `Vec` of pairs. | S1.4 | provisional |
| Q84 | TDD 10.3 says the API decides who sees what per the constitution but lists only true vs. attributed output, DMs, org channels and manager views. Which events are private? | `Paid`, `PaymentMissed`, `DividendPaid`, `CreditInstallment`, `CreditMissed`, `CreditRepaid`, `CreditDefaulted`, `RentPaid`, `RentMissed`, `Transferred`, `CitizenSeen`, `PlanChanged`, `LaborSet`, `Drew`, `StoreDrawRequested`, `StoreReturned` are readable by their parties and by the manager of an org named in them; `Produced` and `TickResolved` are rewritten per viewer; everything else (trades, orders, offers, foundings, hardship flags, cycle aggregates) is public, per the GDD's legible-state pillar. | S1.4 | provisional |
| Q85 | TDD 8 lists a `notice_board` projection table and TDD 12 says the board is a projection of live `Offer` objects. | No table: `GET /s/{id}/notice-board` reads the live `World.offers` (S1.4), which is exactly the projection and can never lag or need a rebuild. Only the Chronicle and free-text messages have tables. | S1.5 | provisional |
| Q86 | TDD 5.2 gives the Commonwealth `employment (membership)` contracts; GDD 6.5 says coops admit members by vote and share the surplus. Which is it? | Membership replaces employment inside a cooperative: `OfferEmployment` by a coop is `NotAuthorized`, `AdmitMember` (the manager, until the S0.17c vote) admits an unplaced citizen and places them at the coop's least-crowded workplace with room as a contract-less position; a new member's other coop requests lapse (one coop at a time). Associations keep the S0.11 membership rules. | S0.17b | provisional |
| Q87 | Are coop members paid a wage during the cycle, a share at the end, or both? | Only the share-out: no wage advance. `ShareOutPaid` counts as wages for the householder's saving and rent rules and as taxable income. Because a member's income is the coop's residual, the householder script gives members the mobility GDD 6.5 grants: after two cycles, one whose coop shared less than a cycle's living cost moves to a coop with an open place that shared more (the steward stays: a coop without a manager is dead, and at seeding every steward is alone). The declaration is made every cycle, zero included, so the share per member that guides the choice is never stale. Without it, coops whose asks lose the price-time race at identical cost-plus prices leave their members with nothing. | S0.17b | provisional |
| Q88 | "Surplus" is never defined (revenue less what, measured when, before or after the levy?). | `surplus = clamp(treasury - surplus_base - obligations due, 0, treasury)` at step 8a, where `surplus_base` is the treasury at the last cycle close plus capital received since (seed money, loan principal), and the obligations are this cycle's loan installments and the capital levy. Escrowed bids lower this cycle's surplus and lift the next. Equal: floor(surplus / members) each; hours-weighted: by the member's tick-hours at the coop's workplaces this cycle; the remainder cents go one each in id order; the sum is exactly the surplus. | S0.17b | provisional |
| Q89 | "Leaving forfeits" (GDD 6.5). Forfeits what? | The leaver's hours drop out of this cycle's share-out and there is no capital claim: `LeaveOrg` and emigration emit `MemberLeft` plus `Unassigned` for every position at the coop, nothing is paid out. | S0.17b | provisional |
| Q90 | Who manages a seeded cooperative before the members can elect anyone? | The round-robin householder manager, admitted as the coop's first member and placed at its workplace at seeding; `AppointManager` by the System is the election stub for a coop. | S0.17b | provisional |
| Q91 | The legacy manager's machine rule and input reserve are in cycles of payroll, which a coop has none of. | A coop reserves its obligations due plus a cycle of what its members would earn at the legacy wage before bidding for inputs (with no reserve a Mill spends every credit on Grain and shares out nothing), and buys a Machine when its treasury (less what this round's bids committed) exceeds `legacy_machine_buy_payroll_mult` times what its members would earn at the legacy wage plus the price (a threshold keyed to the last share-out ratchets to zero: a coop with no surplus yet would turn every credit into Machines and its members would starve); hiring becomes admissions, oldest request first, while a workplace has room and the coop is not glutted; a glutted coop (Builders: unlet dwellings) buys no inputs. Householders spread their requests by open places and leave a coop that shared less than a living for one that shared more (Q87). | S0.17b | provisional |
| Q92 | The Public Investment Bank is "funded by a capital levy" and "lends"; the TDD names no holder for the pool and the Commonwealth's "basic provision of Food" has no purse. | The bank is a seeded society-owned association (`bank::bank_org`), its treasury is the levy pool (no new holder; conservation unchanged), its loans are ordinary credit contracts with the bank as lender (S0.11 installments and defaults apply and flow back to the pool). At 8b, after the levy, the pool pays the society's basic provision as a money floor of `minimum_food_ration` Food at the last price (`tax::need_floor`, as the Republic's treasury does); lending gets what is left. | S0.17c | provisional |
| Q93 | "A small % of machine value per cycle": which machines, at what value? | Installed Machines at the cooperative's workplaces (inventory Machines are stock) times the last Machines price (trade, else start price), times `capital_levy`, floored to the cent, paid at 8b into the pool and reserved by the coop's share-out (Q88). | S0.17c | provisional |
| Q94 | The lending rule "formula or vote" has no formula. | First come first served: applications (`RequestBankLoan { org, principal, term_cycles }`, a coop manager, within `params.bank.max_loan_credits` and `max_term_cycles` [new tunables]) are decided in offer order at 8b; granted when the pool covers the principal, the coop has no open bank loan and has never missed a payment, at `params.bank.rate_per_cycle_bp` [new tunable], no collateral, and the first installment falls at 8c of the same cycle end (as for peer credit accepted on a cycle's last tick); declined for an open loan or a missed payment; otherwise the application waits. `lending_rule = vote` is treated as the formula until the bank board can vote (Phase 4). The coop manager applies for the price of one Machine when it cannot afford one. | S0.17c | provisional |
| Q95 | "Coops admit new members by vote" (GDD 6.5); the manager's `AdmitMember` has stood in. | `ProposeAdmission` (a member; the proposer's vote is a yes) and `VoteAdmission` (one vote per member): more than half the members admits and places the candidate at the least-crowded workplace; half or more against fails; proposals still open at 8j lapse (votes close each cycle). `AdmitMember` by the manager stays as the householder script's path. `Proposal` gains `kind` and `votes`. | S0.17c | provisional |
| Q96 | `OfferCredit`/`AcceptCredit` were enabled by `public_credit` too, so peer lending existed in the Commonwealth. | Peer credit needs the `credit` contract; `public_credit` enables only `RequestBankLoan`. The S0.17b test that lent to a coop by command now applies the credit events directly. | S0.17c | provisional |
| Q97 | The householder saves a tenth of lifetime wages (`save_fraction`) and never spends it; a coop member whose share-outs stop starves with a hundred credits in hand, and the need floor counts that reserve as means. | Hunger before savings: when the pantry holds no Food the balance reserve does not apply to the Food bid, so a saver dips into savings only when there is nothing left to eat. Universal (the plan is the same in every money society): Freeport and the Republic move by a few hundred events over five epochs (a handful of their unemployed reach an empty pantry under the reserve) and both re-run green on seeds 1-5; the Commune and Directorate are event-for-event unchanged. | S0.17c | provisional |
| Q98 | A coop's Machine rule is keyed to its members' wage-equivalent, so a coop of one with a steady income (the Builders' rent) put every credit into Machines: 350 Machines for one steward, and every Materials unit the Foundries made went to the Machine Shop. | The steward buys no Machine, and applies for no loan, past `params.coop.max_machines_per_member` per working member (12 [new tunable]: beyond it the capital multiplier's log gains under a percent per Machine). Firms keep the payroll-scaled rule. | S0.17c | provisional |
| Q99 | A member moves to a coop "with a place open that shared more" (Q91) and a citizen asks the coop with an open place, but a glutted coop admits nobody (the Q2 hiring cap), so in a glutted economy movers left their coops and landed nowhere: ten of forty citizens unplaced for half an epoch. | The glut test moves into the coop module (`coop::glutted`, `coop::admitting`) and both the request and the mobility rule count only coops that would admit someone this hour; a member of a poor coop stays put when no coop is admitting. Tried and dropped in the same pass: letting a coop under its reserve bid for inputs with half of what it holds (it produced its way into a deeper glut and need fell to 87 %). | S0.17c | provisional |
| Q100 | The GDD 17 "materials sinks balanced" target was checked as the mean of per-cycle `investment_share` (Materials to the Machine Shop over Materials made that cycle); a cycle in which the Foundries made two units and the Machine Shop consumed thirty-eight scores 19 and carries an epoch whose honest share is 0.16. | An epoch's investment share is the ratio of sums over its cycles (`CycleAggregates.{materials_produced, materials_to_machines}` [engine]; the per-cycle ratio stays). The four earlier reports' `invest` columns were means of ratios; their epochs stay inside the band under either statistic (re-run in S0.17c). | S0.17c | provisional |
| Q101 | Strike pay "from dues" (GDD 6.4) has no numbers anywhere. | `params.union.dues_credits_per_cycle` = 0.50 (paid at 8a by every member who can) and `strike_pay_credits_per_cycle` = 32.00 (half a cycle at the legacy wage) [new tunables]; a striking member receives `min(strike pay, union treasury / members)` each cycle end while the strike runs, so the union never pays out more than it holds. Strike pay counts as income. | S0.17d | provisional |
| Q102 | A collective agreement's `hours` term (TDD 6): a floor, a cap, or a norm? | A cap: a union member may allocate at most the agreed hours at the firm (`OverContractHours`), and is paid at least the agreed floor per hour whatever their contract says, while the agreement is active; the floor also raises `wage_floor` for the firm's new offers. Non-members at the same firm are unaffected. One agreement per firm at a time; it ends at term like an employment contract. | S0.17d | provisional |
| Q103 | Should householders unionise, so the Republic's tuning exercises strikes? | No: unions, agreements and strikes are built and unit-tested and never triggered by a script (GDD 11.3 gives the householder no such move); the Republic report is unchanged and a strike is the first thing a human worker can do that a householder cannot. | S0.17d | provisional |

Structural deviations recorded as ADRs: seeding as events (ADR-0003), the `Seeded` ledger term and legacy treasuries (ADR-0004), `OrgSelf` share holder (ADR-0005). Continuous matching is ADR-0001 (S0.8). The make-on-Windows choice is ADR-0002 only if the GNU make install had failed; it succeeded, so no ADR.
