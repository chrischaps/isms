# Sessions

Append-only log. One entry per session card (template: `docs/tdd.md` Appendix C).

## S0.1 — Repository scaffold — 2026-09-12 — PR #1
Built: Cargo workspace (edition 2024, resolver 3, pinned workspace deps, `unsafe_code = forbid`, clippy pedantic as warnings promoted to errors by `make check`), six empty crates, `web/` Vite 8 + React 19 + TS 6 placeholder with `pnpm check` (tsc, oxlint, vitest), Makefile, CI workflow, `presets/_base.toml` with every GDD App. A value and TDD App. A tunable plus the new ones from QUESTIONS/ADRs, five preset TOMLs, lexicon placeholders and `KEYS.txt`, Freeport welcome copy, `CLAUDE.md` (TDD App. D plus the libm and Windows notes), `docs/` layout with QUESTIONS.md pre-seeded (Q1–Q18).
Deviations from TDD: the Vite template now ships oxlint instead of ESLint; kept oxlint (same role, zero config). `make` on Windows is GNU make 3.81 from GnuWin32, run from Git Bash; recipes are plain cargo/pnpm calls. No ADR needed.
Provisional answers added to QUESTIONS.md: Q1–Q18 (from the pre-build TDD review); ADR-0003/0004/0005 are reserved for S0.3a/S0.4.
New tunables: `legacy_treasury_credits` (384), `legacy_hire_inventory_cycles_cap` (2), `legacy_offer_max_hours` (8), `legacy_offer_notice_cycles` (1), `living_cost_food` (24), `living_cost_wares` (4), `meter_start` (100), `food_full_output_meter` (50), per-good pantry caps, Republic `tax_rate`/`need_floor_food`, Commonwealth `capital_levy`.
Next session should know: toolchain pinned to 1.92.0; `_base.toml` keys are nested by topic (`params.labor.*`, `params.needs.*`, …) and money keys carry a `_credits` suffix that the S0.2 loader converts to cents. `presets/test/` is reserved for test-only fixtures. arm64 CI runners are not free on a private repo (affects S0.13b).
make check: green · new tests: 1 (web placeholder) · sim-check: n/a

## S0.2a — Core types, Params, preset loader — 2026-09-12 — PR #2
Built: `isms-core` modules `ids` (u32 newtypes, Tick/Cycle/Epoch aliases), `kinds` (Good, Product, WorkplaceKind with `job_family()`, JobFamily, Need, Effort + EffortTable, OrgKind, ContractKind, Channel, ClientKind incl. `Plan` per Q6, CitizenKind), `money` (Money cents newtype with credits<->cents serde helpers, exact-to-the-cent check), `constitution` (eight axis enums, OfficeSpec, Constitution with `has_money()`), `policy` (Policy with per-preset Option fields; credits fields renamed `*_credits` in TOML), `params` (nested Params mirroring `_base.toml`, `deny_unknown_fields`, no defaults), `config` (base + preset deep-merge overlay, Preset struct, cross-axis validation, `load_all`). Presets now use one `seeded_workplaces` key instead of legacy_firms/legacy_coops.
Deviations from TDD: constitution sets are `BTreeSet` rather than `EnumSet` for canonical serialization; `EnumSet` is reserved for `Capabilities` (S0.2b). The cross-axis constraints were placed in the loader now (Q14) rather than waiting for S0.2b; S0.2b adds the invalid-combination fixture tests.
Provisional answers added to QUESTIONS.md: none new.
New tunables: none beyond S0.1's.
Next session should know: `WORKSPACE_PRESETS_DIR` resolves presets relative to the crate at compile time (tests and sim); `Preset` round-trips through JSON (credits serialized as floats). `Capabilities`, the per-preset capability table test, lexicon KEYS test, and six invalid-combination tests are S0.2b.
make check: green · new tests: 11 · sim-check: n/a

## S0.2b — Capabilities, constraint fixture, lexicon test — 2026-09-12 — PR #3
Built: `Capabilities::derive(constitution, policy, params)` with the TDD 5.2 fields (money/order_books/administered/common_store flags, contract and org sets, labor/pay/capital/redistribution/governance, monitoring as overridden by policy plus the resolved sigma, offices, published land slots and rate limit) and `allows_contract`/`allows_org`; the per-preset capability table test; eight invalid axis-combination tests; Params TOML round-trip; preset-name-mismatch test; `lexicon` module (`required_keys` from KEYS.txt, `load_lexicon` with completeness check) and the every-preset-defines-every-key test; `scripts/pr-cycle.sh` (push, PR, wait for CI to register and pass, squash-merge).
Deviations from TDD: `Capabilities` uses `BTreeSet` instead of `EnumSet` (canonical list serialization for the API, no derive conflicts); `enumset` removed from the workspace. `proposal_kinds` is omitted until Phase 2 defines `ProposalKind`. `Capabilities::allows(&Command)` arrives with `Command` in S0.4.
Provisional answers added to QUESTIONS.md: none new.
New tunables: none.
Next session should know: PR #2 was merged before its CI check registered (the watch returned "no checks reported"); main's CI run for it passed afterwards. Use `scripts/pr-cycle.sh` from now on. S0.2 done gate is complete.
make check: green · new tests: 10 · sim-check: n/a

## S0.3a — World, events, apply, ledger — 2026-09-12 — PR #4
Built: `world` (World with SocietyMeta, Citizen/Household/LaborState/Skill/Needs/CitizenFlags/StandingPlan, Org with `Ownership::Shares { issued, holdings: BTreeMap<ShareHolder, u64> }`, Workplace/Assignment, LandRegistry from `params.land`, Dwelling/Owner, Instrument/Order/OrderBook, CommonStore, StateStock with a `till`, Contract/ContractBody, Offer/OfferBody, Offices/Proposal placeholders, `EscrowKey`-keyed escrow and share escrow, NextIds; `canonical_bytes()` = postcard, `hash()` = blake3); `event` (all TDD 5.4 variants with payloads, `Actor`, `WorkerOutput`, `CitizenDelta`/`WorkplaceDelta` carrying new absolute values, `CycleAggregates` stub, `kind()` and `ALL_KINDS`); `explain` (RuleId closed enum with `doc()`, Num, Explain builder); `ledger` (Party, Holder, Asset, LedgerMeta with seeded/produced/consumed/depreciated/burned, `conservation_check` incl. negative-balance and per-org share checks); `apply` for SocietyCreated, EpochStarted, Seeded, CitizenJoined, HouseholderJoined, PlanChanged, LaborSet, Transferred, TickResolved, with `UNIMPLEMENTED` registry and a test that every kind is exactly one of implemented or listed. ADR-0003/0004/0005 written.
Deviations from TDD: `TickResolved` deltas carry the citizen's new state (meters, budget, debt, skill) rather than arithmetic deltas; food/wares eaten leave the pantry to the `consumed` sink inside the same event (still no holder-to-holder movement, per D4). `Ledger` is not a separate struct: holders are the World's own fields and `ledger::holdings` sums them. `LedgerMeta.burned_money` is separate from per-good `burned`.
Provisional answers added to QUESTIONS.md: none new (ADRs cover the structural ones).
New tunables: none.
Next session should know: postcard cannot deserialize `#[serde(untagged)]`, `#[serde(flatten)]`, or fields with `skip_serializing_if`; none may appear anywhere in `World` or `Event`. The credits helpers now deserialize plain `f64`. `apply::credit`/`debit` are `pub(crate)` movers for later cards; escrow holders are keyed in `world.escrow` and moved by the market/contract events (S0.7/S0.8). S0.3b builds `test_support` (WorldBuilder that emits events, Harness with fold==live, proptest strategies, golden helper).
make check: green · new tests: 9 · sim-check: n/a

## S0.3b — Test harness — 2026-09-12 — PR #5
Built: `isms_core::test_support` (feature `test-support`, also on under `cfg(test)`; the crate dev-depends on itself with the feature so integration tests see it): `WorldBuilder` (humans/householders/pantry/balance_extra/with_preset/seed; builds only by emitting events), `Harness` (apply/apply_all with conservation + fold==live after every step, `check_every_step` toggle), `fold`/`assert_fold_equals_live` (JSON field diff on mismatch), `check_golden` (postcard bytes + .jsonl twin, `UPDATE_GOLDEN=1`), `assert_deterministic`, and `strategies` (proptest `Step`s named by index and fraction, resolved against the live world so every generated event is well-formed; `arb_scenario`, `run_scenario`). `tests/harness.rs` holds the S0.3 gate: builder/fold/golden/determinism tests, `apply` never panics + conserves (Freeport and Commune), meters and balances in range. First golden `s0_3b_builder` committed.
Deviations from TDD: none. `Harness::cmd`/`tick` arrive with `handle`/`tick` in S0.4.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the crate's `Step` enum is the place to add command-shaped steps once `handle` exists; keep every step resolvable (never generate an id or amount that could be invalid). S0.3 done gate complete.
make check: green · new tests: 9 (+3 proptests) · sim-check: n/a

## S0.4 — Clock, RNG, tick skeleton, handle skeleton — 2026-09-12 — PR #6
Built: `tick` module (`TickInput::next_for` with `derive_seed` = blake3(society_seed, epoch, tick); `TickError::{EpochEnded, WrongTick}`; `TickBuilder` on a scratch clone with `emit` applying to the scratch world, seeded `ChaCha8Rng`, per-tick shuffled active citizens; the ten phases and the 8a–8m cycle-end steps as named functions; 8m emits `CycleClosed` with population counts and the collapse counter; phase 9 emits `EpochEnded { Scheduled }` after the last cycle or `{ Collapse }` when `collapse_enabled` and the low-population counter reaches `collapse_cycles`; phase 10 appends `TickResolved`; `start_epoch(world, rules, epoch) -> Vec<Event>` per ADR-0003, body deferred). `command` module (`Envelope` with `system`/`citizen`/`on_behalf_of`/`via` builders, the full `Command` catalog, `RejectCode` closed enum, `Reject`, `Capabilities::allows(&Command)`, `handle` gating epoch-ended then capabilities then dispatch; `Join` (endowment with Explain where money exists, handle uniqueness), `Seen` (throttled per tick), `EndEpoch` (operator) implemented; everything else `NotImplemented`). `rules` module. `apply` now handles `CitizenSeen`, `CycleClosed`, `EpochEnded`. `SocietyMeta.low_population_cycles` added; `CycleClosed` carries it. Harness gained `cmd`, `cmd_dry`, `tick`, `try_tick`, `run_cycle`, `run_cycles`.
Deviations from TDD: `TickInput` carries the derived 32-byte seed rather than the raw inputs; the collapse counter lives in `SocietyMeta` and is set by `apply(CycleClosed)` so replay reproduces it.
Provisional answers added to QUESTIONS.md: none (Q7 and Q10 applied).
New tunables: none.
Next session should know: fixtures with fewer than 40 humans collapse at cycle 4 unless `collapse_enabled` is set false via `WorldBuilder::with_preset`. `Harness::cmd` takes `Envelope` by value. `TickBuilder.citizen_deltas`/`workplace_deltas` are `BTreeMap`s keyed by id so phase code can update a delta more than once per tick. S0.4 done gate complete.
make check: green · new tests: 9 · sim-check: n/a

## S0.5 — Needs, consumption, hardship — 2026-09-12 — PR #7
Built: `needs` module: meters in integer tenths (`TENTHS`, `FULL`; `Needs::at_start`), `effort_for_decay` (Q19), `output_multiplier(needs, housed, destitute, params) -> (f64, Explain)` (Food term linear 1.0 -> floor over 50 -> 0, unhoused x0.7, destitute floor 0.25), `phase_5_needs` (auto-eat +4, Wares when Comfort can absorb +6, decay per effort 3.2/4/5.2, Shelter -2 unhoused / +2 housed, Comfort -1 or -2 unhoused, destitute Comfort forced to 0, clamp, low-food tick counting, next tick's `output_mult`, consumption to the ledger), `cycle_end_8h_hardship_and_fatigue` (hardship only when every tick of the cycle was below 20, consecutive counter, destitution at 3, fatigue debt = min(cap, high-effort half + hardship half), `budget` = base - debt, Hardship/Destitution Began/Ended events). `apply` handles the four flag events and `CitizenDormant`/`CitizenReturned` (flag only; freeze semantics are S0.9). `LaborState.output_mult` added. `TickBuilder.consumed` and phase 10 now builds a `CitizenDelta` for every non-dormant citizen from the scratch world. Harness gained `set_needs` (synthetic delta that does not advance the clock).
Deviations from TDD: meters are `u16` tenths rather than 0–100 integers so the GDD's fractional rates are exact; the multiplier's `Explain` is computed on demand by `output_multiplier` rather than carried in every delta (TickResolved size budget).
Provisional answers added to QUESTIONS.md: Q19 (decay effort = highest active allocation), Q20 (eating cannot lift the meter: hardship is sticky as specified; flagged for tuning and for Chris).
New tunables: none.
Next session should know: `LaborSet` to a `WorkplaceId` that does not exist is accepted by `apply` (tests use it to set effort); S0.6's `SetLabor` command must validate the assignment (Q16). High-effort fatigue reads `consecutive_high_effort_cycles`, which S0.6 maintains.
make check: green · new tests: 8 (+1 proptest) · sim-check: n/a

## S0.6a — Labor and production — 2026-09-12 — PR #8
Built: `labor` module: `skill_level` (k ln(1 + h/h0), capped, via libm), `skill_mult`, `capital_mult` (1 + coeff ln(1 + machines/workers), 1.0 with no workers), `phase_3_labor` (integer tick-hours per assignment, pro-rata to budget, Q4/Q16), `phase_4_production` (per-worker true output base x skill x effort x needs x capital x hours with an Explain, attribution = true x max(0, 1 + N(0, sigma)) sampled only when sigma > 0, integer units with carried remainder capped by inputs (Q21), input consumption and output to the org inventory, per-cycle worker accumulators for payroll, skill accrual), `cycle_end_8g_skill_and_effort` (idle-family decay -1 per 10 idle cycles, high-effort streak counter, cycle accumulator resets), `set_labor` (max workplaces, assignment required, sum of hours within budget, duplicates rejected), `skill_of`. New events `Assigned`/`Unassigned` (Q22). `apply` handles `OrgFounded` (fee burned from the founder), `WorkplaceAdded` (Materials from org inventory, slot claimed), `Produced`, `Assigned`, `Unassigned`. `Assignment` gained cycle accumulators and `Skill` a per-cycle hours counter; `WorkplaceDelta` carries the accumulators. `WorldBuilder` gained `org`, `workplace`, `org_inventory`, `assign`.
Deviations from TDD: `SetLabor` uses integer tick-hours (Q5); the Builder produces nothing until S0.11 (Q17).
Provisional answers added to QUESTIONS.md: Q21 (input-capped remainder resets), Q22 (`Assigned`/`Unassigned` events).
New tunables: none.
Next session should know: skill grows within the very first tick, so second-tick expectations must include `skill_mult`; unhoused citizens run at x0.7 from tick 1. `max_workers_per_workplace` and land-slot limits are not enforced yet (S0.6b: `FoundOrg`/`AddWorkplace` commands, machines install/uninstall/depreciation, the 9th Farm and 7th worker rejections).
make check: green · new tests: 9 · sim-check: n/a

## S0.6b — Orgs, land slots, machines, depreciation — 2026-09-12 — PR #9
Built: `orgs` module: `managed_org` (manager check, `on_behalf_of` must agree), `slot_for` (slot-limited kinds need a free slot, `NoSlotAvailable`), `check_room` (`max_workers_per_workplace`, `WorkplaceFull`; S0.10/S0.16 call it before `Assigned`), `found_org` (firms/coops/associations; collectives are seeded; destitute cannot found; fee burned (Q9); first workplace moves 20 Materials from the pantry to the org then `WorkplaceAdded`), `add_workplace` (manager, org Materials), `install_machines`/`uninstall_machines`, `appoint_manager` (controlling owner for firms, current manager for member orgs, System for society orgs), `controlling_owner` (> 50% of issued, ADR-0005), `cycle_end_8f_depreciation` (wear += machines x rate; whole units emitted as `MachinesDepreciated` with Explain; fraction carried). `apply` handles `ManagerAppointed`, `MachinesInstalled`/`Uninstalled`/`Depreciated`. New tunable `founding.initial_shares` = 100.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: none.
New tunables: `initial_shares` (100).
Next session should know: the builder golden `s0_3b_builder` was regenerated because `SocietyCreated` embeds `Params` and a param was added; any param addition will do that until the S0.12 scenario golden replaces it as the meaningful one. S0.6 done gate complete (9th Farm and 7th worker rejected, machines and depreciation conserve).
make check: green · new tests: 7 · sim-check: n/a

## S0.7 — Money, transfers, escrow, direct sales — 2026-09-12 — PR #10
Built: `transfers` module: `acting_party` (citizen, or an org via `on_behalf_of` when the citizen manages it), `check_pantry_room` (per-good caps, Q12; orgs uncapped), `check_has`, `transfer` (money or goods to any citizen or org; self-deal rejected), `offer_sale` (escrows the seller's goods at offer time; shares and dwellings are `NotImplemented` until S0.10/S0.11), `accept_sale` (addressee check, price paid atomically, pantry caps both ways), `cancel_sale`, `post_wanted`/`remove_wanted`. `apply` handles `SaleOffered`/`Accepted`/`Cancelled`, `WantedPosted`/`Removed` with `EscrowKey::Offer` escrow. Strategies gained command-shaped `CmdStep`s run through `handle` with rejections ignored (`arb_cmd_scenario`, `run_cmd_scenario`).
Deviations from TDD: "escrow on both sides" is implemented as seller-side escrow at offer plus atomic payment at accept (a buyer who has not yet accepted has nothing to escrow).
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: `Transfer` is now real, so the S0.4 gating test expects `SelfDeal` rather than `NotImplemented` for a self-transfer. `CmdStep` is the place to add order steps (S0.8 drafts already do).
make check: green · new tests: 5 (+1 proptest) · sim-check: n/a

## S0.8 — Order books — 2026-09-12 — PR #11
Built: `market` module: `OrderBook` per instrument with per-tick VWAP accumulators, `place_order` (escrow money for bids at `remaining x limit`, goods for asks; pantry cap counts open bids; share instruments `NotImplemented` until S0.10; default expiry = end of the next cycle), continuous matching on submission at the resting order's price with price-time priority and partial fills, `cancel_order`, `phase_6_markets` (expiry, VWAP per instrument, basket price index over priced goods, Q23), `last_price` (trade, else start price, Q8), `depth`. `apply` handles `OrderPlaced`/`Cancelled`/`Expired`/`Trade` (price-improvement refunds to the bidder, fully filled orders removed, accumulators; `TickResolved` resets them and sets `World.price_index`). `TickResolved` gained `vwap`. Strategies gained `PlaceOrder`/`CancelOrder` command steps. ADR-0001 written. Golden `s0_8_order_tape` (20 scripted orders) committed.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q23 (index ignores Dwellings until they have prices).
New tunables: none.
Next session should know: the S0.7 conservation proptest now also cancels resting orders before asserting the escrow drains, because the shared `CmdStep` strategy places orders. `open_bid_qty` is `pub` for the plan executor (S0.9).
make check: green · new tests: 6 (+1 proptest) · sim-check: n/a

## S0.9 — Standing plan executor and dormancy — 2026-09-12 — PR #12
Built: `plan` module: `set_standing_plan` (validated against the constitution: labor plan kind, money rules, standing orders, vote defaults), `default_max_price` (last x 1.25 to the cent), `phase_2_standing_plans` for market systems (standing orders refreshed each tick or each cycle by cancel-and-replace, the Food shortfall bid at the limit within the saving floor and pantry room counting open bids, the Wares rule sized to refill Comfort, Q24), `cycle_end_8k_dormancy` (humans absent for 7 full cycles: open orders cancelled, then `CitizenDormant`; householders exempt), `away_digest`/`touches`. `Seen` from a dormant citizen emits `CitizenReturned`. `apply` suspends/reactivates the citizen's contracts on dormancy/return. Plan commands run through `handle` on the scratch world with `ClientKind::Plan` (Q6).
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q24 (Wares quantity and refresh semantics), Q25 (presence and the freeze).
New tunables: none.
Next session should know: every joined citizen carries the default plan (keep Food at 24), so any fixture with money and a resting Food ask will see plan bids; tests that need a quiet book set `keep_food_at_least = 0` via `PlanChanged`. Long-running fixtures without `Seen` go dormant at cycle 7; raise `dormancy_absent_cycles` in `with_preset` when that is not the point.
make check: green · new tests: 8 · sim-check: n/a

## S0.10a — Employment contracts and payroll — 2026-09-12 — PR #13
Built: `employment` module: `offer_employment` (manager-only, hourly or piece-rate, max hours, term, notice; the notice board's job ads), `accept_employment` (creates the contract and the `Assigned` assignment; destitute citizens cannot sign long terms, Q26), `terminate_employment` with the T18 rules (Q27), `accrued_pay`, `cycle_end_8a_payroll` (hourly from tick-hours, piece-rate from attributed output per T17, `Paid { explain }`, pro-rata `PaymentMissed` that ends the contract and flags the org), `employed`; `apply` arms for `EmploymentOffered/Accepted/Terminated`, `Paid` (wage counters) and `PaymentMissed`; `labor::set_labor` caps hours at the contract's `max_hours`.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q26, Q27, Q28.
New tunables: `contracts.long_contract_cycles` (5).
Next session should know: `Assigned`/`Unassigned` (Q22) are emitted alongside the employment events, so assignments are the only labor link the tick knows about. Two goldens regenerated for the new param. (backfilled in S0.14c from the code and QUESTIONS.md)
make check: green · new tests: 8 · sim-check: n/a

## S0.10b — Shares, share trading, dividends, net worth — 2026-09-12 — PR #14
Built: `shares` module: `holder_of`, `shares_held`, `citizen_held`, `issue_shares` (new shares land in the org's own holdings, ADR-0005), `declare_dividend` (controlling owner, one per org per cycle), `cycle_end_8e_dividends` (pro rata to the treasury, Q30), `book_value`, `share_value`, `net_worth` (balance plus holdings at last price, book value where no trade exists), `self_made` (net worth minus endowment), `control_change` (a fill or sale crossing 50 percent appoints the new controlling owner as manager, Q29); `Instrument::Share` on the order book (`market`), `OfferSale { Shares }` (`transfers`), `Org.declared_dividend`.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q29, Q30.
New tunables: none.
Next session should know: S0.10 done gate complete (payslips differ exactly as the formulas say, dividends split by share count and are rejected with no controlling holder, conservation across a full cycle with 3 firms and 12 workers). (backfilled in S0.14c from the code and QUESTIONS.md)
make check: green · new tests: 5 · sim-check: n/a

## S0.11a — Dwellings, leases, rent, evictions, owner-occupancy — 2026-09-13 — PR #15
Built: `housing` module: `owner_of`, `check_dwelling_sale` (an occupied dwelling keeps its tenancy, Q34), `offer_lease`/`accept_lease`/`end_lease` (either party, no notice, Q32), `move_in`/`move_out` (owner-occupancy, Q34), `cycle_end_8d_rent` (paid in full or missed, Q33), `phase_7_evictions` (after `lease_grace_cycles` misses, on the first tick of the next cycle; occupancy drives Shelter recovery); the Builder arm of production now emits `DwellingBuilt` (closes Q17); `LedgerMeta.dwellings_built`; `WorldBuilder::dwelling`. Workplace leases stay `NotImplemented` (Q31).
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q31, Q32, Q33, Q34.
New tunables: `contracts.lease_grace_cycles` (1).
Next session should know: a dormant tenant's rent is suspended with the contract (S0.9 freeze semantics). Dwellings are owned by orgs or citizens only; `Owner::Society` exists in the type for the collective presets and nothing creates it yet. (backfilled in S0.14c from the code and QUESTIONS.md)
make check: green · new tests: 6 · sim-check: n/a

## S0.11b — Credit, defaults, associations — 2026-09-13 — PR #16
Built: `credit` module: `schedule` (simple interest, equal integer-cent installments, remainder on the last), `offer_credit` (lender escrows the principal; `CreditOffered` now carries `by`), `accept_credit` (addressee, self-deal, destitute long-term, collateral ownership; share collateral escrowed under the contract), `cycle_end_8c_credit_installments` (`CreditInstallment` with Explain, `CreditRepaid`, or `CreditMissed`), `phase_7_defaults` (missed installments default on the first tick of the next cycle: collateral to the lender, contract ended, borrower flagged), `pledged` (a pledged dwelling cannot be sold or let), and associations: `request_membership` (`MembershipRequested`, new event), `admit_member` (manager), `leave_org` (a leaving manager vacates the chair). `ContractBody::Credit.missed` added; `CreditAccepted` carries the rate.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q35, Q36.
New tunables: none.
Next session should know: S0.11 done gate complete (the 100-credit, 5-cycle, 2% loan repays exactly 22 per cycle). S0.12a (seeding, fill, emigration) is drafted.
make check: green · new tests: 5 · sim-check: n/a

## S0.12a — Epoch seeding as events, householder fill and emigration — 2026-09-13 — PR #17
Built: `seeding` module: `seeded_org_kind` (firm, cooperative, state enterprise or collective by constitution), `start_epoch` for epoch 0 (one legacy org per `seeded_workplaces` entry named `Legacy <Kind> No. n` with `legacy_treasury_credits`, workplaces on land slots, `initial_dwellings` round-robin among the Builders, householders to the population floor, a householder manager per org round-robin; all of it events, ADR-0003, Q38), `cycle_end_8l_householder_fill` (`max(0, floor - active_humans)` with `HouseholderJoined`/`HouseholderEmigrated`; the most recently joined leave first and their balance and pantry are burned, T19, Q37); `WorldBuilder::seed_epoch`.
Deviations from TDD: none (ADR-0003 and ADR-0004 were written in S0.3a in anticipation).
Provisional answers added to QUESTIONS.md: Q37, Q38.
New tunables: none.
Next session should know: sim humans join with `Actor::System` and `ClientKind::Sim` (Q10) and never `Seen`, so they go dormant at cycle 7 and the fill refills; tests account for that. (backfilled in S0.14c from the code and QUESTIONS.md)
make check: green · new tests: 4 · sim-check: n/a

## S0.12b — Householder and legacy-firm scripts, runner, SCRIPT.md, 3-cycle golden — 2026-09-13 — PR #18
Built: `householder` module: `living_cost` (24 Food + 4 Wares at last price plus rent, Q15), `householder_plan` (Food target 24, Wares when Comfort is low and balance covers two living costs, save 10 percent, Q39), `decide` (keep the plan current; accept the best-paying open offer or `SetLabor` full hours; rent the cheapest dwelling within 25 percent of last cycle's income; ask surplus goods at last price), `cost_plus` and `decide_manager` (cost-plus asks at `legacy_markup`, input bids for the shortfall against one cycle's recipe demand at last price times markup, Q1; hiring at the median open offer or `legacy_wage` while inventory is under `legacy_hire_inventory_cycles_cap` cycles of sales, Q2; machine buys when the treasury exceeds `legacy_machine_buy_payroll_mult` cycles of payroll plus the price, Q43; dwelling leases at `legacy_rent`; a standing `OfferSale` of all shares at book value), `run_round` (every non-dormant householder in citizen order, then the orgs they manage, through `handle` and `apply`), `Rejection`; `Citizen.{wages_total, cycle_wages, last_cycle_wages}` rolled at `CycleClosed` (Q39); `docs/SCRIPT.md`; golden `s0_12_freeport_3_cycles`.
Deviations from TDD: `decide` takes `&World` and a citizen id rather than the TDD's `CitizenView`/`PublicView`; the views are an API concern (S1.4) and the script must see exactly what the API would show (Q40).
Provisional answers added to QUESTIONS.md: Q39, Q40, Q43 (and Q1, Q2, Q15 applied).
New tunables: none (`householder.legacy_machine_buy_payroll_mult` lowered 3 -> 1.0, Q43; raised to 1.5 in S0.14).
Next session should know: S0.12 done gate complete: 40 householders run 3 cycles with zero rejections, everyone employed and housed by cycle 2, legacy treasuries positive at cycle 3, a human buying a legacy share offer becomes the controlling owner and the householder manager steps down. Observed after 3 cycles at base rates: Food asks at about 1.31, Mills hold the largest stock; the full 42-cycle picture is in `docs/tuning/freeport-00.md` (S0.13b). (backfilled in S0.14c from the code and QUESTIONS.md)
make check: green · new tests: 5 · sim-check: n/a

## S0.13a — Cycle aggregates (TDD 13) and the epoch rollover — 2026-09-13 — PR #19
Built: `metrics` module: `CitizenCycle` and `WorldCycle` accumulators (continuous state in `Citizen` and `World`), `gini`, `consumption_score` (Food eaten + Wares at basket weight + housed ticks at `housed_tick_weight`), `aggregates` (every TDD 13 field that applies: real output, median wellbeing, need-fulfillment, consumption Gini, investment share, price index, mean cycle wage, unemployed, firm count, credit outstanding, hardship count, store stock), `record_tick`, `cycle_end_8m_aggregates` (emits `CycleClosed { aggregates }` and resets the accumulators); `start_epoch(world, n)` for n >= 1 with `apply::reset_material_state` (constitution, params, id counters and the human roster survive; everything material is cleared and re-seeded, Q41).
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q41, Q42 (API share deferred to the server).
New tunables: none.
Next session should know: the 3-cycle golden was regenerated because `CycleClosed` now carries aggregates. `params.metrics.mobility_window_cycles` is parsed and unused (mobility needs cross-cycle deciles; deferred). (backfilled in S0.14c from the code and QUESTIONS.md)
make check: green · new tests: 4 · sim-check: n/a

## S0.13b — Headless simulator, seed sweeps, first tuning report — 2026-09-13 — PR #20
Built: `crates/isms-sim`: `run` (forces `collapse_enabled = false`, `start_epoch` per epoch, `run_round` then `tick`), `Row` (one CSV row per cycle: every `CycleAggregates` field plus Food and Wares ask depth and the Food last price), `summarize`, `stability_failures` (the GDD 17 targets: need >= 95 percent, price index 0.7..1.3, Food stock-out at most 3 cycles, investment share 0.05..0.6, zero rejected householder commands), `table`, `write_csv`; CLI `run` with `--preset`, `--epochs`, `--seed`, `--seeds a..b`, `--param key=value` (applied before validation via `config::load_preset_with_overrides`), `--out`, `--check`; `make sim` and `make sim-check`; `.github/workflows/cross-arch.yml` (manual dispatch, x86_64 and arm64, replays the two goldens); `docs/tuning/README.md` and `docs/tuning/freeport-00.md`. Engine fixes found by the first 42-cycle run (Q44): legacy firms price on `reference_price` (labor at the legacy wage plus reference-priced inputs, times the markup) instead of the last trade, and plan bids are placed at the best resting ask or the last price and re-placed when a crossable ask appears above them.
Deviations from TDD: the cross-architecture job is `workflow_dispatch` rather than on every PR because arm64 runners are not free on a private repo; run it by hand at phase gates.
Provisional answers added to QUESTIONS.md: Q44 (Q26 revised: open-ended offers are short).
New tunables: none.
Next session should know: the first raw run fails three of four targets (need 2-4 percent, hardship 40, stock-out all epoch): the Q20 trap plus an empty day one. Five epochs of forty citizens take about a second. The investment-share band 0.05..0.6 is the simulator's own choice; the GDD asks only for a documented band. (backfilled in S0.14c from the code and QUESTIONS.md)
make check: green · new tests: 5 (4 + 1 ignored stability test) · sim-check: red (need-fulfillment; see freeport-00.md)

## S0.14b — Legacy inventory seeding (engine session pre-authorised by the plan) — 2026-09-13 — PR #21
Built: `params.seeding.legacy_inventory` (per workplace kind, goods seeded into each legacy org at `start_epoch`, counted in `seeded`; ADR-0004, Q45): Mills start with 320 Food and 120 Grain, Foundries with 60 Ore. Seeding test extended; goldens regenerated.
Deviations from TDD: none (ADR-0004 anticipated `legacy_inventory`).
Provisional answers added to QUESTIONS.md: Q45.
New tunables: `seeding.legacy_inventory`.
Next session should know: with this seed the householder Freeport passes every GDD 17 target at the GDD's own needs values (`food_meter_per_unit` stays 4); the Q20 trap is real but no longer triggered without humans. Seeding Materials into the Machine Shop pushed the investment-share metric to 0.66 (seeded Materials are consumed but never produced), so it is not seeded. S0.14 is the report and the green `make sim-check`.
make check: green · new tests: 0 (1 extended) · sim-check: red (investment share 0.65 in one epoch on seeds 2-5; this line was first written as green before the sweep finished, corrected in S0.14)

## S0.14 — Freeport tuning pass (config only) — 2026-09-13 — PR #22
Built: `docs/tuning/freeport-01.md` (the Phase 0a exit report); `householder.legacy_machine_buy_payroll_mult` 1.0 -> 1.5 (Q43), which removes the one-epoch-per-seed 0.65 investment share by staggering legacy machine buys; goldens regenerated for the new constant. `needs.food_meter_per_unit` stays at the GDD value of 4 (report 00 proposed 6; not needed once day one has stock).
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q46 (structural unemployment of 15-22 with 40 householders); Q20 and Q43 updated.
New tunables: none.
Next session should know: **Phase 0a exit gate met**: `make sim-check PRESET=freeport` is green on seeds 1-5 (need 99.4-100%, index 1.02-1.14, stock-out 2, investment share 0.27-0.29, 0 rejections; about 18 s wall clock). Q20 (sticky hardship) is still open and will bite the first human who runs out of money; decide it before Phase 1. Phase 0b (S0.15+, the other four presets' rule slices) can start from `main`.
make check: green · new tests: 0 · sim-check: green on seeds 1-5

## S1.0 — Q20 decision and Phase 1 dev infrastructure — 2026-09-13 — PR #23
Built: `needs.food_meter_per_unit` 4 -> 6 (Q20 decided by Chris: one Food per tick recovers +2 at normal effort, so a human at 0 climbs out of hardship within a cycle); the three goldens regenerated; the S0.5 table test pins the GDD value of 4 in its fixture so it still checks the mechanic it was written for. `deploy/docker-compose.dev.yml` (postgres:17 on host port 5433, `ISMS_DB_PORT` override), `.env.example`, `make db` / `make db-stop` / `make dev` (server and Vite pieces arrive with S1.2 and S1.7), CI `check` job gains a postgres:17 service with `DATABASE_URL` and `SQLX_OFFLINE=true`, and `scripts/pr-cycle.sh` now rebases on `origin/main` and re-runs `make check` before pushing (the two-track merge rule).
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: none new; Q20 moved to decided.
New tunables: none.
Next session should know: this is the first card of the Phase 1 track, run from the worktree `../Isms-p1` in parallel with Phase 0b in `../Isms-0b`. Engine-touching PRs from either track carry `[engine]` in the title. Port 5432 on this box belongs to another project's container, hence 5433. sim-check numbers are byte-identical to S0.14 (householders never sat below 20, so the +2 never fires for them).
make check: green · new tests: 0 (1 fixture pinned) · sim-check: green on seeds 1-5

## S0.14d — Preset-aware stability targets, five stability tests, sim-all (Phase 0b infrastructure) — 2026-09-13 — PR #25
Built: `isms_sim::StabilityTargets` (`for_capabilities`: need >= 0.95, price band 0.7..1.3 only when the society has order books, Food stock-out at most 3 cycles, investment share 0.05..0.6, zero rejected commands) and `stability_failures(&targets, &summary)`; `Row` gains `food_available` (resting Food asks, the Common Store's Food, or the state stock's Food by capability: the stock-out reading for every system), `store_food`, `state_food`, `treasury_credits`, `till_credits`; `EpochSummary` price index bounds are `Option` (the table prints `-` where no index exists); `RunResult.capabilities`; `RunSpec::new`; `sweep` (one preset over a seed range, shared by the CLI and the tests); CLI subcommand `all` (five presets in turn, `--check`); `make sim-all`; five `#[ignore] stability_<preset>` tests so `make sim-check PRESET=x` is real for every preset; `targets_derive_from_capabilities` and `all_five_presets_complete_one_epoch_and_conserve` (seed 1, one epoch each, conservation at the end) so no preset's card can silently break another; `docs/tuning/TEMPLATE.md`; README updated.
Deviations from TDD: none. The GDD 17 targets stay in the simulator (they are a tuning gate, not a rule of any society); the investment band is still the simulator's own.
Provisional answers added to QUESTIONS.md: none (Q58 in the Phase 0b plan is implemented here rather than in S0.15c).
New tunables: none.
Next session should know: baseline of `isms-sim all --epochs 5 --seeds 1..1` before any 0b engine work: Freeport stable; Republic and Commonwealth read as stable only because they are still Freeport-shaped (no tax, no coops, no levy); the Commune completes 42 cycles with 0 rejections, 0 need-fulfillment and no Food anywhere (nothing puts output into the store); the Directorate rejects 40320 householder commands per epoch (one per citizen per tick: the standing plan bids into a society without order books) and produces nothing. All four gaps are the S0.15-S0.17 cards. No engine type changed.
make check: green · new tests: 2 (+4 ignored stability tests) · sim-check: freeport green on seed 1 (the seed 1..5 sweep is unchanged from S1.0)

## S0.15a — Common Store: society stock, draws by need, need-first, collective dwellings — 2026-09-13 — PR #26
Built: `ledger::stock_holder` (a Society-owned org's goods live in the Common Store or the state stock, Q48), `payer_of` (the till for state enterprises, used by `apply(Paid)`), `goods_at`; production output, recipe inputs, machine installs, workplace and dwelling Materials all move through the society stock, and a dwelling built by a Society-owned org is `Owner::Society`; `store` module: `entitlement` (meter shortfall in units, less pantry and pending requests, within the pantry cap), `RequestStoreDraw { good, qty }` (gated on `common_store`; `NoStore`, `OverEntitlement`), `execute_store_plan` (phase 2 files the Food and Wares draws through `handle` with `ClientKind::Plan`), `phase_6_store` (everyone served when the stock suffices, else need-first: largest request first, ties by the tick RNG; one `Drew` with an Explain per served request; `equal_shortfall` and `lottery` fall back to need-first until S0.15b); `CommonStore.requests` (cleared by `TickResolved`); events `StoreDrawRequested` and `StoreReturned` (appended, Q50); `Drew` applied (leaves `UNIMPLEMENTED`); `housing::free_society_dwelling`, `society_dwelling_of`, `cycle_end_8b_assign_dwellings` (step 8b, at join, and again after the 8l fill, Q51); seeding puts `legacy_inventory` into the society stock and hands each new householder a society dwelling; emigration and dormancy release a society dwelling, emigration unassigns contract-less positions and returns the pantry and balance to the society stock (Q47); a `RequestStoreDraw` command step in the proptest strategies.
Deviations from TDD: none. One engine fix outside the card: phase 10 now emits a `CitizenDelta` for a citizen who ate this tick and then went dormant (emigration at 8l), otherwise the live pantry kept the eaten unit; conservation held either way, the unit was merely stranded in a dormant pantry. Freeport is byte-identical (goldens untouched; 594015 events on seed 1 as in S0.14).
Provisional answers added to QUESTIONS.md: Q47, Q48, Q49, Q50, Q51.
New tunables: none.
Next session should know: a seeded Commune now runs 42 cycles with 0 rejections and draws until the 960 seeded Food is gone (about one cycle), then everyone starves because no householder works and no collective manager acts: that is S0.15c (`JoinWorkplace`, the Commune `decide`, managers installing Machines from the store). S0.15b adds the other two rationing rules, `SetPolicy`, the Materials split cap, `Pledge` and the Ledger of Contribution. `run_plan_command` is `pub(crate)` for the store and state-store executors.
make check: green · new tests: 7 · sim-check: freeport green on seed 1 (unchanged); commune red as expected (need 4.8 %, stock-out 41 cycles)

## S1.1 — Persistence: event log, snapshots, replay — 2026-09-13 — PR #26
Built: `isms-store` on sqlx 0.8 (postgres, tokio): migration `0001` with `societies`, append-only `events` (`PRIMARY KEY (society_id, seq)`, JSONB payload plus JSONB `actor`/`client_kind`, `kind` text, tick/cycle/epoch columns, UPDATE/DELETE/TRUNCATE triggers) and `snapshots` (zstd postcard bytes, blake3 hash, `last_seq`); the `isms_app` NOLOGIN role with INSERT/SELECT on events; `EventStore` trait (`create_society`, `list_societies`, `append_batch` as one `INSERT ... SELECT FROM UNNEST` transaction with `seq` assigned by the caller, `read_from(after, limit)`, `latest_snapshot`, `write_snapshot`) and `PgEventStore`; `EventMeta::at(world, actor, client_kind)`; `load_world` (verified snapshot + tail, or fold from `SocietyCreated`), `replay_hash`, `verify_latest_snapshot`; `MIGRATOR`. `isms-server migrate` (real), `rebuild --society` (verify latest snapshot against a full replay, then write a fresh one), `serve` (S1.2 stub). `.sqlx/` offline metadata committed; `make sqlx-prepare`; `make test` sources `.env`. `serde_json` gains `float_roundtrip` workspace-wide (Q47).
Deviations from TDD: `snapshots` carries `last_seq` (the TDD table has no way to say where the tail starts). `actor`/`client_kind` are JSONB (`null` for tick-produced events) rather than text so the serde shapes round-trip unchanged.
Provisional answers added to QUESTIONS.md: Q52 (JSONB floats replay byte-for-byte only with `float_roundtrip`; the 3-cycle replay diverged by one ulp without it).
New tunables: none.
Next session should know: sqlx::test builds a database per test from `DATABASE_URL` (superuser needed; the dev container and the CI service both are) and applies the migrations itself. After changing any `query!`, run `make sqlx-prepare` with the dev database up, or CI (which sets `SQLX_OFFLINE=true`) fails. `EventMeta::at` is what the actor stamps before persisting; the S1.2 actor owns `seq` (start at `Loaded.last_seq + 1`). `read_from` takes an exclusive `after` (pass -1 for the whole log).
make check: green · new tests: 5 · sim-check: n/a

## S0.15b — Rationing rules, SetPolicy, Materials split, pledges, Ledger of Contribution — 2026-09-13 — PR #28
Built: `store::equal_shortfall` (proportional floors, leftover one each in RNG order) and `store::lottery` (whole requests in RNG order), selected by `world.policy.rationing` at phase 6 (Q52); `store::cycle_end_8b_surplus_shares` (equal shares of Food and Wares at cycle end, capped by pantry room, `RuleId::StoreSurplusShare`, Q53); `Policy::validate_against` (every field checked against the constitution's axes; grades ascending; rates in range) used by the config loader and by `SetPolicy`, which is now real for `Actor::System` and applied (`PolicyChanged` leaves `UNIMPLEMENTED`, which is now empty; Q55); the Materials split as a per-tick consumption cap per sink kind on society stock (`labor::phase_4_production`, Q58); `norms` module: `Pledge` (with `to: Option<OrgId>`, Q56) creating a `ContractBody::Pledge` via `Pledged`, `cycle_end_8i_norms_ledger` emitting `NormsLedgerClosed` (hours exact, output as attributed, norm met) folded into `Citizen.contribution: ContributionRecord`, and `PledgeClosed` when a pledge's term runs (Q54); the per-cycle workplace accumulator reset moved from 8g to 8m (`labor::reset_cycle_accumulators`, Q57).
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q53, Q54, Q55, Q56, Q57, Q58, Q59 (Q52 was taken by S1.1 in the meantime).
New tunables: none (the surplus goods are the two need goods by definition; `_base.toml [params.governance]` still carries the unused `plan_bonus_fraction`, `ratchet`, `work_norm_hours` duplicates, to be dropped with the S0.16b param change so the goldens regenerate once).
Next session should know: goldens untouched (no Params, Policy or CycleAggregates shape change; new Event variants are appended). `Citizen` gained `contribution`; the Phase 1 track's snapshots will see it. A builder fixture has population floor 0, so its householders emigrate at the first cycle end: fixtures that run a whole cycle use `.humans(n)` or `seed_epoch()`. S0.15c: `JoinWorkplace`/`LeaveWorkplace`, `orgs::least_staffed` with `balance_weights`, the Commune `decide` (follow the norm at the least-staffed workplace) and the collective manager installing Machines from the store, then the tuning report and `make sim-check PRESET=commune`.
make check: green · new tests: 9 · sim-check: freeport unchanged (594015 events, seed 1); commune still red as expected until S0.15c

## S0.15c — Commune householder, balancing rule, collective managers, stock-out by system, first stable Commune — 2026-09-13 — PR #29
Built: `JoinWorkplace`/`LeaveWorkplace` (norm systems; contract-less positions within `max_workers_per_workplace` and `max_workplaces`; `norms` module), `orgs::least_staffed` (fewest workers per unit of `params.labor.balance_weights`, ties to the lowest id; Q62), the householder script's norm branch (plan `FollowNorm`, join the least-staffed workplace, work the published norm at normal effort; Q60), the collective manager's script (install one Machine from the society's stock per workplace per round, never at a Machine Shop; Q61), `labor::has_position` and `unemployed` redefined as "no position" (Q63), the simulator's per-system stock-out reading (`food_unfilled`, `stocked_out` columns; market = no asks resting, store = unserved requests; Q64), `SCRIPT.md` "In the Commune", `docs/tuning/commune-01.md`, golden `s0_15_commune_3_cycles`.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q60, Q61, Q62, Q63, Q64.
New tunables: `labor.balance_weights` (S0.15c); the three goldens regenerated for it.
Next session should know: **S0.15 done gate complete**: `make sim-check PRESET=commune` green on seeds 1-5 at the GDD's starting values (need 100 %, stock-out 0, investment share 0.067, 0 rejections, about 122 000 events per seed) and the five seeds are event-for-event identical (no tie ever needs the RNG); Freeport is unchanged (594015 events on seed 1). Read `commune-01.md` for why the Gini is 0 and the investment share sits near the floor. S0.16a starts the Directorate: state store queue, ration cards, provision, assignment at join via the same `least_staffed` rule, and the till as the state's purse (`ledger::payer_of` is already wired into `apply(Paid)`).
make check: green · new tests: 5 · sim-check: commune green on seeds 1-5; freeport green on seeds 1-5 (unchanged)

## S1.2 — Society actor and scheduler — 2026-09-13 — PR #30
Built: `isms-server` as a library plus binary. `actor` (TDD 9.1): one tokio task per society owning `Arc<RwLock<World>>`; mailbox `ActorMsg::{Command, Tick, Snapshot, Shutdown}`; `handle` under the read lock, persist (`append_batch`, actor-assigned `seq`), `apply` under the write lock, `broadcast` of `Arc<Batch>`; rejections counted in metrics, not persisted; `Rules` derived once per epoch; a `SeqCollision` stops the actor. The householder round runs on a scratch clone and its events are persisted in the same transaction as the tick's (one commit per tick); a snapshot is written at every cycle end and on shutdown. `scheduler` (TDD 9.2): `Schedule { tick_seconds, tick_origin }`, tick n due at `origin + n x tick_seconds`, missed ticks run back-to-back in order, `tick_seconds = 0` ticks continuously, 60 s maximum sleep, `CancellationToken` shutdown, idle once the epoch has ended (rollover is S1.15). `runtime`: `seed_society` (row + `SocietyCreated` + `start_epoch(0)` + first snapshot), `start_society` (optional full-replay verification, TDD 9.1), `Runtime::start` for every active society, graceful `shutdown`. Binary: `seed --preset --name --tick-seconds --param`, `serve [--verify-replay]` (Ctrl-C = final snapshots), JSON logs with `ISMS_LOG_JSON=1`; `metrics` facade counters and histograms (`isms_tick_seconds`, `isms_command_seconds`, `isms_commands_rejected_total{code}`, `isms_actor_mailbox_depth`). Migration `0002` adds `tick_seconds`, `tick_origin`, `cycle_boundary_hour` to `societies`; `EventStore::next_society_id`. `make check` now defaults `DATABASE_URL` to the `make db` container; the compose project is named `isms` so every worktree shares it.
Deviations from TDD: householder events are stored with `actor = null` and `client_kind = "householder"` (`run_round` does not report the citizen per event); the API's `Seen` throttling and `client_kind` stamping arrive with S1.3.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: **every worktree must run `make db` once before `make check`** (the store and server tests need Postgres; CI has a service container). Tests reuse `isms_store::MIGRATOR` (`#[sqlx::test(migrator = "isms_store::MIGRATOR")]`). The server tests set `synchronous_commit = off` server-wide on the test Postgres. An epoch at `tick_seconds = 0` is 3 s of engine time but ~45 s on Docker Desktop for Windows (a flat ~45 ms per INSERT on its network path, independent of payload; commits are sub-millisecond); the Linux CI number is in the PR. Per-tick persistence is well inside the TDD 17 budget (500 ms). `SocietyHandle::command` returns `Ok(Err(reject))` for engine rejections; `Ok(Ok(events))` otherwise. `Runtime::handle(id)` is what the S1.3 API will hold.
make check: green · new tests: 6 (5 integration + 1 unit) · sim-check: n/a
## S0.16a — State store: queue, ration cards, provision, assignment at join, the till — 2026-09-13 — PR #30
Built: `state_store` module: `list_price`, `RequestStateStore { good, qty }` (administered only; `NotOnPriceList`, pantry room counting pending requests), `execute_administered_plan` (phase 2: the plan's Food target and Wares rule as purchase requests at the list price within the saving floor; price limits ignored, Q69), `phase_6_state_store` (arrival order; each request served within stock, balance / price and the ration card; `StateStoreSold` with `RuleId::StateStorePrice`, money to the till, goods to the pantry; `StateStoreShortage` for the rest), `ration_remaining` (`policy.ration_caps`: units per citizen per cycle, `StateStock.issued_this_cycle` cleared at `CycleClosed`, Q66), `cycle_end_8b_provision_ration` (provision systems: pantry topped up to `minimum_food_ration` at zero price, `RationIssued` with `RuleId::ProvisionRation`, Q68); `StateStock { stock, till, requests, issued_this_cycle }` (the old `ration_caps` field moved to policy); `WorldCycle.{store_unfilled, rations_issued}`; assignment at join, at the 8l fill and at seeding by the balancing rule (`seeding::balance_seeded` over workplaces that exist only as events; Q62); the legacy budget seeded into the till (Q67); the simulator counts state-store requests and sales in `food_unfilled`.
Deviations from TDD: none (TDD 5.2's "zero-rent state leases" are assignments, Q65).
Provisional answers added to QUESTIONS.md: Q65, Q66, Q67, Q68, Q69.
New tunables: none (`Policy.ration_caps` is a policy field, None by default; the four goldens regenerated for the Policy shape).
Next session should know: a seeded Directorate already runs 2 epochs with 0 rejections and 100 % need-fulfillment **without wages**: the 24-unit free ration is a whole day's Food and the endowment buys the Wares, so the till only ever gains. S0.16b adds wage-scale pay from the till, the plan bonus and ratchet, `SetPlan`, transfer requests and the plan-fulfillment metrics (regenerating goldens once more for the `[params.governance]` cleanup and the `CycleAggregates` fields, including the Commune's `contribution_gini`). S0.16c's tuning question is whether `minimum_food_ration` = 24 is what the Committee means by a floor. `payer_of` already routes `Paid` to the till for state enterprises.
make check: green · new tests: 6 · sim-check: freeport unchanged (594015 events, seed 1); directorate reads green on seed 1 but is not the gate until S0.16c

## S0.16b — Wage grades, plan bonus, ratchet, SetPlan, transfer requests, plan metrics — 2026-09-13 — PR #32
Built: `planning` module: `grade_of` (skill band of the workplace's job family, `params.labor.skill_band` = 20, capped at the top grade; Q73), `cycle_end_8a_scale_payroll` (every position paid from the till at its grade for the cycle's tick-hours with `RuleId::PayScale`, plus `RuleId::PlanBonus` = floor(pay x `plan_bonus_fraction`) where the workplace's output reached its target; a short till pays pro rata with no `PaymentMissed`, Q72; then the ratchet: `TargetSet { max(target, output x ratchet_mult) }` for every overfulfilled workplace, `params.governance.ratchet_mult` = 1.0, Q70), `SetPlan { targets, materials_split, price_list, wage_grades, ration_caps }` (System; `PlanPublished` plus one validated `PolicyChanged`, Q71), `RequestTransfer`/`DecideTransfer` (`World.transfer_requests`; approval unassigns and reassigns), `Workplace.{last_cycle_output, last_fulfillment}` set at 8m and carried by `WorkplaceDelta` (Q74); `CycleAggregates` gains `plan_fulfillment`, `store_unfilled`, `rations_issued`, `state_stock`, `till`, `contribution_gini` (the Commune's scoreboard metric, Gini of last cycle's ledger hours); the simulator's rows carry the new columns; `[params.governance]` drops the policy duplicates (`plan_bonus_fraction`, `ratchet`, `work_norm_hours`).
Deviations from TDD: none (`SetPlan` carries `ration_caps` in addition to the TDD's four parts).
Provisional answers added to QUESTIONS.md: Q70, Q71, Q72, Q73, Q74.
New tunables: `governance.ratchet_mult` (1.0), `labor.skill_band` (20); three governance keys removed; the four goldens regenerated (Params, `WorkplaceDelta`, `CycleAggregates`).
Next session should know: with wages the Directorate's till (18 x 384 = 7200 seeded) drains to zero by cycle 38 of epoch 0: forty positions at grade 0 cost about 1900 a cycle while the store takes in only Wares money (Food is free under the 24-unit ration), after which wages go pro rata; need-fulfillment stays at 100 % on the ration throughout, 0 rejections. That balance (price list, grades, `minimum_food_ration`, the seeded budget) is S0.16c's tuning pass, together with the scripted System planner (targets = last cycle's output x 1.05 via `SetPlan`, `DecideTransfer` approvals), the Directorate `decide` branch and `SCRIPT.md`. No workplace has a target until a plan is published, so `plan_fulfillment` is `None` in the baseline. CI on this track is red only on the Phase 1 store test `app_role_cannot_update_or_delete_events` (main's own S1.1 run failed the same way); the engine checks pass.
make check: green · new tests: 5 · sim-check: freeport unchanged (594015 events, seed 1); directorate reads green on seed 1 without a planner (not the gate until S0.16c)

## S0.16c — Directorate householder, the System planner, policy sweeps, first stable Directorate — 2026-09-13 — PR #34
Built: `planner` module (`decide_system`: on the first tick of each cycle `SetPlan` with targets = last cycle's output x `sim_planner_target_growth` for every producing workplace, and `DecideTransfer { approve: true }` for every pending request; `run_system_round` executes it through `handle`; Q76); the simulator runs the System's round before the householders' and counts its rejections; the householder's plan prices its living cost from the list where prices are administered (`price_of` falls back to `state_store::list_price`); the ration is a fixed issue of `minimum_food_ration` per cycle within pantry room (Q75, superseding Q68) and `directorate.toml` sets it to 8; `load_preset_with_overrides` accepts `policy.<key>` overrides so `--param` can sweep a society's own levers (Q77); `SCRIPT.md` gains "In the Directorate" and "The System's planner"; `docs/tuning/directorate-01.md`; golden `s0_16_directorate_3_cycles`; sim tests `five_directorate_epochs_run_and_roll_over` and `policy_overrides_reach_the_engine`.
Deviations from TDD: none. The Directorate `decide` branch needed no new code: `AcceptAssignment` plus the existing "work full hours at my position" rule is the whole script.
Provisional answers added to QUESTIONS.md: Q75, Q76, Q77.
New tunables: none; policy values moved: `price_list_credits` food 1.20 -> 2.50 and wares 3.00 -> 5.00 (the list must cover the wage fund: the state's takings are bounded by what citizens spend, so at the old list the till was empty from cycle 20 and every position was paid pro rata), `minimum_food_ration` 24 -> 8. Goldens: the Directorate golden regenerated for the preset change; the other four untouched.
Next session should know: **S0.16 done gate complete**: `make sim-check PRESET=directorate` green on seeds 1-5 (need 100 %, stock-out 0, investment share 0.067, plan fulfilment 0.94-1.00, till positive all epoch, 0 rejections, about 403 000 events per seed, seeds identical event for event). Freeport unchanged (594015 events on seed 1); the Commune unchanged. S0.17a starts the Republic (tax, treasury, need floor, minimum wage, public dwellings): `Holder::Treasury` exists and is a holder in `conservation_check`; step 8b is already a dispatch (`tick::cycle_end_8b_tax_and_provision`: dwellings, surplus shares, provision ration), so add the tax step there behind `redistribution == TaxTransfer`. The minimum-ration rule from Q75 is the one the Commonwealth's basic provision should reuse from its bank pool (S0.17c).
make check: green · new tests: 6 · sim-check: directorate green on seeds 1-5; freeport green (unchanged); commune green (unchanged)
## S1.3 — API skeleton, auth, OpenAPI — 2026-09-13 — PR #36
Built: `isms-api-types` (utoipa `ToSchema` wire types: `Clock` (1-based epoch/cycle/tick plus `engine_tick`), `Problem` (RFC 9457 with `code: RejectCode`), account/society/join shapes, `CapabilitiesView` mirroring the engine's `Capabilities`). Migration `0003`: `accounts`, `invite_codes`, `magic_links`, `sessions`, `api_keys`, `citizens` (unique `(society_id, account_id)`), grants for `isms_app`; `isms_store::accounts` query module. `isms-server`: `mail` (`MailSender` trait, `StdoutSender`, `MemorySender`; SMTP is S1.14), `auth` (random 256-bit tokens hashed with SHA-256 (Q78), `Auth` extractor for the `isms_session` cookie or `Authorization: Bearer isms_<prefix>.<secret>`, CSRF header `X-Requested-With: isms` on state-changing browser requests, `ClientIp`), `limiter` (token buckets), `error` (`ApiError` -> `application/problem+json`), `state` (`AppState`, `SocietyEntry`, `clock_of`), `api` (handlers, `OpenApiRouter`, `/openapi.json`, `/docs`, `/healthz`). Endpoints: `POST /auth/magic-link` (invite code required for a new email; per-IP bucket 1 per 6 s burst 10), `GET /auth/callback` (session cookie, 303), `POST /auth/logout`, `GET /me`, `POST /me/api-keys`, `DELETE /me/api-keys/{id}`, `GET /societies`, `GET /societies/{id}`, `/capabilities`, `/lexicon`, `/welcome` (placeholders filled from the live world), `POST /societies/{id}/join` (System-issued `Join` stamped with the caller's client kind; idempotent per account). Any authenticated society read issues a `Seen` at most once per tick per citizen (`AppState::first_sight_this_tick`, on top of the engine's own throttle). Binary: `serve --bind --base-url` (axum with graceful shutdown), `invite --count`, `openapi`; `make openapi-lint` (Redocly) runs in CI.
Deviations from TDD: SHA-256 rather than argon2 for random tokens (Q78). `/join` rate limit is a fixed 1 per 10 s burst 3 per account rather than the society's `rate_limit` (which is for in-society commands, S1.4).
Provisional answers added to QUESTIONS.md: Q78.
New tunables: none.
Next session should know: `Auth` requires `AppState` as the router state (`FromRequestParts<AppState>`); every society-scoped handler calls `touch_presence` then reads `entry.handle.world`. Engine rejections surface as 422 problems with `code`. The web session cookie is `Secure` only when `base_url` is https. `axum-test` with `save_cookies()` drives the tests; a fixture spawns the actor without a scheduler so nothing ticks unless the test says so (and the engine counts the join tick as presence, so tick before asserting a `Seen`). S1.4 adds the `Viewer` and the in-society endpoints on the same router; S1.7's `make api-types` can read `target/openapi.json` from `make openapi-lint` without a server.
make check: green · new tests: 7 (6 integration + 1 unit) · sim-check: n/a
## S0.14e — Simulator detail output — 2026-09-13 — PR #32
Built: `isms_sim::observer::{Observer, NoObserver}` and `run_with` (`run` is `run_with` with `NoObserver`; hooks after the epoch start, after every tick with both the householder round's and the tick's events, at cycle close, at the end); `isms_sim::detail::{DetailOptions, DetailWriter}` writing `citizens.csv`, `orgs.csv`, `flows.csv`, `trades.csv`, `moves.csv`, `depth.csv` and, with `--detail-ticks`, `citizens_ticks.csv` under `--detail-dir` (default `target/sim/<preset>-<seed>/`); `--detail`, `--detail-ticks`, `--detail-dir` on `run` and `all`; `sweep` takes `Option<&DetailOptions>`; `make sim-detail`; the "Detail output" section and schema table in `docs/tuning/README.md`; card S0.14e in TDD §18.2.
Deviations from TDD: none (the card was written this session; TDD §13's "sim produces the same `CycleClosed.aggregates` as CSV" is untouched, the detail files are additional).
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the writer only reads, so a `--detail` run is byte-identical to a plain one (asserted); hours per cycle are the difference in cumulative `Skill.tick_hours` between cycle closes (the per-cycle accumulators are zeroed inside the closing tick, so they read 0 at the hook); `Trade` events occur in both the round slice and the tick slice (`phase` column); headers are derived from `Default` rows and written at open, so a moneyless preset's tape and depth are header-only rather than empty. A five-epoch Freeport seed writes about 20 MB (243 000 trades) in about 0.2 s over the plain run. Follow-ups not in this card: a `sweep --grid` driver for heat maps, a run manifest (git sha, overrides, final hash) with a raw-vs-tuned archive convention, and redrawing the Phase 0a ledger from these files.
make check: green · new tests: 7 · sim-check: freeport unchanged (594015 events, seed 1)

## S0.17a — Tax and transfer: income tax, treasury, need floor, minimum wage, public dwellings — 2026-09-13 — PR #36
Built: `tax` module: `assess` (marginal brackets over `tax_rate`, ascending, floored per slice, empty = flat; `RuleId::TaxIncome`; Q84), `wage_floor` (the minimum wage, raised by collective agreements in S0.17d), `floor_money` and `need_floor(b, source, floor_food)` (a money top-up to the floor's Food at the last Food price, largest shortfall first, until the source runs dry; `NeedFloorPaid` with `RuleId::NeedFloorTransfer`; shared with the Commonwealth's bank in S0.17c; Q81), `cycle_end_8b_tax` (every earner assessed, even at zero tax, then the floor from the treasury; Q83); `Citizen.taxable_income` fed by `Paid` and `DividendPaid` and reset by `TaxAssessed`; `RejectCode::BelowMinimumWage` in `employment::offer_employment` (hourly as is, piece rates at the workplace's base rate; 0 or none = no enforcement; Q82); public dwellings seeded as Builder-built and `DwellingTransferred` to the society (no event shape change), housed by the shared 8b rule; `Policy::validate_against` requires strictly ascending brackets; `WorldCycle.{tax_collected, floor_paid}`, `CycleAggregates.{treasury, tax_collected, floor_paid}`, `metrics::wellbeing_index`; sim columns `tax_collected_credits`, `floor_paid_credits`; `SCRIPT.md` "In the Republic"; `republic.toml` `minimum_wage_credits` 0 -> 6.00; golden `s0_17_republic_3_cycles`; `docs/tuning/republic-01.md`.
Deviations from TDD: none. The Republic householder and legacy firm are Freeport's, untouched; tax and the floor act on them from outside.
Provisional answers added to QUESTIONS.md: Q83, Q84, Q81, Q82 (Q78 was taken by S1.3 in the meantime).
New tunables: none (policy value moved: `minimum_wage_credits` 6.00). The six goldens regenerated for the `CycleAggregates` shape.
Next session should know: **`make sim-check PRESET=republic` is green on seeds 1-5** at the legislature's defaults (need 98.9-99.2 %, index 1.02-1.14, investment share 0.27-0.30, 0 rejections; about 597 000 events per seed) and the other four presets are byte-identical to their reports. The Republic's treasury only accumulates (about 450-510 a cycle in tax, 18 600 by epoch end): the need floor of 24 Food at the last price (about 31 credits) never binds for a householder whose endowment is 1000, and there is no public spending yet (`public_dwellings` = 0, the public bank is Phase 3). That is the legislature's own default, recorded in `republic-01.md`; a human who runs out of money is the first citizen the floor will reach. `Holder::Treasury` was already in `conservation_check`; step 8b runs tax first, then dwellings, surplus shares and the state ration. S0.17b (cooperatives) starts next: `seeded_org_kind` already yields `Cooperative`/`Members` for the Commonwealth and its legacy managers run the Freeport script, which is why the Commonwealth reads stable today; the coop slice replaces employment with membership and payroll with the share-out.
make check: green · new tests: 9 · sim-check: republic green on seeds 1-5; freeport, commune, directorate unchanged; commonwealth reads green as a Freeport twin until S0.17b
## S1.4 — Freeport command and query endpoints, WebSocket stream — 2026-09-13 — PR #38
Built: `isms_api_types::society` (Situation view, plan, labor, payslips, digest, books, orders, prices, orgs and manager actions, notice board, offers, contracts, transfers, dwellings, stats, citizens, scoreboard, householders, explain, `Committed`, `EventRef`, `StreamFrame`; money in cents; engine-shaped payloads travel as JSON objects). `isms-server`: `viewer` (the one place visibility lives: `Produced` per-worker figures for the worker and, attributed only, for the manager; `TickResolved` reduced to own deltas and managed workplaces; a private-kind list for payslips, transfers, credit and rent events readable by their parties and the org manager; `explains_in`), `views` (`World` -> wire, pure), `society_api` (39 handlers under `/s/{id}/...`, each: resolve citizen (403 otherwise), presence, per-citizen token bucket from the society's published `rate_limit`, `Viewer`-filtered `Committed` responses with log positions), `stream` (`GET /s/{id}/stream` WebSocket: a hello frame, then every committed batch filtered per viewer, `lagged` when behind). `isms_store::reads` (`read_one`, `read_since_tick`, `read_last_of_kind`, `read_kind_since_tick`). Actor: `SocietyHandle::command` returns `CommandOk { first_seq, tick, cycle, events }`; `Batch` carries tick and cycle. Fixture `presets/test/freeport-medium-monitoring.toml`. `[engine]` `ShareHolder` now serializes as a string (`citizen:7`, `org_self`): it keys the share registry inside `OrgFounded`, and JSON maps must have string keys, so a human-founded firm could not be persisted until now (Q83); goldens regenerated.
Deviations from TDD: manager actions are one route per action under `/s/{id}/orgs/{oid}/...` rather than a single `{manager-actions}` path; `POST /offers/{kind}` covers sale, wanted, credit, lease (employment offers are org actions); `DELETE /offers/{oid}` withdraws sale offers and wanted ads (the engine has no cancel for credit and lease offers); `/dwellings/{did}/move-in|move-out` added for the Q34 commands. Headlines on `/home` are empty until S1.5.
Provisional answers added to QUESTIONS.md: Q83 (string map keys in event payloads), Q84 (which event kinds are private).
New tunables: none.
Next session should know: `Viewer::view_event` is the only filter; S1.5 should feed the Chronicle projection from the same broadcast batches (`Batch { first_seq, tick, cycle, events }`) and can reuse `read_last_of_kind`. `EventRef.seq` gives the UI a stable id for `/explain/{seq}`. The integration fixture `crates/isms-server/tests/society_api.rs` (`fixture`, `client`, `grant`) is the model for S1.5 and S1.6 tests; legacy offers exist from tick 1, and a human who founds a firm needs 20 Materials (the fixture gathers them from legacy orgs). The tests run one society actor per test without a scheduler.
make check: green · new tests: 8 (+1 fixture preset) · sim-check: n/a

## S1.5 — Chronicle, notice board, comms — 2026-09-13 — PR #39
Built: `presets/copy/freeport/chronicle.toml` (Freeport's voice: per-event templates with `{{field}}`, `{{citizen:field}}`, `{{org:field}}`, `{{credits:field}}` placeholders; cycle-end templates for price moves over `thresholds.price_move_pct`, hardship, unemployment). `isms_server::chronicle`: a pure `Projector` that learns names from joins and foundings and the previous index from the previous `CycleClosed`, so live projection and `rebuild` run the same code over the same input (`observe(event, seq, tick, cycle) -> Vec<NewHeadline>`); `replay` (idempotent catch-up from the log), `rebuild` (clear + replay), `spawn_projector` (subscribe to the actor's broadcast, catch up, follow; replays on `Lagged`). Migration `0004`: `chronicle` (keyed by source event seq and ordinal, regenerable) and `messages` (D12). `isms_store::projections` queries. `isms_server::comms`: `GET /s/{id}/chronicle?cycle=` (one edition), `GET|POST /s/{id}/channels/{channel}/messages` (`square`, `org:<id>` with membership = manager, member, or active employee), `GET|POST /s/{id}/dm/{citizen}`; bodies trimmed and capped at 2000 bytes; a per-citizen message bucket. `/home` now carries the five latest headlines. `Runtime::start_society` spawns the projector when `StartOptions.presets_dir` is set (`serve` sets it); `isms-server rebuild-projections --society N`.
Deviations from TDD: no `notice_board` table (Q85); the daily edition is the set of headlines of a cycle rather than a separately assembled row. The Welcome Brief copy already existed (S0.1).
Provisional answers added to QUESTIONS.md: Q85.
New tunables: none (thresholds live in the copy file, which is voice, not economy).
Next session should know: the three-headline scripted cycle of the card is a unit test in `chronicle::tests` (founding, a 12% index move, a hardship); the integration test checks live headlines, `/home`, one edition per cycle, and that `rebuild` reproduces identical rows. Templates skip silently when a field is null (e.g. `OrgFounded` with no founder). The projector is per society and holds only names and the last index in memory; S1.13's spectator routes can read `chronicle` rows without a citizen. `comms::can_access` is the membership rule for org channels; S1.6's CLI `watch` can combine `/stream` with `/chronicle`.
make check: green · new tests: 3 (1 unit + 2 integration) · sim-check: n/a

## S1.6 — CLI client and the core-loop e2e — 2026-09-13 — PR #40
Built: `isms-cli` (binary `isms`): a hand-written `reqwest` client over `isms-api-types`; `login --session|--key`, `me`, `societies`, `join`, `use`, `home`, `board`, `accept`, `labor set`, `plan set`, `payslips`, `books`, `order bid|ask <instrument> <qty> @ <credits>`, `org list|found|offer`, `contracts`, `chronicle`, `say`, `read`, `key create`, `watch` (tails `/stream` over WebSocket); `--json` prints responses raw; amounts are typed in credits and sent as cents; config in `~/.config/isms/config.toml` (`ISMS_CONFIG`, `ISMS_URL`, `ISMS_KEY`, `ISMS_SESSION`, `ISMS_SOCIETY` override it); errors print the problem title, detail and `RejectCode`. `isms-server session --email X` mints a browser session token for dev and e2e (no invite, no mail), so a headless client can sign in, join, and create its own API key. `scripts/e2e/core-loop.sh` (`make e2e`): seeds a Freeport at `tick_seconds=1`, serves it, and runs GDD 9.1 with the CLI as an agent (API key): join, take a legacy job, set labor, get a payslip at cycle end, buy Food on the book, buy 20 Materials, found a mine, post a job offer, a second account joins and takes it (householders are all employed in the first cycles, Q46, so the hire is a person), the Chronicle reports the founding, a word in the Square. The Chronicle now narrates humans only: the forty seeded householders no longer produce forty arrival headlines and eighteen manager appointments at tick 0. CI runs it after `make check`. `make dev` now runs the server.
Deviations from TDD: `isms plan set` edits `keep_food_at_least` and `keep_balance_at_least` (the TDD's `--effort` belongs to `labor set`); `isms org found` takes the workplace kind as `--workplace`.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the e2e is the first thing that runs a whole loop against a live server; it takes about three minutes at one tick per second, most of it waiting for a cycle end and for foundries to make 20 Materials. `isms-server session` is the bootstrap for any scripted client; Playwright (S1.7) can use the same token by setting the `isms_session` cookie. The CLI has no local state beyond the config file; every command is one request.
make check: green · new tests: 1 unit + the e2e in CI · sim-check: n/a

## S1.7 — Web scaffold, generated client, lexicon, component kit — 2026-09-13 — PR #42
Built: `web/` on Vite 8, React 19, TanStack Router (code-based routes) and Query, Tailwind 4 (`@tailwindcss/vite`), uPlot. `make api-types` dumps `/openapi.json` from the binary and runs `openapi-typescript` into `src/api/schema.d.ts` (committed, so `pnpm check` needs no server); `src/api/client.ts` is the one `openapi-fetch` client with the CSRF header middleware and `credits()`; `src/api/hooks.ts` has `useMe`, `useSocieties`, `useSociety`, `useCapabilities`, `useLexicon` (`t(key)`), `useWelcome`, `useHome`, `useBoard`, and `useStream` (a WebSocket per society that invalidates the society's queries on every frame: no polling, no client-side simulation). Components with vitest coverage: `Num` (value plus an Explain footnote: rule, inputs, formula, result), `Meter` (0..100 with the hardship line), `Ledger`, `OrderBook` (hand-rolled SVG depth), `TimeSeries` (uPlot), `DiffSinceLastSeen` (digest events narrated in the society's words), `Countdown`. `src/styles/tokens.css`: paper, ink, one accent, a serif display face, tabular numerals; a deliberately plain shell so the vocabulary does the work (GDD 15). Routes: `/` (society list, or the sign-in prompt when signed out), `/login` (magic link form), `/s/$id` (the society shell: nav from capabilities, labels from the lexicon, a first Situation summary), `/gallery` (every component with fixed data). Playwright: `web/e2e/login.spec.ts` (signed out, the landing offers sign-in; signed in via an `isms-server session` cookie, the landing lists Freeport; the gallery renders every component) driven by `scripts/e2e/web.sh` (`make e2e-web`, in CI with Chromium). The Vite dev server proxies `/auth`, `/me`, `/societies`, `/s` (including the WebSocket) to the API.
Deviations from TDD: routes are code-based rather than file-based (no router plugin, fewer moving parts); the `StreamFrame` and event payload types are declared in `client.ts` because a WebSocket type has no OpenAPI path and `Object` schemas generate as empty records.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: S1.8 builds Onboarding and the full Home on this shell; `SocietyHome` in `src/screens/Society.tsx` is the placeholder to replace, and the join flow belongs at `/s/$id` for a non-citizen (the 403 branch there). `pnpm dev` needs `make dev` (the API on 8080) in another shell. Playwright reuses a running Vite server locally; CI starts its own. The `useStream` hook invalidates by society key; screens that need per-tick animation can pass `onFrame`.
make check: green · new tests: 6 vitest + 3 Playwright · sim-check: n/a
## S0.17b — Cooperatives: membership, share rule, share-out, forfeiture, manager stub, coop scripts — 2026-09-13 — PR #41
Built: `coop` module: membership replaces employment inside a cooperative (`OfferEmployment` by a coop is `NotAuthorized`; `AdmitMember` places an unplaced citizen at the coop's least-crowded workplace as a contract-less position and lapses their other coop requests; Q86), `ShareRule::{Equal, HoursWeighted}` with `SetShareRule` and `params.coop.default_share_rule` = hours_weighted, `surplus` (treasury less the base set at the last share-out, less obligations due; seed money and loan principal raise the base; a paid installment lowers it; Q88), `share_out` (sums exactly to the surplus; remainder cents in id order), `cycle_end_8a_share_out` (every coop declares every cycle, zero included, so the share per member is never stale; `SurplusDeclared`, `ShareOutPaid` with `RuleId::PayShare`, counted as wages and taxable income; Q87), `leave_events` (leaving or emigrating forfeits: `MemberLeft` plus `Unassigned`; Q89), `open_places`, `tenure_cycles`, `surplus_per_member`; `Org.{share_rule, surplus_base, member_since, last_surplus, last_share_out_members}`; a seeded coop's householder manager is its first member at its workplace and `AppointManager` by the System is the election stub (Q90); `CycleAggregates.{coop_surplus_per_member, mean_tenure_cycles}`; a `bank::levy_due` stub the obligations already reserve (S0.17c fills it). Householder script: with no position, ask the coop with an open place whose last share per member was highest (staffing by the balancing weights, counting requests already waiting, breaks ties; one live request at a time); after two cycles, a member whose coop shared less than a living cost moves to a coop with an open place that shared more (the steward never leaves). The coop manager: admissions instead of job offers; a reserve of obligations due plus a cycle of the members' wage-equivalent before input bids; the Machine rule on that wage-equivalent, net of the round's own bids; a glutted coop (Builders: unlet dwellings) buys no inputs (Q91). `SCRIPT.md` "In the Commonwealth".
Deviations from TDD: none (TDD 5.2's `employment (membership)` is read as membership, Q86).
Provisional answers added to QUESTIONS.md: Q86, Q87, Q88, Q89, Q90, Q91 (Q83-Q85 were taken by S1.5 in the meantime).
New tunables: `coop.default_share_rule`; the six goldens regenerated (`CycleAggregates`, `Params`).
Next session should know: the first real coop economy took five script rules to reach the 95 % line, each recorded in Q87/Q91 with the failure it cures: without the reserve a Mill spends every credit on Grain and shares nothing; keyed to the last share-out the Machine rule ratchets to zero; identical agents choosing at once pile into one coop; a coop whose asks lose the price-time race at the same cost-plus price leaves its members with nothing unless they can move; a stale share per member misdirects the movers; and letting a lone steward leave kills its coop (tried, reverted). Result on seeds 1-3: need 93.3-97.4 % (around the line), hardship 4-11, consumption Gini 0.02-0.06, 0 rejections, stock-out 1, but the investment share is erratic (0.38-1.26: coops buy Machines out of the Machine Shop's seeded and accumulated stock faster than the Foundries make Materials) and 0-7 citizens sit between coops at cycle end. `make sim-check PRESET=commonwealth` is S0.17c's gate, where the capital levy and the bank's lending shape that investment. Freeport, the Commune, the Directorate and the Republic are byte-identical to their reports.
make check: green · new tests: 7 · sim-check: commonwealth not yet green (need 93-97 %, investment share out of band in some epochs); the other four unchanged

## S1.8 — Onboarding and Home — 2026-09-13 — PR #44
Built: `web/src/screens/Onboarding.tsx` (GDD 9.4): a non-citizen opening `/s/{id}` gets the join flow on one route in four steps: handle plus the consent text v1 (comms are logged, records are pseudonymous and may be published), the Welcome Brief rendered from the society's copy (`lib/markdown.tsx`, a tiny renderer for our own Markdown: paragraphs, bold, italics, code), the job board (open employment offers with pay, hours and places; "Take this position" accepts and sets labor to the contract's hours at normal effort in one go), and the standing plan's defaults with "Keep these and go home". `web/src/screens/Home.tsx` (GDD 9.1 step 1): meters with the hardship line, balance, pantry, dwelling and rent, labor status and budget, the digest since last seen, the latest payslips with their Explain footnotes, the Chronicle's latest lines, the plan summary with a one-tap "Keep my plan", the next tick and payday countdowns, and a one-line society pulse. `api/society.ts`: `usePlan`, `usePayslips`, `useJoin`, `useAcceptOffer`, `useSetLabor`, `useSetPlan`, each invalidating the society's queries. `Num` prints the engine's `{Float}`, `{Int}` and `{Money}` values (money as credits). `web/e2e/onboarding.spec.ts`: from a fresh session cookie to an accepted employment offer in at most 8 interactions (the test counts 6), the allocation visible on Home, then the first payslip's Explain showing `pay_hourly`, hours, rate and the formula; `scripts/e2e/web.sh` seeds at one tick per second and mints a second session for it.
Deviations from TDD: the join flow lives at `/s/{id}` (the 403 branch of Home) rather than a separate onboarding route, so a link from the society list is the whole entry; org names on the job board are ids until S1.11's org screens (the offer payload carries only the org id).
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: **this is the pause card.** Chris reviews Onboarding and Home against `docs/wireframes.pdf` p.1 on a running dev server before S1.9 (`make db`, `make dev`, `pnpm --dir web dev`, then `isms-server invite` for a code, or `isms-server session --email` for a cookie). Known gaps to look at together: the job board names orgs by id; the payslip line says "org #n"; the digest narrates only the kinds `DiffSinceLastSeen` knows; there is no Work, Market, Org, Contracts, Society or Talk screen yet (the nav shows their labels from the lexicon, unlinked). The Home layout follows the wireframe's three bands (accounts, what changed, society) but not its exact columns.
make check: green · new tests: 1 Playwright · sim-check: n/a
## S0.17c — Capital levy, Public Investment Bank, admission votes, the Commonwealth floor; commonwealth-01 — 2026-09-13 — PR #46
## S0.17c — Capital levy, Public Investment Bank, admission votes, the Commonwealth floor; commonwealth-01 — 2026-09-13 — PR #45
Built: `bank` module: the bank is the society-owned association seeded when capital is `public_bank`, its treasury is the levy pool (Q92); `machine_value` (installed Machines at the last Machines price), `levy_due` (times `policy.capital_levy`, floored; Q93), `LevyPaid` at 8b with `RuleId::CapitalLevy` (lowers the coop's surplus base so the levy is capital, not surplus); the pool then pays the society's Food floor of `minimum_food_ration` through `tax::need_floor` and lends what is left by formula (`RequestBankLoan { org, principal, term_cycles }` by a coop manager, gated by `public_credit`; applications decided in offer order at 8b: declined for an open bank loan or a missed payment, granted when the pool covers the principal, else waiting; loans are ordinary `CreditOffered`/`CreditAccepted` with the bank as lender, so the S0.11 installments flow back to the pool, the first at 8c of the same cycle end; Q94). Admission by vote (Q95): `ProposeAdmission` (a member; the proposer's yes counts), `VoteAdmission` (one per member), more than half admits at the least-crowded workplace, half against fails, proposals open at 8j lapse; `Proposal.{kind, votes}`, `ProposalKind::Admission`. `OfferCredit`/`AcceptCredit` gate on `credit` only; `public_credit` enables only `RequestBankLoan` (Q96). `CycleAggregates.{levy_pool, bank_loans_outstanding, materials_produced, materials_to_machines}`; sim columns `bank_pool_credits`, `bank_loans_credits`, `materials_produced`, `materials_to_machines`. Coop manager: applies for one Machine's price when it cannot afford one. Tuning the Commonwealth to its gate took three householder rules, each with the failure it cures in `docs/tuning/commonwealth-01.md`: hunger before savings (the balance reserve does not apply to the Food bid when the pantry is empty; universal; Q97), a coop steward buys no Machine past `params.coop.max_machines_per_member` per working member (Q98; never binds in five epochs), and the request and mobility rules count only coops that would admit someone (`coop::glutted`, `coop::admitting`; Q99); one rule was tried and dropped (a coop under its reserve bidding with half its treasury). The epoch investment share in `isms-sim` is now the ratio of sums, not a mean of per-cycle ratios (Q100). `SCRIPT.md` updated (Freeport plan, the Commonwealth section).
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q92, Q93, Q94, Q95, Q96, Q97, Q98, Q99, Q100.
New tunables: `bank.rate_per_cycle_bp`, `bank.max_loan_credits`, `bank.max_term_cycles`, `coop.max_machines_per_member`; the six goldens regenerated (`Params`, `Event`, `CycleAggregates`).
Next session should know: Freeport and the Republic moved under Q97 (Freeport seed 1 594 015 -> 594 244 events, the Republic 596 909 -> 597 267) and both re-run green on seeds 1-5 (need 99.8-100 %); the Commune and the Directorate are event-for-event unchanged. Under the ratio-of-sums statistic every epoch of every preset is inside the investment band (Freeport 0.249-0.257, the Republic 0.250-0.262, the Commune and the Directorate 0.067, the Commonwealth 0.141-0.247); the four earlier reports print means of ratios and were not rewritten. The Commonwealth's pool drains in the last quarter of every epoch (the floor outruns a one-per-cent levy on a few hundred Machines) and lone stewards of glutted coops capture the coop's whole surplus: both are the system's own and flagged for Chris in the report. The S0.17b entry above said PR #38; it merged as #41. Main's CI has been red since S1.6 on `isms-store` (`SQLX_OFFLINE=true` with no cached data for a query; `cargo sqlx prepare`), which is the Phase 1 track's; `scripts/pr-merge.sh` merged #41 despite that red check, so its `fail` grep needs a look. S0.17d (unions) is next; its drafts use Q101-Q103.
make check: green (locally, isms-store excepted as above) · new tests: 7 · sim-check: commonwealth green seeds 1-5 (need 96.5-100 %); freeport, republic, commune, directorate green seeds 1-5

## S0.17d — Unions, dues, collective agreements, strikes — 2026-09-13 — PR #46
Built: `union` module: `FormUnion { firm, name }` (an employee of a firm founds its union: `OrgFounded { kind: Union, ownership: Members, manager: founder }` plus `UnionFormed { org, firm }`; one union per firm; `Org.union: Option<UnionState { firm, strike_until }>`), `AdmitMember` for a union admits only the firm's employees (steward only), `OfferCollectiveAgreement { union, wage_floor, hours, term_cycles }` (steward; `OfferBody::CollectiveAgreement`) and `AcceptCollectiveAgreement { offer }` (the firm's manager) make a `ContractBody::CollectiveAgreement` between union and firm that ends at term at 8a; enforcement: `tax::wage_floor` is the greater of the society's minimum wage and the agreement's floor (so new offers below it are `BelowMinimumWage`), a member's hourly pay accrues at `max(rate, floor)`, a member's hours at the firm are capped at the agreement's `hours` (`OverContractHours`; Q102); non-members are untouched. `CallStrike { union, cycles }` (steward) sets `strike_until`; phase 4 skips striking members (no hours, no wages, no output); at 8a after payroll every member who can pays `params.union.dues_credits_per_cycle` (`DuesPaid`), strikers receive `StrikePaid` = `min(strike_pay_credits_per_cycle, union treasury / members)` with `RuleId::StrikePay` (counted as wages and taxable income; the pool is split from its pre-payment size so the union never pays more than it holds; Q101), and `StrikeEnded` at `until_cycle`. Capabilities: `FormUnion`/`CallStrike` need the `union` org kind, the agreement commands the `collective_agreement` contract. No householder forms a union (Q103); `SCRIPT.md` says so.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q101, Q102, Q103.
New tunables: `union.dues_credits_per_cycle`, `union.strike_pay_credits_per_cycle`; the six goldens regenerated (`Params`, `Event`).
Next session should know: all five presets are event-for-event identical to S0.17c (Freeport 594 244, Commune 121 736, Directorate 403 036, Republic 597 267, Commonwealth 519 457 events on seed 1) since no script touches a union. The server's offer view gained the `collective_agreement` kind (an exhaustive match; one line in `crates/isms-server/src/views.rs`, as S0.17c did for `bank_loan`). S0.18 (cross-preset invariants, the nightly workflow, `neutrality-00.md`) closes Phase 0.
make check: green · new tests: 5 · sim-check: republic green seeds 1-5 (unchanged); the other four unchanged

## S0.18 — Cross-preset invariants, nightly sim-all, neutrality-00, Phase 0 exit — 2026-09-13 — PR #47
Built: `crates/isms-sim/tests/invariants.rs`: an `Observer` that checks the world at every cycle close and epoch start (conservation of money and every good, no meter above full, no citizen balance, org treasury, society treasury or till below zero) and, per preset, that a second run from the same seed gives the same world hash and event count; `all_five_presets_hold_engine_invariants` (one epoch, seed 1, in `make check`) and the ignored `all_five_presets_five_epochs_invariants` (five epochs, seeds 1-5; run once by hand this session, green). `.github/workflows/sim-nightly.yml`: `make sim-all`-equivalent with `--check` plus the ignored invariants test, nightly at 04:17 UTC and on `workflow_dispatch`; not a PR gate. `docs/tuning/neutrality-00.md`: the five presets side by side (constitution axes, every `[policy]` field named to its system, `[params]` overrides (none), the four seeding paths, headline metrics, flags for review, the open items for Chris, the Phase 0 exit checklist). `docs/tuning/README.md` lists the five reports, the neutrality note, the nightly workflow and the invariants test.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: none.
New tunables: none. No engine change; every golden and every preset's event count is as in S0.17d.
Next session should know: Phase 0 exit is met on every line of the checklist in `neutrality-00.md` except the cross-arch dispatch, which needs main to carry this PR first (`gh workflow run "cross-arch determinism"` and read the run). The flags for Chris are in the note: the Commonwealth's thin pool, lone stewards capturing a glutted coop's surplus, the inert `max_machines_per_member`, the ratio-of-sums investment statistic, and `lending_rule = vote` / `public_bank` waiting for Phase 3-4. Q-numbers in the Phase 0b plan (Q47-Q86) do not match the file after three renumberings against the Phase 1 track (Q47-Q103 in the file; the plan's Q72-Q86 are Q86-Q103); the file is authoritative.
make check: green · new tests: 2 · sim-check: all five presets green seeds 1-5 (unchanged)

## S1.9 — Work and Standing Plan screens — 2026-09-15 — PR #49
Built: `web/src/screens/Work.tsx` (GDD 4.3): the allocation editor over the positions you hold (one row per employment contract: employer, pay, hours input, effort select), each effort level annotated with what it costs and yields, the running total against the budget with fatigue debt shown, and the engine's rejection text shown as is (`OverBudget`, `OverContractHours`, the union cap); the full payslip list with Explain; the skill panel (family, level, output multiplier, hours). `web/src/screens/Plan.tsx` (GDD 9.3, TDD 6): the whole `StandingPlan` on one screen, sent whole on save: labor mode (choices from `capabilities.labor`), keep-Food-at-least, the Food price ceiling, the Wares rule, the saving floor, standing orders (good or share, side, qty, limit, refresh) and the vote default; money rules mount only with `money`, standing orders only with `order_books`, the vote default only when `governance != none`. Routes `/s/{id}/work` and `/s/{id}/plan`; the nav links Home, Work and the plan and leaves the rest as labels; Home links to both. `LaborView.effort` (`EffortCosts`) carries the preset's effort table so the tooltips hold no numbers of their own. `lib/payslips.ts` names payslip orgs from the labor view (shared by Home and Work). `useSetLabor` takes the full allocation list. `web/e2e/work-plan.spec.ts`: join, take a position, set 9 hours and read the rejection, set 6 h at high effort, edit the plan, reload, see it persisted; `scripts/e2e/web.sh` mints a third session for it.
Deviations from TDD: none. Against `docs/wireframes.pdf` p.5: Work and the plan are two routes rather than one page; the Leave (notice) button waits for S1.12's contract termination and "add a second workplace" for S1.11's job board; payslips are ledger rows (when, what, amount with Explain) rather than the cycle/employer/basis table; the Food ceiling shows "last price x 1.25" without computing the current figure (S1.10 has the last price). The Vite proxy now serves the app for HTML navigations under `/s/{id}` (a reload of `/s/1/plan` was reaching the API); the deploy's reverse proxy (S1.14) needs the same rule.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the effort tooltips and the `max_workplaces` line read the preset through `LaborView.effort`; add there if a screen needs another labor parameter. Payslips still say "org #n" for an employer you no longer work for; S1.11's org screens replace `orgNamer`. The plan's vote-default "follow" takes a citizen id until S1.13's citizens list can offer a picker. Standing orders are editable but nothing on this screen shows the last price; S1.10 should link each order to its book.
make check: green · new tests: 1 Playwright · sim-check: n/a

## S1.10 — Market screens — 2026-09-15 — PR #50
Built: `web/src/screens/Market.tsx` (GDD 6.1, TDD 11) at `/s/{id}/market` and `/s/{id}/market/{instrument}`: the instrument list (goods with last price and the change over three cycles from `/prices`; every firm with a share registry as a share instrument with its book value and your holding, from `/orgs`) and the price index; one book at a time with the bid and ask ladders ("n yours" per level), the depth SVG, the tape (buyer and seller named, your trades in ink), and the price history on `TimeSeries`; the order form with side, quantity, limit (defaulting to the last price), the default expiry, and the escrow preview (a bid holds qty x limit, balance after against the plan's floor; an ask holds the goods or shares); the plan's Food and Wares rules surfaced inline on those books; pantry room on a goods bid (cap minus pantry minus open bids, as the engine counts it); your open orders on the book with per-row cancel and their source (manual or standing). `HouseholdView.pantry_capacity` carries the preset's per-good caps. `api/market.ts`: `useBooks`, `useBook`, `usePrices`, `useOrgs`, `usePlaceOrder`, `useCancelOrder`. The nav's Market entry is a link; the shell resolves a section to its route through one table. `web/e2e/market.spec.ts`: a new citizen bids for Food above the legacy asks, reads the fill count from the reply, sees the trade in the tape and Food in the pantry, and reads the engine's rejection for an oversized bid; `scripts/e2e/web.sh` mints a fourth session.
Deviations from TDD: none. Against `docs/wireframes.pdf` p.6: the change column is over three cycles rather than one; ask levels do not name the firm behind them (the book view carries no owner per level); expiry is the engine's default and not editable; cancel is per row rather than "cancel selected"; the fill message comes from the command's reply rather than from watching the tape.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: a fresh society's Food book has no asks until the legacy firms have produced (about a cycle at one tick a second), so the market test waits up to three minutes for an ask; a seeded society for a demo should run a few cycles before the Market is shown. Share rows come from `/orgs` because `/books` lists a share book only once an order has been placed on it. The tape names the counterparty as `org #n`; S1.11 can name it once org names are on hand in a shared lookup (`orgNamer` in `lib/payslips.ts` is the seed of that).
make check: green · new tests: 1 Playwright · sim-check: n/a

## S1.11 — Org screens: found and manage a firm — 2026-09-15 — PR #51
Built: `web/src/screens/Orgs.tsx` at `/s/{id}/orgs` (GDD 6.1 firms, 9.2 office archetype): your organizations, the public job board (employment offers named by firm, with pay, hours, term, notice and places, and a Take-it button), the found-an-org flow (kind from `capabilities.org_kinds`, name, first workplace picked from the slot list showing free-of-total per slot-limited kind, the cost preview against your balance and pantry, disabled until you can pay), and every organization with kind, workplaces, staff and book value. `web/src/screens/Org.tsx` at `/s/{id}/orgs/{oid}`: the overview (treasury, book value, inventory, staff, shares), the registry with percentages, your own contract with the org when you work there, the production view per workplace (workers, hours, attributed output this cycle, with the monitoring note from `monitoring_sigma`), the org's open offers, and, for the manager, the workspace: post a job offer (hourly or piece rate, hours, places, term, notice), sell from inventory (an ask on behalf of the org), machines (install, uninstall, bid for more from the treasury), add a workplace with slot scarcity; for the controlling owner: declare a dividend with the treasury cost shown, issue shares, list shares for sale on the notice board, appoint or vacate the manager. `OrgsView.founding` and `OrgsView.slots` carry the founding costs and slot counts so the client previews what the engine will charge. `api/orgs.ts` has one hook per command; `lib/offers.ts` reads an employment offer for both screens. `web/e2e/orgs.spec.ts`: a new citizen buys 20 Materials on the Market, founds "Iron & Sons" with a Mine, posts a job offer, a second person takes it through the public API and sets 8 hours, and the manager's production view shows the worker with attributed output within a tick; `scripts/e2e/web.sh` mints the two sessions.
Deviations from TDD: none. Against `docs/wireframes.pdf` p.7: one page rather than tabs; no per-worker Explain popover yet (the org view carries attributed output per cycle, not the per-tick `Produced` explain; `/explain/{seq}` exists for a later card); payroll-due and last-cycle revenue lines are not computed (no view carries them); the job offer has no Edit, only a new offer.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the hire in the org test is a second human through the API because householders are all employed in a fresh society (Q46). `useOrgs` (Market) and `useOrgsView` (Orgs) share one cache entry and `useOrgs` narrows it with `select`; a hook that caches a different shape under a shared key crashes the other screen. Org names for the tape and payslips can now come from `useOrgs`. The employee's "leave with notice" is S1.12's contract termination; the org channel is S1.13's Talk.
make check: green · new tests: 1 Playwright · sim-check: n/a

## S1.12 — Contracts, housing, transfers, notice board — 2026-09-15 — PR #52
Built: `web/src/screens/Contracts.tsx` at `/s/{id}/contracts` (GDD 7.2, 7.3, 6.1 credit and housing): housing first, with the Shelter meter, your dwelling (owner, rent, end the lease or move out) or the dwellings to let on the board with Rent-it; the notice board's typed ads (sale with Buy, wanted as an ad to answer with an addressed sale, credit with the schedule spelled out and Borrow, withdraw on your own); the forms: offer a loan (principal, rate a cycle, term, an optional borrower, collateral as a dwelling or shares) with the schedule preview computed as the engine does it, offer goods for sale, post a wanted ad, let a dwelling you own, and transfer money or goods to a citizen or an org with a memo; your contracts (employment, loans in and out, leases in and out, anything else by its kind) with counterparty, terms, status and an end button where the engine allows termination. Money forms mount only with `money`; the loan form only where `credit` is an enabled contract kind; the lease form only with `lease`. `api/contracts.ts` has one hook per command. `web/e2e/contracts.spec.ts`: a new citizen rents a legacy dwelling and watches Shelter climb, lends 100 cr to a second person (who accepts through the API) over two cycles, and sees the installments-left count fall at cycle end; `scripts/e2e/web.sh` mints the two sessions.
Deviations from TDD: none. There is no wireframe page for this screen. The client computes the repayment schedule for the preview with the engine's formula (interest = principal x rate x term, floored installments, the last takes the remainder); the engine remains the authority and the contract's own figures replace the preview once accepted.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the legacy builders keep every empty dwelling on offer at the legacy rent, so a fresh society always has something to let. A wanted ad is answered by a sale offer addressed to its poster; there is no accept on a wanted ad. Owned dwellings are not listed anywhere yet (the API has no per-citizen dwelling list), so the let-a-dwelling form takes an id. Credit collateral seizure and the public default flag are engine behaviour the screen only narrates.
## S1.13 — Society, Chronicle, Talk, Profile — 2026-09-15 — PR #53
Built: `web/src/screens/SocietyScreen.tsx` at `/s/{id}/society` (GDD 10, 14.1; TDD 13): the stat tiles chosen by capability (citizens and people, without work, the price index and mean cycle wage where money exists, firms where orgs exist, credit outstanding where credit exists, need fulfillment, hardship, median wellbeing and the consumption Gini for everyone) from the live pulse and the last `CycleClosed.aggregates`; the Chronicle read by cycle with earlier and later; the scoreboard the constitution names (net worth, self-made, firms at book value); citizens with kind, arrival and flags, each a link to a DM; the published householder script (`/householders`, rendered from its Markdown) behind a Read-it. `web/src/screens/Event.tsx` at `/s/{id}/events/{seq}`: one event as the viewer may see it, every Explain inside it as a `Num` footnote, and the payload as recorded; every headline on Home and in the Chronicle links there. `web/src/screens/Talk.tsx` at `/s/{id}/talk` and `/s/{id}/talk/{channel}`: the Square, the channels of orgs you manage, belong to or work for, and DMs opened by handle, with the logging notice. `web/src/screens/Profile.tsx` at `/profile`: the biography line (new `PATCH /me`, `accounts.biography` in migration 0005), the societies you are a citizen of with the action share (commands by client kind, computed from the event log's `client_kind` column per citizen: `web`, `api_key`, `plan`), and API keys with the agent-use notice, creation showing the key once, and revocation. `web/src/screens/Public.tsx` at `/public` and `/public/s/{id}`: the spectator views on the new no-login routes `GET /public/societies`, `GET /public/s/{id}/stats` (`PublicStatsView`, with the flags the tiles mount on so a visitor needs no capabilities call) and `GET /public/s/{id}/chronicle`. The shell header links to the profile; the Vite proxy serves the app for HTML navigations under `/public` as it does under `/s/{id}`. `web/e2e/society.spec.ts`: a headline leads to its event; a word on the Square; the biography survives a reload; a key minted on the profile is used for a `PUT /s/{id}/plan` with a Bearer header and the profile then shows `api key` in the action share; a fresh browser context with no cookie reads a society's Chronicle at `/public/s/{id}`.
Deviations from TDD: none. The done gate's "CLI call" is made as the CLI makes it (an API-key Bearer request against the public API) from the test's own request context rather than by spawning the `isms` binary, so the web suite needs no built CLI. `/public/archives/{society}/{epoch}` waits for S1.15's epoch archives. There is no wireframe page for these screens.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the action share is a store query over `events` (`actor = {"citizen": n}` grouped by `client_kind`), not engine state; `Citizen.api_share` in `isms-core` is still never incremented (Phase 2 can either drive it from an event or drop it). `PublicStatsView` duplicates `StatsView` plus the society's name and three booleans on purpose: a visitor's page makes one call. The dev database `isms_dev` refuses this branch's migrations (checksum of migration 2 differs between checkouts, CRLF against LF); a branch gets its own database (`isms_s113`) and `cargo sqlx prepare` must run with that `DATABASE_URL` in the environment, since `make sqlx-prepare` sources `.env` and overrides it.
make check: green · new tests: 1 Playwright · sim-check: n/a

## S1.13b [engine] — Collapse only after the floor has been reached — 2026-09-16 — PR #54
Built: ADR-0006. `World.meta.reached_floor` is set at the first cycle end of an epoch where active humans are at or above `params.population.floor`, cleared by `start_epoch`; phase 8m runs the `low_population_cycles` counter only once it is set, so a society that never drew a crowd runs as an AI economy to its scheduled end and only a society that had the crowd and lost it for `collapse_cycles` collapses. `CycleClosed` carries `reached_floor` (serde default) for replay and the fold. `tests/collapse.rs`: one human under a floor of two runs twenty cycles without ending; two humans reach the floor, go dormant unseen, and the epoch ends by collapse after the grace cycles with the counter at `collapse_cycles`. Goldens for the four 3-cycle preset runs regenerated (the new field only). QUESTIONS Q104 decided.
Deviations from TDD: ADR-0006 (TDD 5.5 phase 9 and GDD Q7 read "active humans below the floor for 5 cycles"; the counter now waits for the floor to have been reached once).
Provisional answers added to QUESTIONS.md: none; Q104 moved from provisional to decided.
New tunables: none. The threshold is `population.floor`; `collapse_enabled` stays the simulator switch.
Next session should know: `Meta` gained a field, so postcard snapshots written before this PR do not decode; dev databases from before it need reseeding (Phase 1 has no snapshot versioning; S1.15 should decide whether to add one before the playtest). Demo seeds no longer need `collapse_enabled=false`.
make check: green · new tests: 2 · sim-check: n/a (the simulator runs with collapse disabled; no preset bytes change beyond the new field)

## S1.11b — Payroll line and the balance in the header — 2026-09-16 — PR #55
Built: on the org page the manager sees a payroll block under Production: what is due at cycle end (hourly contracts at the worker's hours this cycle, piece-rate ones at what is attributed so far, computed from the org's active employment contracts and its workers), the treasury, and covered or short by how much, with the consequence spelled out; a top-up form moves money from the manager's balance to the treasury as a transfer with the memo "treasury", with a one-click fill of the shortfall. The job-offer form now says that a new firm starts with an empty treasury because the founding fee is burned. The shell header shows the citizen's balance beside the clock where money exists. `orgs.spec.ts` asserts the due, the shortfall, the top-up and the header balance.
Deviations from TDD: none. Prompted by Chris's playtest on 2026-09-16: ChapsCo hired at 8.25 an hour with an empty treasury and lost the worker at the first payday; nothing on the screen had said payroll would fall due.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the payroll figure is the client's estimate from the contract terms and the current allocation; the engine's own figure arrives with the `Paid` and `PaymentMissed` events. Householders take a job only while unemployed and never switch for a better wage; whether they should is a design question, not a UI fix.
make check: green · new tests: 0 (assertions added to one) · sim-check: n/a

## S1.11c — Treasury ledger, Max, and the book hint on the org page — 2026-09-16 — PR #56
Built: `GET /s/{id}/orgs/{oid}/ledger` (`OrgLedgerView`): the events that moved the org's treasury or its escrow (Trade, SaleAccepted, Transferred, Paid, PaymentMissed, DividendPaid, OrderPlaced), oldest first, filtered by the org appearing anywhere in the payload, for the manager, shareholders and members (403 otherwise), as `EventRef`s through the viewer. The org page shows it as a `Ledger`, newest first, with the treasury's sign on each line (sales in, purchases and wages out, escrow on placed orders) and the Explain where the event carries one. The sell-from-inventory form gains a Max button that fills the quantity from the inventory of the chosen good and a hint beside the limit with the good's last price, best bid and best ask from its book. `orgs.spec.ts` asserts the top-up in the ledger, Max, and the hint.
Deviations from TDD: none. Prompted by Chris asking how his treasury rose without a sale he could see: his resting asks were filling tick by tick against the legacy foundries' bids, visible only on the Market tape.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the ledger reads the last 500 events of each kind and filters, like the tape; a long-lived society will want a store query by org (an index on `payload` or an `org_id` column) before S1.15's load test. Order cancellations and expiries are not in the ledger because their events name only the order id; the escrow return shows up as the treasury total moving.
make check: green · new tests: 0 (assertions added to one) · sim-check: n/a

## S1.13c — Operator mode: pause, step, resume, tick length, end epoch — 2026-09-16 — PR #57
Built: ADR-0007. `scheduler::Control` (paused flag, live `Schedule`, a wake-up) replaces the copied `Schedule` on `SocietyEntry` and in the scheduler loop; pause holds the loop, resume re-anchors `tick_origin` so the next tick is due one tick length from now (a pause is not downtime: nothing is caught up), step resolves one tick while held, and a new tick length re-anchors the same way. `societies.status` persists `paused`, and a paused society now loads at start with its scheduler held (before, anything but `active` was skipped). The store gains `set_society_status` and `set_society_schedule`. Operators are account emails in `ISMS_OPERATORS`; `Account.operator` tells the client; the routes are `GET /admin/societies`, `POST /admin/s/{id}/pause|resume|step|tick-seconds|end-epoch`, 403 for anyone else, `end-epoch` sending the engine's one operator command as `Actor::System`. The summary's `next_tick_at` is null while held. Web: `/admin` (the Operator room) with each society's clock, people, state, tick length and the buttons, ending an epoch behind a second click; the shell header links there for operators; the Vite proxy forwards `/admin/`. `web/e2e/admin.spec.ts`: pause holds the tick for three seconds, step advances it by one, resume lets it run, and a non-operator gets 403 and the refusal page; a scheduler unit test asserts the re-anchoring. `scripts/e2e/web.sh` names `admin@example.test` as the operator and mints its session.
Deviations from TDD: ADR-0007 (operator routes on the public API; a pause that does not catch up).
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: commands still apply while a society is held (citizens can trade with the world standing still); a playtest that pauses should say so. Ending an epoch by hand leaves the society idle until S1.15's rollover. `.env` now documents `ISMS_OPERATORS`; the dev server for Chris runs with his email in it.
make check: green · new tests: 1 unit, 1 Playwright · sim-check: n/a

## S1.13d — Operator mode: start the next epoch — 2026-09-16 — PR #58
Built: ADR-0007 amended. `ActorMsg::NewEpoch`: once the epoch has ended, the actor takes the engine's `start_epoch(world, rules, epoch + 1)` (S0.13: roster kept, material state reset, tick 0, legacy firms and householders reseeded) as one batch, with each event's meta taken against a scratch clone as the seeding at creation does; while the epoch is running it is rejected with `EpochEnded`. The scheduler no longer parks forever on an ended society: it waits for the control's wake-up, then looks at the world again. `POST /admin/s/{id}/new-epoch` (operator) starts the epoch, re-anchors the clock so tick 0 is due one tick length from now, and records the epoch on the society row (`set_society_epoch`). The Operator room shows "Start epoch N+1" on an ended society. `admin.spec.ts` now ends the epoch by hand, starts epoch 2, and sees it tick.
Deviations from TDD: the amendment to ADR-0007; GDD 11.2's end-of-epoch sequence (announcement at N-2, closing statements, archive, Observatory snapshot) remains S1.15's, so this is the plain restart until then.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: a new epoch keeps every human citizen and their plan but nothing material; citizens who were dormant stay dormant. Epoch numbers on stored events come from the world at the time, so the ledger and Chronicle split cleanly by epoch. The society row's `epoch` column is informational; the world is the authority.
make check: green · new tests: 0 (assertions added to one) · sim-check: n/a

## S1.13e — The world clock in familiar units — 2026-09-16 — PR #59
Built: `components/WorldClock.tsx`: the shell header reads "Epoch 1 · Day 6 · 2:35 PM" with a thin bar for the share of the current tick already elapsed and the countdown to the next tick beside it; a cycle is a day and a tick an hour when the cycle has 24, the minutes coming from the wall clock's position between ticks (`tickProgress` from `next_tick_at` and `tick_seconds`), and a cycle of another length falls back to "tick t/N". A held clock shows an empty bar and "clock held"; a society at tick length 0 says "as fast as it can". The public society page and the Operator room say "Day" for the cycle as well. Unit tests for the time and the progress; the gallery shows the running and the held clock.
Deviations from TDD: none. "Day" and "Epoch" are the client's words rather than lexicon entries: every preset would spell them the same, and the lexicon's key test would want them in all five files for no gain.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the in-world time of day is a presentation of the tick index (tick 1 is midnight), not the preset's `cycle_boundary_hour`, which is informational until S1.14 decides how cycle ends map to wall-clock hours.
make check: green · new tests: 2 unit · sim-check: n/a

## S1.13f — A day of play: plain words, epoch boundaries, where goods come from; the plan changes — 2026-09-17 — main ef25765..b2a0185 (no PR, ADR-0008)
Built: all from Chris playing his own firm and asking about what he saw. Web: `lib/when` and `lib/names` replace `c3 t14`, `org #15`, `workplace 4` and `citizen 12` with "Day 3, 2 PM" and names everywhere a player reads (the operator page keeps ticks); player copy says day and hour for cycle and tick; the Contracts forms pick citizens and orgs by name; every rejection passes through `names.inText` before a screen shows it ("c41 already works at w19" reads "You already work at the mill at Hollow Mill"); the job board confirms a job taken, says its hours are 0 until set, and shows "you work here" in place of the button; operator actions say what they did and how many ticks remain to payday; need meters carry tooltips told from `needs.rs` without preset numbers; a good's market page shows where it comes from (recipe, producers with workers, made today, stock and why idle, upstream goods, uses) and says when an empty ask side is a sold-out one; one CSS rule gives every table column a gutter. Server: `read_since_tick` and `read_kind_since_tick` take an epoch (ticks restart at 0 each epoch, so the second epoch's price history was the first epoch's opening days, and Home's digest read the old epoch); `EventRef.epoch`; `OrgsView.former` (orgs only the log remembers, so last epoch's payslips name their payer) and `OrgsView.recipes`; lists that span epochs draw a labelled line at the boundary. CLI: `--session` accepts a token that starts with a hyphen (URL-safe base64; a 1-in-64 failure that reddened CI once). Workflow: ADR-0008, `scripts/check-and-push.sh`, `scripts/hooks/pre-push`, `make push`, `make hooks`; `scripts/e2e/web.sh` honors `CARGO_TARGET_DIR`.
Deviations from TDD: ADR-0008 (PRs for engine changes only; check, then push). ADR-0009 (Phase 1 exits with a small cohort and synthetic players: S1.14 parked, S1.15 trimmed, new card S1.16, society class `lab`).
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: the next card is **S1.16**, then S1.15; S1.14 waits for a second human. Text the server sends (rejections, headlines) is still written with ids and "cycle"; the web translates rejections on the way in, headlines are untouched, and the engine's own wording is an engine PR if it is ever wanted. Householders still never switch jobs (undecided by Chris); S1.16's wage-maximiser persona will show what that costs. The main worktree has its own git-ignored `.env` (database `isms_play2`); a plain `make check` there fails while the dev stack runs because `target\debug\isms-server.exe` is held open, which `make push` avoids. `GET /s/{id}/orgs` now reads up to 20,000 `OrgFounded` events on every call for `former`; fine for a lab society, a projection before the S1.15 load test. Payslips, the org ledger and the tape still read "the last N of a kind" across epochs by design, with the boundary drawn.
make check: green at every push (check-and-push) · new tests: 29 web unit (were 7), 1 store, 1 cli; Playwright 10 of 10 · sim-check: n/a

## S1.16 — Synthetic players: a hybrid cohort in lab societies — 2026-09-17 — main eeaee1c.. (no PR, ADR-0008)
Built: `societies.class` gains `lab` (CHECK constraint, `isms-server seed --class lab`, `SocietySummary.class`, the three `/public/*` routes skip it; Q105). `agents/`: a TypeScript harness (own pnpm package) whose typed client comes from the same `/openapi.json` as the web's; a `Brain` seam with two brains (ADR-0010): scripted strategies per persona plus a fuzzer for the rule-prober, and an LLM brain on the Anthropic SDK's tool runner (rules + persona as a cached prefix, `end_turn` last, a once-a-day reflection on the cycle model as structured output). Seventeen read tools and twenty-two act tools over the public API, every call validated, capped per turn and journaled by one `invoke`; refusals come back as the server's problem document verbatim. Tick edges from the `/s/{id}/stream` WebSocket (Q106) with a `/home` poll fallback. Eight personas as markdown with frontmatter. Journals (one JSONL line per turn), a per-run budget in tokens and dollars, a report (`pnpm --dir agents report <run>`) with refusals grouped by code and blanked text, fuzzer findings, confusions by endpoint, a paragraph per player. `scripts/agents/run.sh` + `make agents` (fresh DB, lab Freeport, side port, cohort, report, `rebuild`), `make e2e-agents` (scripted, two one-second days, fails on any accepted probe or 5xx not in `agents/known-defects.txt`) in CI, `make agents-check` in `make check` and in check-and-push. `docs/playtest/phase1-defects.md` (D1–D4), Q107–Q108, `crates/isms-store/build.rs` so a new migration rebuilds the migrator.
Deviations from TDD: ADR-0010 (scripted brains and a fuzzer beside the LLM personas; model selectable per run and per persona); the stream is the WebSocket, not SSE (Q106).
Provisional answers added to QUESTIONS.md: Q105 (lab visibility), Q106 (WS not SSE), Q107 (unknown ids answer 422 with a code), Q108 (SetStandingPlan validates instruments).
New tunables: none in presets. Harness knobs live in `agents/config.toml` (models, effort, action cap, concurrency, budget, personas).
Next session should know: the live mixed run was played the same evening once Chris put a key in `.env`: `docs/playtest/runs/live-20260917-2136.md`, eight players (five on Sonnet 5, Opus 5 reflections), 855 turns, six days and seventeen hours, $12.74, stopped by the 15M-token cap (cache reads count; the default is now 60M and dollars are the real cap), `rebuild` clean. The Anthropic account ran out of credit at the end of day 2 and a day of model turns failed until it was topped up; the brain now stops a player on a billing or key error. The run's findings are D5–D11: a transfer into an org's treasury that the org's next order cannot see (D5, major), a Builder whose dwellings never appear anywhere the manager can read (D6, major), payslip hours that are not the hours set (D8), an employment offer that cannot be withdrawn (D9, Q109). Two loud findings were the harness's own and are fixed: the situation header printed every flag key, and the wage-maximiser worked a contract in its notice period (120 refusals). The model players skipped about half the hours at ten-second ticks, as expected; the founder, borrower and landlord each founded a firm, which householders never do. The scripted cohort ran three times end to end and its replay is the fixture in `agents/test/fixtures/transcripts/` (a scripted player; record an LLM player with the same command once a key exists, see `agents/README.md`). The fuzzer's D1 (`SetStandingPlan` accepts a share order on a nonexistent org) is an engine fix for S1.15 through a PR and is allowlisted in `agents/known-defects.txt` until then; D2 (refusals name raw ids, twelve distinct texts) is the same finding Chris made by hand on 2026-09-17 and now has a catalogue. `sqlx::migrate!()` does not see a new migration file in an incremental build; the store's `build.rs` now fixes that. The founder and landlord strategies need more than two days to found or buy anything; at seven days the mixed run will show whether the LLM founder reads the recipes.
make check: green via check-and-push · new tests: 50 harness (vitest), 1 server, 1 store · sim-check: n/a

## S1.15a — The playtest follow-ups: eleven defect rows closed, three engine PRs, a subscription-side reflection — 2026-09-18 — main dfc2052.. (PRs #60 D1, #61 D8, #62 D9)
Built: the ordered list in `docs/playtest/phase1-defects.md` (items 1–15), in its order, with two rows re-read against the evidence first. **D5 closed, not a defect:** the landlord's journal shows both refused orders came *before* the transfer in the same turn, and the event log in `isms_agents_live_20260917_2136` reconciles the 8.88 to the cent (200.00 in, then the manager's own Materials bids and payroll); the fix is `OrgView.escrow`, money resting in the org's bids, shown beside the treasury. **D6 needed no engine change:** the same log shows the firm built six dwellings the API never listed; `OrgView.dwellings`, `DwellingView.offer`, `RecipeView.produces_asset`, `DwellingBuilt` on the org ledger, a Dwellings section on the org page with let/sell on the firm's behalf, and the rule in GDD §4.1 (Q110 decided, corrected: per hour, not at cycle end). D7 (`last_payment_missed`: who, when, owed, paid, for the org's manager, owners and members) and D12 (`declared_dividend` on the view, the form hidden until the day ends, the cost preview on citizen-held shares). **D2 as a server-side naming pass** (`crates/isms-server/src/names.rs`, applied in `send`): every refusal reaches every client as the web already showed it, "You already work at the mill at Hollow Mill", handles and org names, "you" with its verb, days and hours; the engine's texts and `code` are unchanged and the web's own pass leaves a named sentence alone. D3 and D10: an offer that is not on the board answers `unknown_offer` from accept and withdraw, "was taken or withdrawn" when the id was once issued (Q107 decided). D11: the Comfort tooltip says it is a third of wellbeing and changes neither output nor pay; the Welcome Brief sentence rode the D8 PR. D4: the hours editor caps a row at its contract and refuses to save past the day's budget. Three engine PRs: **D1** (`market::check_instrument` shared by `PlaceOrder` and `SetStandingPlan`, Q108 decided, the probe leaves `agents/known-defects.txt`), **D8** (the payslip Explain carries `tick_hours`, `ticks_per_cycle`, `hours`, `rate` and `x treasury / owed` when short; the slacker's 2.625 was 63 tick-hours over 24 after a mid-day change; goldens regenerated), **D9** (`Command::WithdrawOffer` for every open offer kind, `Event::OfferWithdrawn` appended after `StrikeEnded`, a credit offer's escrow returned; the server sends it for every kind and infers `on_behalf_of` from the poster, which also fixes the web's org-sale withdraw that was refused `not_party`; Q109 decided). Harness: `models.cycle_provider = "claude_code"` hands the once-a-day reflection to `claude -p --output-format json --json-schema` on the account's subscription (Chris's ask), tokens counted at no dollar cost under `<model> via claude-code`; the Makefile refuses to run without `MSYSTEM` (Git Bash) instead of hanging in WSL's bash.
Deviations from TDD: none. New rows in TDD §5.3/§5.4 for `WithdrawOffer` / `OfferWithdrawn`.
Provisional answers added to QUESTIONS.md: Q107–Q110 decided; Q111 (membership requests are not withdrawable yet), Q112 (an org landlord's rent is not on its ledger), Q113 (the Builder arm emits no `Produced`, so piece rates never see Builder work), Q114 (whether Comfort should feed output, GDD §20).
New tunables: none in presets. `agents/config.toml` gains `models.cycle_provider`.
Next session should know: the second `make agents` run (item 12) is the next thing to do, on this build, with the reflection on the subscription; then record an LLM player for the replay fixture (item 13). Main's CI went red twice on Playwright: once on the work-plan spec, which waited for a refusal the editor now prevents (fixed), and once on the operator pause test racing a tick in flight on the runner (the spec now waits for a settled clock). Items 16–18 (epoch end and the archive, the load test with the §17 assertions and T5, the interview guide `docs/playtest/phase1.md`, Chris's own Freeport epoch) are S1.15 proper and untouched. Three worktrees exist now (`../Isms-p1`, `../Isms-p2`, `../Isms-check`); the p2 one was added for D9 while p1 carried D8. A fresh target dir may fail on `utoipa-swagger-ui`'s download once; a retry fixed it. Docs rows in `phase1-defects.md` and `QUESTIONS.md` conflict on every rebase because each fix edits an adjacent row: resolve by row id, both sides.
Later the same day: three model-driven runs on this build, none past day 3 (`docs/playtest/runs/2026-09-18-partial-runs.md`); Chris stopped the tests at his budget and ADR-0011 replaces the full-epoch exit criterion with the closed findings plus the free `make e2e-agents`. D13 (a society's clock stops for good after one failed tick) is the blocking row to fix first in S1.15 proper. Harness: the daily reflection runs on the subscription through `claude -p`, one at a time, off the turn path.
make check: green via check-and-push and three PR cycles · new tests: 6 engine (plan 1, employment 2, credit 1, housing 1, transfers 1), 7 server (5 naming unit, dwellings, withdraw), 2 web unit, 4 harness; goldens regenerated for the payslip inputs · sim-check: n/a

## S1.15b — The epoch-end sequence: the announcement, the frozen summary, the archive, the closing statements, the rollover — 2026-09-18 — PR #63 (engine) + main (no PR, ADR-0008)
Built: `docs/plans/s1.15-epoch-end.md`, steps 1–6. **Engine (PR #63):** `Event::EpochEnding { final_cycle }`, appended after `OfferWithdrawn`, at the close of the cycle two before the last (none when `epoch_cycles ≤ 2`, Q115; collapse and an operator's end give no warning); `apply` keeps `meta.epoch_ending`, cleared by `EpochStarted`. `EpochEnded` carries `summary: EpochSummary` (the last cycle's `CycleAggregates` and `standings`: every citizen ranked by net worth with handle, kind, dormant, net worth, self-made), computed in the ending tick from the scratch world so the fold reproduces it; an operator's end carries the running cycle's figures. `params.time.closing_window_minutes = 2880` in `_base.toml`; the Freeport Chronicle says "Two days remain in the epoch. Settle what you can." Goldens regenerated. **Store and server (main):** migration `0007_epoch_archives` (society, epoch, ended_at, reason, final_cycle, ended_seq, summary jsonb, closes_at, closing_statements jsonb); `EventStore::append_batch_archiving` writes the row in the same transaction as the `EpochEnded` it belongs to, the actor building it from the event and the society's window; `isms_store::archives` reads, `write_closing_statement` (one entry per citizen, arrival order, under `FOR UPDATE`), `close_statements`. Routes: `GET /public/s/{id}/archives` and `/archives/{epoch}` (404 for a `lab` society like every spectator route), `GET /s/{id}/archives` and `/archives/{epoch}` (with `mine`), `PUT /s/{id}/closing-statement` (trimmed, ≤ 2000 chars, 404 before any epoch has ended, 422 `epoch_ended` once the window has closed). `runtime::start_next_epoch` is the one rollover: the actor's `new_epoch`, the clock re-anchored, the row updated, the statements closed; the scheduler calls it when `closes_at` passes (a held clock holds the rollover; an ended epoch with no archive row waits for an operator as before), and `POST /admin/s/{id}/new-epoch` calls it as "start now". `Clock.epoch_ending`; `AdminSocietyView.statements_close_at`. **Web:** `screens/Archives.tsx` at `/s/{id}/archives` (nav "Archive") and `/public/s/{id}/archives`: one card per epoch (how it ended, the summary tiles, the top ten standings, the closing statements as quotes, the citizen's own box with the countdown while open); Home says "The epoch ends after Day 42: today and tomorrow remain." once announced and links to the archive once ended; the Operator room shows when the statements close and offers "Start epoch N now". **Harness:** `pnpm --dir agents rollover` (the first player leaves a closing statement, then waits for the next epoch and rewrites the report with "The epoch's end"); `scripts/agents/run.sh` seeds a one-minute window and runs it after the cohort, so `make e2e-agents` sees the rollover.
Deviations from TDD: none. The plan's `epoch.closing_window_hours` became `time.closing_window_minutes` (lab societies need a minute); the routes follow the existing `/public/s/{id}/…` shape; the statement route is a PUT (a replace); the closed window answers with the engine's `epoch_ended` code since `Problem.code` is the engine enum. TDD §5.4, §10.2, §12 and Appendix A rows updated; the plan carries an as-built section.
Provisional answers added to QUESTIONS.md: Q114 (a pointer: the number the S1.15a entry used for the Comfort question without a row), Q115 (an epoch under three cycles never announces).
New tunables: `time.closing_window_minutes` (2880).
Next session should know: the rest of S1.15 is the load test with the §17 assertions and the T5 measurement (on this build, so an epoch's events include the archive write), the interview guide `docs/playtest/phase1.md`, and Chris's own Freeport epoch. `World` gained `meta.epoch_ending`, so a database seeded before this build cannot load its snapshot: reseed on a fresh one (`isms_s115` on the dev Postgres has every migration and served `cargo sqlx prepare`). The runtime test with the one-minute window takes about 65 s on purpose. The archive page has been read by Playwright and by the harness, not yet by a person: its look is unverified [H]. Statements are not events and never replay; `isms-server rebuild` leaves `epoch_archives` alone.
make check: green (clippy workspace, core/sim/store/server tests, web and agents checks) · new tests: 3 engine (tick), 3 server (runtime, society_api, api), 1 web unit; Playwright admin spec extended and green locally; `make e2e-agents` green with the rollover in its log · sim-check: n/a

## S1.15c — The load test, T5 decided, the interview guide — 2026-09-18 — main (no PR, ADR-0008)
Built: `scripts/load/run.sh` + `make load` (a lab Freeport whose floor is householders plus players, so 100 householders and 20 of the harness's scripted brains as the "CLI agents" (Chris's call), `tick_seconds=5`, JSON tick and command log, RSS sampled every 30 s through `tasklist`, the cohort, the rollover, `rebuild`), `scripts/load/measure.py` (folds the log, the journals, Postgres through the dev container's psql and the rebuild output into `docs/playtest/load/<run>.md` with a pass/fail per TDD §17 line; ticks longer than a minute are listed as sleep interruptions, not measured), `isms-server rebuild` logs `world_bytes`, the actor logs `command resolved` with its ms at debug. Three runs: **the full epoch** (`load-20260918-epoch`: 1008 ticks, 259k events; tick p99 253 ms, max 522 ms; 128 MB of table per epoch; World 26 kB; RSS 0.22 GB), **ten societies** (`load-20260918-ten`: RSS 0.05 GB), **three days** with the command line (`load-20260918-3day`: command p99 10 ms over 1,789 commands, 7 ms besides `Seen`). Every budget line passes. **T5 decided:** JSONB stays; `TickResolved` is 7.4 kB a tick and 8% of an epoch's payload bytes, `OrderPlaced` and `Trade` are 78%. `docs/playtest/phase1.md`: the interview guide for one to three players, built as the fill-in report (the epoch to run, what to record without asking, twenty-six questions in nine sections that ask for moments not verdicts, the write-up, and what "done" means).
Deviations from TDD: the load test's agents are the harness's scripted brains over the same public API rather than a separate CLI script. Measured on the developer's machine with Docker Desktop's Postgres, not the 2 vCPU / 4 GB VPS; noted in every report.
Provisional answers added to QUESTIONS.md: none. TDD §12 T5 and the payload-encoding paragraph now carry the measurement.
New tunables: none.
Next session should know: two ticks of the full epoch took 47 and 28 minutes of wall clock because the laptop slept; the scheduler caught up (D13's fix held) and the report lists them apart. The harness's one retry after a 429 puts a second on a client-side call, so the client-side figure measures the per-citizen rate limit, not the server; the actor-side line is the budget's. The load runs' databases (`isms_load_20260918_*`) and run dirs under `agents/runs/` are left for inspection. What remains of S1.15 is Chris's own Freeport epoch with `docs/playtest/phase1.md` filled in; that is the Phase 1 exit.
make check: green via check-and-push · new tests: 0 (three measured runs) · sim-check: n/a

## S1.15d — Phase 1 closed (ADR-0012); Phase 2 cut into cards — 2026-09-19 — main (docs, no PR)
Built: ADR-0012 (every human playtest moves to a new Phase 5 after Phase 4; Phases 1–4 exit on automated gates only; Phase 1 closed on the gates met 2026-09-18). TDD §18.4 S1.15 marked done with the Freeport-epoch line struck; §18.5 rewritten: the Phase 2 milestone becomes eleven cards S2.1–S2.11 in Appendix C format (engine chain S2.1–S2.4 as PRs; server, web, copy and harness cards on main; ∥ marks), a sequencing paragraph, the Phase 2 exit gate, and a Phase 5 paragraph; §18.6 chains the cards and adds the Phase 5 node. GDD §18 table: Phase 1 and 2 exits reworded, Phase 5 row added. `docs/plans/phase2.md` (what exists to reuse, the order, worktrees, four decisions to confirm). `docs/playtest/phase1.md` marked as the Freeport section of the Phase 5 guide, retention struck; `phase1-defects.md` item 18 moved; `CLAUDE.md` card order.
Deviations from TDD: ADR-0012.
Provisional answers added to QUESTIONS.md: Q116 (who may propose which kind), Q117 (quorum denominator and Abstain), Q118 (approval voting, ties, by-elections), Q119 (resolutions and honors), Q120 (open/close workplace instant, costs founding Materials), Q121 (colliding policy changes, one-hop Follow), Q122 (disbursement votes by majority of membership).
New tunables: none (S2.1, S2.2 and S2.4 name theirs).
Next session should know: the next card is **S2.1** in `../Isms-p1` (engine PR); read `docs/plans/phase2.md` first, then the card, then Q116–Q122. The four "decisions to confirm" in the plan are provisional until Chris rules. The card list was cut without interviews, so a card may shrink or split when its Read sections are opened; record that in SESSIONS, not by editing the card silently. `docs/playtest/phase1.md` is not to be filled in before Phase 5.
make check: n/a (docs) · new tests: 0 · sim-check: n/a

## S2.1 — Typed proposals, ballots, quorum, cycle-end close — 2026-09-19 — PR #64
Built: one proposal machine for every society. `Constitution.proposal_kinds: BTreeSet<ProposalKindTag>` and `proposers: Anyone | OfficeHolders` (`serde(default)`), carried on `Capabilities`; `commune.toml` declares `policy_change, resolution, election, recall, honor` and `proposers = "anyone"`. `ProposalKind` gains `PolicyChange { patch: PolicyPatch }` (the Commune's four levers: monitoring, work norm, rationing, Materials split; `apply_to` + `validate_against`), `Resolution`, and the declared-only `Election`, `Recall`, `Honor`, `Disbursement` (refused `NotImplemented` until S2.2–S2.4); `ProposalKind::tag()`. `Proposal` gains `text`, `closes_cycle`, `ballots: BTreeMap<CitizenId, Ballot>` (Admission migrated; `votes` gone). New `governance.rs`: `propose` (tag or proposer outside the constitution → `NotInThisSociety`; householders `NotAuthorized`; dormant `Dormant`; `governance.open_proposals_per_citizen` → `TooManyProposals`; a patch validated at `Propose`; a patch touching `rationing` needs a Coordinator seat, Q116; closes at the end of the cycle it opens in), `vote` (one ballot, replaceable until close; an admission proposal takes the members' vote by the coop's rule, Q123), `cycle_end_8j_close_proposals` (fills missing ballots from `vote_default` as `Voted { by_default: true }`, `Follow` one hop, Q121; quorum `ceil(quorum_fraction × active humans)` over ballots cast with Abstain; pass = yes > no; `ProposalClosed { tally }`; a passed `PolicyChange` → `PolicyChanged { proposal: Some(id) }`, later-over-earlier in id order; admission proposals lapse with their tally), helpers `holds`, `holds_any_office`, `quorum_needed`, `electorate`, `assembly_tally`, `carries`, `default_ballot`. `bank.rs` keeps the admission rule on ballots and exposes `admission_tally`; `VoteAdmission { approve }` maps onto a ballot. Commands `Propose`, `Vote`; events `Proposed`, `Voted` appended after `EpochEnding`; `PolicyChanged.proposal` (`None` from `SetPolicy`/`SetPlan`); reject codes `TooManyProposals`, `UnknownProposal`. TDD §5.2, §5.3, §5.4, Appendix A rows. Goldens regenerated (only `SocietyCreated` moved) plus the new `s2_1_commune_three_proposals`.
Deviations from TDD: none. `PolicyChanged` carries the whole policy, as it already did, rather than the §5.4 `patch`; the patch is on the `Proposed` event.
Provisional answers added to QUESTIONS.md: Q123 (every ballot replaceable until close, admission included; `Vote` on an admission is the members' vote), Q124 (the server actor must re-derive `Rules` after a `PolicyChanged`, or a voted monitoring change takes effect only next epoch).
New tunables: `governance.open_proposals_per_citizen` (3).
Next session should know: S2.2 is next in `../Isms-p1`. No event seats an office-holder yet, so until S2.2 no rationing proposal can open in the Commune (Q116 needs a Coordinator); `tests/governance.rs::seat_coordinator` mutates `world.offices` directly and turns the fold check off, and S2.2 should replace it with `OfficeTaken`. Declared-only kinds carry minimal payloads (`Election { office }`, `Recall { office, citizen }`, `Honor { citizen }`, `Disbursement { org }`) for S2.2–S2.4 to widen. `World` gained fields: reseed any database. `web/src/api/schema.d.ts` is stale on the new reject codes and commands until S2.5 runs `make api-types`. `Capabilities` gained `proposal_kinds` and `proposers`; the server's `CapabilitiesView` does not show them yet (S2.5).
make check: green · new tests: 11 engine (governance: 9 + 2 proptests), 1 updated (bank) · sim-check: commune green

## S2.2 — Offices: elections, terms, rotation, recall, vacancy — 2026-09-19 — PR (engine)
Built: `offices.rs`: `Stand { office }` / `Withdraw { office }` / `Approve { office, candidates }` (approval ballots, any subset, replaceable until close), and the 8j sweep after the proposals close, per office in constitution order: (1) terms ending this cycle expire (`OfficeVacated { reason: TermEnded }`), (2) holders absent `population.office_vacancy_absent_cycles` cycles vacate (`Absence`), (3) the election due closes: `ElectionClosed { approvals }` in rank order (most approvals, fewer past terms, lower id; Q118) then `OfficeTaken` for the top `seats`, or `ElectionRerun` for the seats short by then when nobody stood, (4) `ElectionOpened` for any empty seat plus the seats whose terms end next cycle (so a successor sits the same 8j the term ends; Q125), (5) `OfficeUnfilled { cycles }` once per cycle from `governance.unfilled_office_headline_cycles` on. `start_epoch` opens the first election of every office (closing at the end of cycle 0). `Recall { office, citizen }` is a proposal that carries by `OfficeSpec.recall` (`recall_carries`: majority, or two thirds of yes and no) after the quorum and emits `OfficeVacated { Recalled }`; `NotAnOfficeHolder` for a non-holder. `consecutive = false` refuses a sitting holder whose seat is on the ballot and a citizen whose last full term ended since the election opened (`ConsecutiveTerm`); a partial term never bars (Q118). `World`: `Offices` gains `elections`, `past_terms: ServiceRecord { terms, last_full_term_ended }`, `short_since`; `Election`, `VacancyReason`; `Offices::{filled, holds, record}`. Nine events appended after `Voted`; reject codes `NoElection`, `NotACandidate`, `ConsecutiveTerm`, `NotAnOfficeHolder`. `tests/governance.rs::seat_coordinator` now applies `OfficeTaken` with the fold check on. Goldens regenerated (the new tunable moves `SocietyCreated`; the three office presets gain `ElectionOpened` at epoch start and an `ElectionRerun` per cycle) plus `s2_2_commune_tied_election`. TDD §5.3, §5.4, Appendix A rows.
Deviations from TDD: none. The card's `Election { office, candidates, seats }` is `Offices.elections`, not a `ProposalKind` payload; `Propose(Election)` is refused (Q125). `ElectionClosed` is a sixth office event (the card names five) so the losers' counts are on the record.
Provisional answers added to QUESTIONS.md: Q125 (an election is its own object, opened by the calendar; `Approve` is the ballot; S2.5 needs a ballot route), Q126 (`OfficeSpec.term_cycles` is the term; the three `*_term_cycles` tunables are unread), Q127 (presence is `Seen`; the sim assembly must send it for its scripted humans).
New tunables: `governance.unfilled_office_headline_cycles` (5).
Next session should know: S2.3 is next in `../Isms-p1`. `holds(world, citizen, kind)` and `Offices::holds` are the gate for the coordinator's powers; `NotAnOfficeHolder` is the refusal code for a non-holder (chosen here, reusable by S2.3). A holder's presence is `Seen`: the S2.4 sim assembly must send it for its scripted humans or the coordinators vacate at cycle 3 (Q127). No candidate exists in the sim until S2.4, so every office preset now logs an `ElectionRerun` per cycle and `OfficeUnfilled` from cycle 4's end: harmless, and what GDD 8.3 says. S2.5's `GET /offices` should show the open election (candidates, seats, closes) and the ballot route is new (Q125). `World` gained fields again: reseed any database.
make check: green · new tests: 8 engine (offices: 7 + 1 proptest), 1 updated (governance) · sim-check: commune green

## S2.3 — The coordinator's three powers and honors — 2026-09-20 — PR (engine)
Built: `coordinator.rs`: `OpenWorkplace { kind, slot? }` / `CloseWorkplace { workplace }` for a sitting Coordinator under `Governance::Direct` with a collective (`office_holder` is the holder check, `NotAnOfficeHolder` the refusal, reused from S2.2 and now written into §5.3). Opening spends `founding.materials` from the collective's stock (the Common Store; `InsufficientGoods` when short) on a free slot of the kind and folds like `WorkplaceAdded`; closing emits an `Unassigned` per worker in id order, then `WorkplaceClosed { machines_returned }`, which moves the machines back to the Store, removes the workplace and frees the slot (Q120). `SetPlan` is open to a Coordinator under `Governance::Direct`, targets only (the four policy fields are `NotInThisSociety`; Q129), advisory by construction; `PlanPublished { by: Citizen }`. `Honor { citizen }` proposals open (`UnknownCitizen`, one motion per subject per cycle as `AlreadyExists`; Q119, Q128) and carry as `Honored { citizen, proposal, cycle }` onto `Citizen.honors: Vec<Honor>`; `metrics::honors_of` and `Standing.honors` (`serde(default)`) put the count on the epoch summary for the Commune's scoreboard. `SetPolicy` from `System` is refused under `Governance::Direct` unless `client_kind == Sim` (Q131). Three events appended after `OfficeUnfilled`; `Capabilities::allows` gates the two commands on `governance == Direct && collective`. TDD §5.3, §5.4 rows.
Deviations from TDD: none. The card's `WorkplaceOpened/Closed { by }` carry the org, slot and quantities too, so the fold needs no lookup and the Chronicle has its numbers.
Provisional answers added to QUESTIONS.md: Q128 (anyone on the rolls may be honored, the mover included), Q129 (the Commune's Plan is targets only; advisory by construction), Q130 (a workplace closed mid-cycle loses its workers' hours for the Ledger), Q131 ("outside the sim" is `client_kind != Sim`).
New tunables: none (the org founding Materials figure is reused, as the card asks).
Next session should know: S2.4 is next in `../Isms-p1`. The server's `ScoreRow` has no `honors` yet: `views::scoreboard` is Freeport's, and the Commune's scoreboard (contribution record, honors, need-fulfilment) is S2.5/S2.8's to shape; `metrics::honors_of(citizen)` and `Standing.honors` are there to read. The sim assembly (S2.4) can now honor the top contributor with a real `Honor` proposal and keep using `SetPolicy` via `Sim` until its humans vote. `World` gained `Citizen.honors`: reseed any database. No golden changed: no preset issues the new commands and `Honored` needs a motion.
make check: green · new tests: 6 engine (coordinator) · sim-check: commune green

## S2.4 — Association disbursement votes and the sim assembly — 2026-09-20 — PR (engine + sim)
Built: `ProposalKind::Disbursement { org, to, asset }` as a member-owned org's vote (association, cooperative, union; `credit.rs`): a member moves it (`NotParty` otherwise; `SelfDeal`, `InsufficientFunds`/`InsufficientGoods`, `PantryFull` checked at `Propose`), members vote, a majority of the membership either way closes it at once, a lone member carries on the spot, a split membership lapses at 8j with the members' tally (Q122). Effect is the new event `Disbursed { proposal, org, to, asset, explain }` (appended after `Honored`; folds like `Transferred`; `RuleId::Disbursement`). Gated by `Capabilities::member_orgs()` and money, not by `proposal_kinds`; `propose` dispatches the members' kinds before the `proposers` and title checks (Q133); a carry the org can no longer cover closes failed (Q132). A manager's direct `Transfer` on behalf of a member-owned org is `NotAuthorized`. Sim assembly (`planner.rs`, pure functions of `World`): `sim.assembly_size` humans joined by `isms-sim` before epoch 0 (so the fill counts them toward the floor and the society's dwellings reach them), `Seen` on each cycle's first tick and whenever dormant, living by `householder::decide` (now any active citizen; `run_round` still filters householders), standing wherever `offices::stand` admits them, approving every candidate, voting yes on a split and an honor; the first active human moves the split nudged `sim.split_nudge` toward a bounded scarcity reading (unhoused; Wares below tomorrow's entitlements; machines below one per worker; else back toward even thirds) and honors the top contributor every `sim.honor_every_cycles` (Q134). Invariants added: a `PolicyChanged` from a proposal per epoch, every seat filled from cycle 1, no proposal past `closes_cycle`. TDD §5.3, §5.4, Appendix A rows; `docs/tuning/commune-02.md`.
Deviations from TDD: the card's `{ org, to, what }` is `{ org, to, asset }` to match `Transfer`; the effect is a new `Disbursed` event rather than a `Transferred`, because `Transferred` carries no `Explain` and the non-negotiable requires one.
Provisional answers added to QUESTIONS.md: Q132 (an uncoverable carry closes failed), Q133 (members' votes skip `proposers` and the title), Q134 (what the sim's humans do and how scarcity is read).
New tunables: `sim.assembly_size` (0; 6 in the Commune), `sim.split_nudge` (0.05), `sim.honor_every_cycles` (10), all `# [new tunable] S2.4, sim only`.
Next session should know: the Phase 2 engine chain is closed; S2.5 onward commit to main. S2.5 names `Disbursed` in `names.rs` and exposes `POST /s/{id}/orgs/{oid}/disbursements` as a `Propose` with a `Disbursement` kind (members vote through the ordinary ballot route). Goldens regenerated for `params.sim` in `SocietyCreated` only; one golden lives in `tests/offices.rs` under a name without "golden". The split vote is inert in the Commune because Materials are never short (labor binds); a first cut that read the Store's Machines stock pinned the split at all-Machines while every target stayed green, so a green sweep is not evidence a script is sane. Humans return from each rollover unhoused for two cycles (Q41's roster reset). The sim's humans carry `ClientKind::Sim`. Reseed any database: `Params` gained `sim`.
make check: green · new tests: 7 engine (disbursement), 2 engine (assembly), 1 sim (commune assembly), 3 sim invariants · sim-all: all five stable, the other four byte-identical to main · sim-check: commune green

## S2.5 — Proposal, ballot and office endpoints; the assembly floor — 2026-09-20 — main
Built: `crates/isms-server/src/assembly.rs`: `GET/POST /s/{id}/proposals` (open proposals with the live tally, the roll and my ballot; closed this epoch from the log with the outcome and the `PolicyChanged`/`Honored`/`Disbursed` it produced; the electorate, `quorum_fraction`, `open_proposals_per_citizen` and my `vote_default`), `GET /s/{id}/proposals/{pid}`, `PUT /s/{id}/proposals/{pid}/ballot` (`Vote`, a members' vote included, Q123), `GET /s/{id}/offices` (each office's rule, holders, the open election ranked by `offices::ranked`, my approvals, and `stand_refusal` from a dry run of `offices::stand` named through `Directory`), `POST/DELETE /s/{id}/offices/{kind}/candidacy`, `PUT /s/{id}/offices/{kind}/ballot` (`Approve`, Q125), `POST /s/{id}/orgs/{oid}/disbursements` (a `Propose` with `Disbursement`, S2.4). `comms.rs`: channel `assembly:<pid>`, readable by every citizen who may see the proposal, postable while open and one cycle after (Q138), `NotInThisSociety` when `assembly` is not in `communication`. `CapabilitiesView` gains `proposal_kinds` and `proposers`; `CitizenPublic` and `ScoreRow` gain `honors`. The away digest and the Situation view add the assembly's news (`assembly::concerns`, Q137). `names.rs` names proposals (`p` token, "no open proposal p3"). The actor drops its cached `Rules` after a `PolicyChanged` (Q124). TDD §10.3 gains the Assembly row. `make api-types` regenerated both clients.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: Q135 (ballots are a public roll call; members' votes and their floors are the members'), Q136 (what "a householder id in `Vote`" means), Q137 (the digest's assembly news is server-side), Q138 (a closed floor refuses with `UnknownProposal`; the log places closed proposals).
New tunables: none.
Next session should know: route names and view shapes are frozen for S2.6–S2.8. Cycles on the wire (`closes_cycle`, `term_ends_cycle`, `closed_cycle`) are the engine's 0-based ones like `EventRef`; `Clock` stays 1-based. The mover of a disbursement casts no ballot by moving; a two-member association needs both yes ballots. `POST /proposals` refuses a `disbursement` kind with 400 and points at the org route. The coordinator's `SetPlan`, `OpenWorkplace` and `CloseWorkplace` have no routes yet: S2.8's. The Freeport constitution has no `assembly` channel and no `proposal_kinds`, so every assembly route answers 422 there except the two GETs, which are empty. Chris's dev server locks `target/debug/isms-server.exe`: tests here ran under `CARGO_TARGET_DIR=target/s25`.
make check: green (cargo test isms-server + isms-api-types, clippy -D warnings, fmt, web and agents typecheck locally; `make push` runs the rest) · new tests: 4 server (society_api.rs), 3 names cases · sim-check: n/a

## S2.6 — The Assembly screen and the ballot builder — 2026-09-20 — main
Built: `web/src/api/assembly.ts` (proposals, one proposal, offices; move, ballot, stand, withdraw, approve; every key under the society's so the stream refreshes it). `screens/Assembly.tsx` at `/s/$id/assembly`, in the nav when `caps.governance !== "none"` (label `t("assembly")`): open proposals with what each would do in words, the live tally, a `Meter` with the quorum line drawn at `quorum/eligible` and the fill at `cast/eligible`, "carries / fails / short of a quorum as it stands", closes at the end of Day N with a `Countdown`, three ballot buttons (replaceable until the close), the roll by name, and the floor (`Talk` on `assembly:<pid>`, mounted `embedded`, with a link to the full page); decided proposals as a ledger (day, kind, carried / failed / no quorum, the tally, and what it did: the `PolicyChanged` diff per patched field, the honor, the disbursement, each linked to its event); an Offices aside per constitution office: rule, holders through their term's last day, seats short since, and the open election with approval checkboxes (each change is a `PUT` of the new set), stand / withdraw, and `stand_refusal` in the engine's words. `screens/roles/BallotBuilder.tsx`: one form per movable kind in `proposal_kinds` (election is stood for, Q125; members' votes are the org's), policy fields gated by capability (work norm on `labor = norm`, rationing on `common_store` with the coordinator note, the Materials split in percent that must make 100), honor and recall pickers by handle, `only=` for S2.8's rationing form. `Talk.tsx`: an `assembly:` channel is titled by its proposal and links back; `embedded` drops the aside. `Plan.tsx`: Follow picks a citizen by handle. `hooks.ts`: a lexicon entry of "TODO" reads as its key (Q140). `lib/policy.ts` (diff, summaries, quorum bar) with four vitest cases. `presets/copy/commune/` placeholder Welcome Brief and Chronicle templates so a Commune can be served (Q140). `scripts/e2e/web.sh` seeds a lab Commune and mints `ISMS_SESSION_ASSEMBLY`; `web/e2e/assembly.spec.ts` joins it, stands for coordinator, waits for an early hour, moves "Seven hours" (work norm 6 → 7), votes yes, speaks on the floor, waits for the day to end, reads "carried", the diff and the `PolicyChanged` link, finds itself among the holders, reads the floor's full page, and then checks Freeport's nav has no assembly and its `/assembly` route says so.
Deviations from TDD: none. The card says "propose as a coordinator"; the Commune's `proposers = anyone` (Q116), so the spec stands and moves in the same day and the one close seats it and carries the change; the coordinator-only motion (rationing) is S2.8's.
Provisional answers added to QUESTIONS.md: Q139 (the diff's "before" is the previous `PolicyChanged` this epoch; a policy view would make the first one whole), Q140 (placeholder Commune copy rather than a server fallback).
New tunables: none.
Next session should know: S2.8 mounts `BallotBuilder` with `only={["policy_change"]}` for the rationing rule; `POLICY_FIELDS` is where a new patch field goes. The offices aside is the holder-check pattern S2.8's `roles/Coordinator.tsx` mounts on (`OfficeView.i_hold`). The Commune lexicon is still "TODO" so its nav reads `home_title`, `assembly`, etc. until S2.9; `t()` maps "TODO" to the key. The e2e Commune runs 24 one-second hours a day, so the spec waits for an hour ≤ 12 before moving (a motion at hour 23 could close before the ballot lands). No policy view exists on the wire (Q139). Chris's dev server holds `target/debug/isms-server.exe`, so the e2e here ran under `CARGO_TARGET_DIR=target/s25`.
make check: web typecheck/lint/test green; `assembly.spec.ts` green locally · new tests: 4 vitest, 1 Playwright · sim-check: n/a

## S2.7 — Common Store and Ledger of Contribution screens — 2026-09-20 — main
Built: `crates/isms-server/src/commons.rs`: `GET /s/{id}/store` (`StoreView`: the shelves in the engine's order with Food and Wares first, each with stock, this tick's requests and requesters, `my_entitlement` from `store::entitlement`, `my_pending`, the meter points a unit restores, and the share each citizen would get if the day ended now; the rule in force; yesterday and today from the log as asked / served / short / hours rationed / shared out, Q142; the caller's `Drew` events oldest first with their Explains; the pantry and its caps; 422 `NoStore` elsewhere), `GET /s/{id}/ledger` (`ContributionView`: every citizen, active first by hours today, hours exact as tick-hours over the day, output as attributed today and yesterday, the norm met today and over the record, honors, workplaces, `is_me`; the norm, the monitoring level and σ; `least_staffed` and the two labor caps for the picker; 422 `NotInThisSociety` where labor is not by norm), `POST`/`DELETE /s/{id}/workplaces/{wid}/position` (`JoinWorkplace`/`LeaveWorkplace`, Q141). `LaborView.positions` (every position held, contract or not); `ScoreRow.contribution` (hours total, days, norm met) and the Commune's ranking by hours then honors; `Citizenship.honors` on `/me`. `web/src/api/commons.ts`; `lib/draws.ts` (draw rows with the surplus share told apart by its rule; the three rules in words); `screens/Store.tsx` at `/s/$id/store` on `caps.common_store` (the shelves with what a unit does for a meter, what you may draw, the pantry and tonight's share; a bare-shelf line; today and yesterday side by side; the draw record as the compensation section); `screens/Ledger.tsx` at `/s/$id/ledger` on `caps.labor === "norm"` (the norm and σ stated in the header, ~ before every estimated figure, my line in words, everyone's rows with the away below a divider, my row marked); `Work.tsx`: positions from `LaborView.positions`, a "Take a position" picker under a norm with head counts against the cap, the least-staffed workplace named, leave on a held position, no payslips under a norm; `Home.tsx`: no balance where there is no money, a Store tile (shelf stock, my entitlement or pending draw) and the draw record in place of the payslips; `Profile.tsx` honors per citizenship; `SocietyScreen.tsx`: the Commune scoreboard (hours given, norm met, honors) where labor is by norm, an honors column on the Freeport scoreboard and the citizens list where there is governance; `DiffSinceLastSeen` narrates a `Drew`. Nav labels `t("store")` and `t("ledger")` (the Commune lexicon is S2.9's; `t()` shows the key). `scripts/e2e/web.sh` mints `ISMS_SESSION_COMMUNE`; `web/e2e/commune.spec.ts` (join, take a position, set hours, the shelves and the rule, my entitlement equal to the API's, a draw lands on the record and in the pantry, my Ledger row with hours once an hour is worked, the Commune scoreboard, and Freeport's nav and routes without either). TDD §10.3 row; `make api-types`.
Deviations from TDD: none. The card names two GETs; the position route is new (Q141) because a Commune human had no way to hold a position through the client.
Provisional answers added to QUESTIONS.md: Q141 (a route and a picker for norm positions; the `follow_norm` plan stays the householder's), Q142 (what "this tick's requests" shows at rest; the day's service from the log; hours as tick-hours over the day).
New tunables: none.
Next session should know: S2.8 (the Coordinator workspace) mounts on `OfficeView.i_hold` and reuses `BallotBuilder` `only={["policy_change"]}`; `StoreView.rule` and `ContributionView` are what S2.10's personas read (`store` and `ledger` read tools). The Commune lexicon is still "TODO", so the nav reads `store`, `ledger`, `work_screen` until S2.9; the e2e specs match those keys and must change with S2.9. `Work.tsx` now lists positions from `LaborView.positions`, not from the employment contracts, so a screen that adds a position kind needs only the server. Between ticks `CommonStore.requests` is empty (Q142): the "asked this hour" column fills only for a hand request. The Commune's `monitoring = low` is σ 0.6, so every attributed figure on the Ledger is an estimate and the screen marks it with ~; the test asserts the wire carries no `true_output` and that my attributed sum differs from the true sum. Tests ran under `CARGO_TARGET_DIR=target/s25` beside Chris's dev server.
make check: green (cargo test isms-server, clippy -D warnings, fmt, web and agents check locally; `make push` runs the rest) · new tests: 2 server (society_api.rs), 3 vitest (draws), 1 Playwright · sim-check: n/a

## S2.8 — The Coordinator workspace — 2026-09-20 — main
Built: `crates/isms-server/src/coordinator.rs`: `GET /s/{id}/plan/published` (`PublishedPlanView`, Q143: every workplace with its target beside yesterday's output and fulfilment, today's so far, workers, machines and the collective flag; every land slot and the unlimited kinds; the collective's id; the founding Materials against the Store's Materials; the last `PlanPublished` this epoch from the log as cycle, tick and citizen; `i_coordinate`; 422 `NotInThisSociety` where nobody publishes a Plan), `PUT /s/{id}/offices/coordinator/plan` (`SetPlan`, targets only), `POST /s/{id}/workplaces` (`OpenWorkplace`), `DELETE /s/{id}/workplaces/{wid}` (`CloseWorkplace`); a non-holder is the engine's `NotAnOfficeHolder` through `named`. `WorkplaceView.target` and `last_cycle_output`. `web/src/api/coordinator.ts`; `screens/roles/Coordinator.tsx` at `/s/$id/coordinator`, mounted on `OfficeView.i_hold` (Q144): the header with fellows and the term; the Plan editor (target per workplace beside working, yesterday with fulfilment, today; blank rows keep their target; the signature line); the land (per kind: open, slots free or no limit, an Open button disabled without a free slot or the Materials; the collective's workplaces with a two-step Close that states the workers who lose the hour); the rationing rule (in force and in words, `BallotBuilder` with `only={["policy_change"]}` and the new `fields={["rationing"]}`); the refusal page for a non-holder naming who sits and linking to the assembly. `Society.tsx`: the nav item `t("office")` when the offices list me as a holder. `SocietyScreen.tsx`: an Offices tile per office (holders through their day, an open election with its candidate count, "your workspace" for a holder). `Work.tsx`: the Plan's target beside a held position and a "The Plan asks" column on the picker, from `WorkplaceView.target`. `scripts/e2e/web.sh` seeds a third lab Commune (`coordinator-*`) and mints `ISMS_SESSION_COORDINATOR`; `web/e2e/coordinator.spec.ts` (the refusal page and the API's own refusal by name, stand, seated at the close, the nav item and the Offices tile, publish a target and read it signed, open a workshop and close it, the rationing builder offering that field alone and the motion before the assembly, the target on the Work screen). TDD §10.3 row; `make api-types`.
Deviations from TDD: none. The card's four routes as named; `WorkplaceView` gained two fields rather than a route for the Work screen's target.
Provisional answers added to QUESTIONS.md: Q143 (what the published-plan view carries and where it exists), Q144 (the holder-check mounting pattern, the `office` label, the refusal page, the in-page close confirmation).
New tunables: none.
Next session should know: `roles/Coordinator.tsx` is the holder-check pattern: the route always exists, `useOffices` decides the nav item and the page, the API refuses by itself. The nav label is `t("office")`, one of S2.9's shared keys, so it reads `office` until the Commune lexicon lands and the e2e matches that literal. The Playwright spec asserts no exact Materials delta after opening a workplace: with one-second ticks the foundries add Materials between reads (the server test asserts the exact 20 under a held clock). `BallotBuilder` takes `fields` as well as `only`. The Offices tile on the Society screen reads `/offices`, which is a citizen route: a non-citizen sees no tile. S2.10's `set_plan`, `open_workplace` and `close_workplace` tools have their routes; `plan/published` is their read. Tests ran under `CARGO_TARGET_DIR=target/s25` beside Chris's dev server.
make check: green (cargo test isms-server, clippy -D warnings, fmt, web typecheck/lint/test locally; `make push` runs the rest) · new tests: 2 server (society_api.rs), 4 vitest (roles.test.ts), 1 Playwright · sim-check: n/a

## S2.9 — The Commune's voice: lexicon, Welcome Brief, Chronicle — 2026-09-21 — main
Built: `presets/lexicon/KEYS.txt` gains the seven shared keys (`assembly`, `proposal`, `ballot`, `office`, `honor`, `store_screen`, `ledger_screen`) and all five lexicons are real copy (GDD §15's four rows verbatim; the Commune's nav reads Hours, Common Store, Ledger of Contribution, Assembly, Coordination; the Directorate has a Bulletin and Commendations, the Commonwealth Merits). `presets/copy/commune/welcome.md` with `{{open_slots}}`, `{{norm_hours}}`, `{{rationing}}`; `render_welcome` fills the two new ones from the policy in force, the rule in the copy's own words (`[words.rationing]`, Q146). `presets/copy/commune/chronicle.toml`: every governance event of S2.1–S2.4 in the Commune's voice, the three `ProposalClosed` outcomes, one line per moved `PolicyChanged` field (the rule and the watch by value), `OfficeUnfilled` as "The Commune has no coordinators, or too few", the night lines (`stock_out` per bare shelf, `need_met` / `need_short` against `need_met_pct`, hunger). Freeport gains the admission-vote lines only. `chronicle.rs`: variant keys `Kind.path=value`; a derived `ProposalClosed.outcome`; the policy in force learned from `SocietyCreated` and diffed at each `PolicyChanged`; names for workplaces (kind) and proposals (title, or "the admission of X to Y"); `{{workplace:}}`, `{{proposal:}}`, `{{pct:}}` fills; list indexing in a path; `Templates::word`; `check_copy` called by `seed_society` before the row exists (Q147 for the `test/` fixture exemption). Web: nav and headings on `store_screen` / `ledger_screen`, `t("proposal")`, `t("ballot")`, `t("honor")`; the three Commune Playwright specs match the words. `docs/tuning/neutrality-01-commune-copy.md`, the copy read against GDD Appendix B.
Deviations from TDD: none. The card's `ProposalClosed` "passed/failed variants" became three (a lapse for want of a quorum is neither).
Provisional answers added to QUESTIONS.md: Q145 (which governance events headline; ballots do not; no householder filter on governance), Q146 (empty placeholders; the rule's words live in the copy; constitution constants written literally), Q147 (`store` versus `store_screen`; the columns and the specs; the `test/` exemption).
New tunables: none in `presets/*.toml`. `need_met_pct` is a threshold of the copy (`chronicle.toml`), as `price_move_pct` was.
Next session should know: S2.10's personas read the Commune's nav by the lexicon's words if they scrape the client, but the API keys are unchanged. A new governance event needs a template in `commune.toml`'s `[templates]` and a fixture in `chronicle_tests.rs`, which asserts the exact line for every one. `PolicyChanged` renders nothing until the projector has seen `SocietyCreated`, which a replay always has first. `OrgFounded` in the Commune reads "an {{kind}} of members" and is right only for associations, the one kind a citizen may found there. `Profile.tsx` still says "Honors" literally (no lexicon hook on that screen). The S1.5 `unemployed` cycle line still never fires (Q145). Tests ran under `CARGO_TARGET_DIR=target/s25` beside Chris's dev server.
make check: green (cargo test isms-server and isms-core, clippy -D warnings, fmt, web typecheck/lint/test locally; `make push` runs the rest) · new tests: 6 server unit (chronicle_tests.rs), 1 server unit (api.rs render_welcome), 0 Playwright (three specs updated) · sim-check: n/a

## S2.10 — Commune personas and tools for the agents harness — 2026-09-21 — main
Built: `agents/src/society.ts` (`SocietyFacts` from `/societies/{id}/capabilities` and `/societies/{id}`, read once per run through an unrecorded client and handed to every brain; `FREEPORT` when absent, so the replay test and every old call site are unchanged). Read tools `proposals`, `proposal`, `offices`, `store`, `ledger`, `published_plan`; act tools `propose` (resolution, policy_change patch, honor, recall; an election kind is offered so the prober can move one), `vote`, `approve`, `stand`, `withdraw_candidacy`, `publish_plan`, `open_workplace`, `close_workplace`, `post_floor`, `take_position`, `leave_position`; `set_plan` gains `vote_default` (Q148). `script.ts`: `workTheNorm` (a position at `least_staffed` when none is held, then the hours), `voteOnOpen`, `normOf`, `electionFor`, the five governance reads. `strategies.ts`: `steward`, `rationer`, `free-rider`, `chronicler` (Q149), each a householder where there is no assembly. `fuzz.ts`: a probe may say where it `applies` and may `check` an accepted call; fourteen governance probes after the Freeport list (Q150); the prober works the norm under `labor = norm`; `p` joins the raw-id regex. `prompts.ts`: `rulesFor(facts)` (the Commune's first paragraph, the Store draw, the position, the assembly line); the LLM brain takes `facts`. `situation.ts`: contract-less positions count as jobs and are named for the model. `report.ts`: a `## Governance` paragraph (accepted / refused per governance tool, by whom; the motions moved) and a governance row in `ENDPOINT_WORDS`. `main.ts --preset`, `config.toml run.personas_for.commune`, `scripts/agents/run.sh PRESET`, `make e2e-agents` runs Freeport then the Commune, `make agents PRESET=`. Four persona files. `test/commune.test.ts` (19 cases) over `test/fixtures/commune/` recorded from a lab Commune (two motions, a candidacy and its approval, the Store, the Ledger, the Plan, the exact refusal each probe meets under `refusals/`). `agents/README.md`.
Deviations from TDD: the card's `set_plan` is `publish_plan` (the name was taken by the standing plan); `approve`, `take_position` and `leave_position` are beyond the card's list because no persona could seat a coordinator or work without them (Q148). "Vote twice" is a checked acceptance, not an expected refusal (Q150).
Provisional answers added to QUESTIONS.md: Q148 (tool names beyond the card), Q149 (what the four personas do), Q150 (the two probes the card names that the API cannot refuse as written).
New tunables: none.
Next session should know: the Commune gate ran green locally (`PRESET=commune`, eight scripted players, two one-second days: 344 turns, 0 skipped; 8 motions moved, 58 ballots, 3 candidacies, 2 Plans published, 2 workplaces opened, 16 floor posts; every probe refused; rollover into epoch 2; `rebuild` clean), then Freeport. Proposal ids are 0-based on the wire (the first motion is `#0`). Two copy findings from the refusals, not fixed here: "You already stands for Coordinator" (`names.rs` puts "You" where the verb wants a third person) and the JSON rejection for an unknown patch field is a bare string, not a problem document (`propose` with `tax_rate` answers 422 with `Failed to deserialize the JSON body ...`). The stewards seat themselves at the first close (three seats, two candidates) and publish from day 2; the rationer never moved in the run because no shelf ran short in two days, so its motion is covered by the unit test alone. Under one-second ticks the eight players all take every hour (0 skipped) because the scripted turns are quick. The S2.9 commit (`0367465`, lexicon and copy) is still on branch `s2.9-commune-voice`: `make push` refuses it because `presets/` counts as an engine change, and the PR cycle this session started from `../Isms-s29` was stopped by the machine for low memory during `make check`, before any PR was opened; S2.10 is branched on top of it (`s2.10-commune-agents`) and lands after it. Tests ran under `CARGO_TARGET_DIR=target/s25`.
make check: agents typecheck, oxlint and vitest green (77 tests); `make e2e-agents` green for both presets locally · new tests: 19 vitest (commune.test.ts), 1 updated (persona.test.ts) · sim-check: n/a

## SB.1 — The Companion foundation: tokens, fonts, theme, primitives, Gallery, guard — 2026-09-22 — main
Built: the style bible moved from `reference/style-bible/` to `docs/style.md` + `docs/style/{reference.html,tokens.css,tailwind.preset.js}` (its appendix rewritten for Tailwind v4); ADR-0013; `docs/plans/style-refresh.md`; SB.1–SB.5 cards in TDD §18.5 ahead of S2.11 and the §11 pointer. `web/src/styles/tokens.css` in three blocks — `@theme static` for the invariant values under Tailwind's names, the bible's colour tokens on `:root` with the two dark selectors, `@theme inline` mapping `--color-*` onto `var(--token)` with the stock palette removed and a fenced `LEGACY` alias block (`paper`, `paper-2`, `ink-2`, `accent-2`, `warn`, `bad`). `components.css` for the four pseudo-element rules. `@fontsource/outfit` 500/700 and `@fontsource/atkinson-hyperlegible` 400/700/400-italic imported in `main.tsx`; a pre-paint theme script in `index.html`; `lib/theme.ts` (`useTheme`, `setTheme`, `isDark`, `onThemeChange`); `ThemeControl` on Profile. The catalog: `Card`/`Tile`/`Stack`/`Two`, `PageHeader`/`FooterStrip`, `Verdict`/`State`/`verdictText`, `NeedCard`/`Needs`, `FactList`, `Pill`, `Explain`/`Why`/`Note`, `More`, `Button`/`ButtonRow`, `Field`/`Input`/`Select`/`BareInput`, `Feed`/`RuleList`, `Sheet`/`SheetRow`, `Toast`/`toast`/`Toaster` (mounted in the root route), `Icon` (25 names). `Meter` gains `meterTone` and the §4.2 fills, a `tone` override and `bare`; `Num` uses the shared `Why` and keeps `role="dialog"`; `Ledger` exports `TH`/`TH_NUM`/`TD`/`TD_NUM`; `Countdown` and `WorldClock` hold a fixed-width value under `aria-live="off"`; `OrderBook` in the good/crit fills; `TimeSeries` reads `--chart-*`, `--chart-grid`, `--muted` at mount and on theme change and draws a `you` series in ink. The root wrapper is `page @container max-w-page-wide px-gutter`. `Gallery.tsx` → `screens/gallery/catalog.tsx`: eleven sections (§7 with tokens, type and icons) at Phone/Tablet/Desktop/Fit, in the page's theme or both side by side. `scripts/style-guard.mjs` in `pnpm check`: a hex, `rgb()`/`hsl()`, `dark:`, or a legacy class name outside the allowlist fails the check; the allowlist names the 26 files still to restyle.
Deviations from TDD: none from the TDD. From the bible: dark is `[data-theme="dark"]`, not `:root[data-theme="dark"]` (ADR-0013 §5); `tailwind.preset.js` is not loaded (§4).
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: no screen changed this card; the catalog is only on `/gallery` and Profile's Appearance section. `login.spec.ts` clicks the first "Explain" button on the gallery and expects a dialog, so the Num section stays first and the generic `Explain` component's default label must not precede it. Container queries (`@min-[620px]:`, `@min-[720px]:`) key on the root wrapper's `@container`; a screen laid out with `md:` keys on the viewport (§3). `Button` defaults to `type="button"` — pass `type="submit"` in a form. `Field` wraps its control in the label, so `getByLabel` works without ids; a `Field` has `max-w-xs`. `--color-*: initial` removed `text-white`: use `text-accent-on`. An old Vite from a previous session was holding `[::1]:5173`; Playwright still ran on `127.0.0.1:5173`. The Chrome extension was not connected, so the Gallery review was Playwright screenshots at 390/720/1100 in both `colorScheme`s. `make e2e-web` ran with `CARGO_TARGET_DIR=target/e2e`.
make check: web typecheck, oxlint (warnings only, the pre-existing class), style-guard, vitest green; `make e2e-web` 13 passed · new tests: 15 vitest (ui.test.tsx), 4 vitest (theme.test.ts), 1 updated (Meter) · sim-check: n/a

## SB.2 — Shell and Home — 2026-09-22 — main
Built: `web/src/components/Nav.tsx`: `TopBar` (society in the display face, preset chip from `sm`, the balance where `capabilities.money` and the caller is a citizen — `header-balance` kept —, the full `WorldClock` from `md` and a `phone` variant of it below, the tick bar and the countdown alone; the account links from `md`), `ScreenNav` (`nav[aria-label="Sections"]`, current item in the accent-soft pill, hidden below `md`), `TabBar` (fixed bottom bar below `md`, the four items marked `tab` with icon + 11px label and a More button that opens a `Sheet` — the rest of the screens, the account links, `ThemeControl`; the More slot lights when the current screen lives inside it). Every item comes in as a `NavItem` from `Society.tsx`, which marks Home, the hours, the market or the Store, and Society as tabs. `router.tsx`: the root is `page @container` alone; a pathless `frame` route carries the gutter and the wide max for every non-society route, so the shell's bars run full width. `lib/verdict.ts`: `homeVerdict` (goods fed / housed / working, one "but" ranked hardship > food under the line > shelter under the line unhoused > food under 50 > no dwelling > no hours (hired or not) > comfort), `needTone`, `greeting` by the society's hour. `lib/needs.ts`: `needStatus`, the status line under each meter (direction and cause; the rates are preset parameters the client does not carry) and the unhoused-Shelter attn override. `Home.tsx` per §10: `PageHeader` greeting + the two countdowns; epoch notices as a pill and a status line (`epoch-ended`, `epoch-ending` kept); the first card titled `t("home_title")` with a hardship `Pill`, the `Verdict`, three `NeedCard`s (status lines, the S1 hints as their notes), a `FactList` (Balance, the Store tile, Pantry with the one gloss "about N hours", Dwelling as a pill + gloss, Work with "adjust") and "More about today's work" (hour budget and fatigue debt, today's output multiplier, the three effort levels' multipliers, workplaces at once — layer 4, new on Home from `LaborView`); a `Two` of While you were away and the compensation card (`payslips` / `draws` kept); the plan as a `RuleList` with Keep my plan (primary) and Edit plan (`ButtonLink`, new in `Button.tsx`); the Chronicle as a `Feed` (hot by text, Q151); the `FooterStrip`. `Icon` gains `globe`. The Gallery's first section is the shell. `style-guard` allowlist minus `Society.tsx` and `Home.tsx`. `e2e/review-home.spec.ts`: skipped unless `REVIEW_OUT` is set; then it joins a society and screenshots Home at 390/720/1100 in both themes, the More sheet and the open detail, and prints the horizontal overflow at 390.
Deviations from TDD: none. From the bible: the first card's title is the lexicon's `home_title` ("Your Accounts", "Your Household & the Store"), not "Right now": GDD §15's row stays on the screen and six Playwright specs recognise Home by that heading; the h1 is the greeting the bible asks for. NeedCard notes open inline at every width (the bible's phone Sheet for long notes is not built).
Provisional answers added to QUESTIONS.md: Q151 (what makes a headline hot when the wire carries only its text).
New tunables: none.
Next session should know: the `frame` layout route changes the route ids of everything outside the society — `useParams({ from: "/frame/public/s/$id" })`. Both navs are labelled "Sections"; Playwright's role queries skip the display:none one, so a spec at the default 1280 viewport sees the `ScreenNav` and a 390 viewport the `TabBar`. `NavItem` is `Pick<LinkProps, "to" | "params">`, so `to` is typed against the router. The shell's `<main>` is `max-w-page-wide`; Home narrows itself to `max-w-page`, so a dense screen (SB.3–SB.5) needs nothing and a single-column one wraps itself. The `Sheet` closes on a link click by `onClose`, and the dialog element's `onClose` fires on Escape and the backdrop. Screenshots were taken with the review spec (`REVIEW_OUT=<dir> CARGO_TARGET_DIR=target/e2e bash scripts/e2e/web.sh e2e/review-home.spec.ts`), not the Chrome extension; `fullPage` captures paint the fixed TabBar mid-page, which is the capture, not the page. Chris's dev server was not running; the e2e run built under `target/e2e`.
make check: web typecheck, oxlint (pre-existing warnings only), style-guard, vitest green; `make e2e-web` 13 passed · new tests: 12 vitest (verdict.test.ts) · sim-check: n/a

## SB.3 — Work, Standing plan, Market — 2026-09-22 — main
Built: `lib/verdict.ts` gains `workVerdict` ("You're working 8 of 8 hours at Legacy Farm No. 1 at normal effort", fatigue the one but; a hired citizen is told to set hours, an unhired one where the board is, a norm citizen to take a position below), `planVerdict` ("Your plan keeps you fed and spends everything else. It runs every hour, here or not"; every rule that is set in the list, in the Store's words without money; no Food floor is the one but, in attn) and `marketVerdict` ("Food costs 1.31 cr and hasn't moved in 3 days. Your plan will bid for 2 food next hour"; a never-traded book, a share, up/down over the window, sold-out or no-asks as the one but, the open-order count); `VerdictPart` takes `ink` for emphasis with no state (a price is not a state, §4.2). Two catalog primitives: `Tabs` (a tablist below md, every panel laid out at once under caps headings from md, every panel in the DOM at every width) and `Segmented` (a radiogroup of levels, the chosen one in the accent-soft pill); both in the Gallery. `Button` gains `inline` for a button in a table row. `Provenance` on the kit (`TH`/`TD`, `text-attn`) with `bare` so a Card carries its title. Work per §10: `PageHeader` with the budget and fatigue debt; "Your hours" with the Verdict, a glance tile (hours used as a display figure over the budget, a bare `Meter` in attn/good, a status line, the cost line of each effort in use), the editor as a `Tile` per position (`allocation-editor`, `Hours at …`, `target-N`, the norm's `leave`; effort as `Segmented` labelled `Effort at …` with the cost line from the preset under it — `output ×1.0 · Food decay ×1.0`), "Set my hours" primary with the over-budget reason as `disabledReason`, and "How output is worked out" as `More` (budget, output today, the three efforts, the high-effort debt rule, workplaces at once); the norm picker as a Card (`positions`, `workplace-picker`, `pick-N`, "The Plan asks", "Taken: the …"); `Two` of compensation (`payslips` / the Ledger note) and Skill (`skills`); the `FooterStrip`. Standing plan per §10: "Your plan" with the Verdict, the whole plan as a `RuleList` (labor, Food floor with its ceiling, the Wares rule or its absence, the balance floor, one line per standing order with a blocked reason for a 0 quantity or limit, the vote default) reading the draft so an edit reads back before it is saved, and an "unsaved changes" pill; one card per rule (`plan-labor`, `plan-consumption` with Eating and Wares as tiles, `plan-saving`, `plan-orders` with a `Tile` per order and `Field`s in place of the six-column table, `plan-vote`) on `Field`/`Input`/`Select`, `MoneyField` on `Field` with `cr` inside the box; the Save card with the one primary, Discard, the engine's text under `role="alert"`, and "What the plan did since you last looked" as `More` (Q152); the `FooterStrip`. Market per §10 on the wide page: the price index in the header meta; the Instruments card (goods and shares as one `ul` each — chips in a horizontal scroll row below md, rows with last and 3-day change from md; `instruments`, `share-instruments`; "All shares" with book values and holdings as `More`) beside a `Stack`: the instrument card titled by name with the 3-day change as a neutral/info `Pill` (never good/crit), the Verdict, three glance tiles (last price, spread, your pantry or holding against the cap), `Tabs` of Book (the two ladders in `ladders` with the "you N" pill on your levels, plus the depth SVG), Tape (`tape`, 10 rows, the rest under "Earlier trades") and the 3-day chart; the sold-out note; `Two` of "Place an order" (`order-form`, radios, `Quantity` with the good as its unit and the room as hint, `Limit` in cr, `escrow-preview`, "Place bid" primary, the plan's Food/Wares rule beneath) and "Your open orders" (`my-orders` as a `Tile` per order — five columns is card-per-row by §3, and it was the one thing that made the page scroll sideways at 390 — with a quiet cancel; `pantry-line`); "Where it comes from" as a Card around `Provenance`; the `FooterStrip`. `style-guard` allowlist minus `Work.tsx`, `Plan.tsx`, `Market.tsx`, `Provenance.tsx`. `e2e/review-sb3.spec.ts`: skipped unless `REVIEW_OUT` is set; then it joins, rests a bid, and screenshots the three screens at 390/720/1100 in both themes (the Tape tab and the open detail at 390), printing each screen's horizontal overflow at 390 and, when there is any, the elements that stick out. Specs changed in the same commit (ADR-0013 §6): `work-plan` selects effort through the radiogroup and reads the cost line with `×` and `·`; `market` finds the instrument heading at level 2 (a Card title).
Deviations from TDD: none. From the bible and the card: the engine's rejection on Work sits in the button row under `role="alert"` in crit, not in a `Field` error, since it is about the whole allocation and not one field; the Standing plan has no "last 24 executions" because the wire has no log of the plan's runs (Q152) — the digest's trades and draws stand in, labelled as both the plan's and your own; the Market's instrument chips are the one horizontal scroll the bible permits besides the book, inside their own box; `Tabs` shows every panel from md rather than keeping tabs at every width, since the bible's tabs are a phone measure.
Provisional answers added to QUESTIONS.md: Q152 (what the plan's detail layer shows with no execution log on the wire).
New tunables: none.
Next session should know: a grid column defaults to `minmax(auto, 1fr)`, so a chip row with `overflow-x-auto` inside a grid item still widens the page unless the column is `minmax(0,1fr)` and the item `min-w-0`; and a table's min-content widens every sibling card in the same `Stack` column, so the review spec's "which element sticks out" report squeezes the Stack and measures min-content per element. The Market keeps its `instruments`/`share-instruments` testids on `ul`s (chips below md, rows from md, one DOM); the instrument heading is a Card `h2`. `Tabs` keeps every panel in the DOM at every width (`hidden md:block`), so testids inside the inactive tabs still resolve. Editing a screen while Playwright is running is a false green: Vite hot-reloads the screen but the spec file was loaded at start; rerun. The review screenshots are under `target/review-sb3/` (`REVIEW_OUT=../target/review-sb3 CARGO_TARGET_DIR=target/e2e bash scripts/e2e/web.sh e2e/review-sb3.spec.ts`). Chris's dev server was not running.
make check: web typecheck, oxlint (pre-existing warnings only), style-guard, vitest green; `make e2e-web` 13 passed; overflow at 390 is 0 on all three screens · new tests: 10 vitest (verdict.test.ts: workVerdict, planVerdict, marketVerdict) · sim-check: n/a

## SB.4 — Organizations, Org, Contracts, Society, Archive, Talk, Event — 2026-09-22 — main
Built: `lib/verdict.ts` gains `orgsVerdict` ("18 firms are hiring; you work at Legacy Farm No. 1", the firms you manage beside them, no work as the one but pointing at the board or the founding form, the norm citizen sent to Work), `orgVerdict` ("Iron & Sons is yours: you hold 100 % and manage it, but the treasury is short by 68.00 cr for tonight's payroll", read from the owner's, manager's, holder's, employee's, member's or stranger's side; a shortfall or a missed payday in crit as the one but, a covered payroll in good), `contractsVerdict` ("You have no dwelling and nothing to let is on the board", crit under the line, the dwellings to let counted, the running contracts, an overdue payment as the but), `societyVerdict` ("Freeport is fed (98 %), one citizen is in hardship, prices are steady"; `fedTone` by the provisional thresholds of Q153, hardship crit at a tenth of the population, prices against yesterday's close, no prices without money, a first-day sentence before any figure exists) and `archiveVerdict` ("Epoch 1 ran its course after 41 days; epoch 2 is on Day 3", collapse in crit, the open statement window). Catalog: `Figure`/`Figures` (a glance tile — caps label, display value with its unit, a status line in the figure's tone — two-up on a phone, four across from 620px; in the Gallery under Card) and, moved out of `SocietyScreen.tsx` per the card, `components/StatTiles.tsx` (four Figures — need fulfillment, in hardship, price index, median wellbeing — the rest as a `FactList`, the Gini and the definitions under "About these numbers") and `components/ChronicleReader.tsx` (earlier / Day N / later as quiet buttons, the headlines as a `Feed`, hot by text as Home does, with `collapse` and `missed` added). `Stack` and `Two` take a `testId`; `Num` exports `showValue`. Organizations per §10: `PageHeader` with the counts; "Where you stand" with the Verdict and two tiles (`my-positions` linking each employer, `my-orgs` kept); `Two` of the job board (`job-board` as a `Tile` per posting — firm, workplace, pay in the display face, the terms line, "Take it" full width below sm or the "you work here" pill; `job-taken` kept) and "Found one" (`found-form` on `Field`/`Input`/`Select`, the radio fieldset with scarcity, `found-cost` as a `Tile` with the short figure in crit, "Found it" as a danger button with the reason it is disabled beside it); "Every organization" (`all-orgs`, five columns from md, Kind and Workplaces folded under the name below md; a missed payday as a crit `Pill`); the `FooterStrip`. Org per §10: the header's meta keeps `you hold 100% (controlling)` and the manager; "The firm" with the Verdict, a crit `Pill` and the missed-payday line, three `Figure`s (Treasury with the payroll as its status line, Staff, Your stake or Members) and the overview `FactList` (`org-overview`, `declared-dividend`); `Two` of Ownership (`share-registry`, the "you" pill, and a sentence when every share sits in escrow — the legacy firms' registers were blank) and your contract (`my-contract` as a `FactList`) or the open offers (`org-offers`, a sale or lease of the firm's in a sentence rather than JSON); Production (a `Tile` per workplace — `workplace-N`, the three-column workers table with `worker-N` rows and the "you" pill — the `payroll` tile with the top-up as a `Field` and "cover the shortfall" as a quiet button, the monitoring line); Dwellings (`org-dwellings`, a `Tile` per dwelling with "Let it" / "Sell it", the two term fields); the manager workspace as a `Two` (`manager-workspace`) of "Post a job offer" (`job-offer-form`; `Pay` exact by aria-label, the basis as its own field) and a `Stack` of "Sell from inventory" (`ask-form`, `ask-held`, "Max", `ask-book-hint` as the Limit's hint), Machines (`machines`, the treasury bid under a dashed rule) and "Add a workplace" (`add-workplace`); "Owner's tools" (`owner-tools`) as four tiles; the treasury ledger (`treasury-ledger`); the `FooterStrip`. Contracts per §10: `housing` with the Verdict, the Shelter `NeedCard` (its bare meter keeps `role="meter"` named Shelter) beside a tile holding `my-dwelling` with "End the lease" / "Move out" as danger buttons, or the no-dwelling line and the `leases` list with "Rent it"; `Two` of the notice board (`ads` as a `Tile` per ad — kind, poster with the "you" pill, terms, Buy / Borrow / withdraw) and "Your contracts" (`contracts` as a `Tile` per contract — `contract-N`, status as a `Pill`, crit and "overdue" when an installment or rent is missed, "End it" as a small danger button); `Two` of "Offer a loan" (`credit-form` on `Field`s, the collateral fields by kind, `schedule` as a `Tile`, "Offer loan" primary) and "Post to the board" with the sale, wanted, lease and transfer forms each under a `More` (`sale-more` … `transfer-more`; the forms' testids kept); the `FooterStrip`. Society per §10: `PageHeader` with the public-view link; "How Freeport is doing" with the Verdict and `StatTiles` (`stat-tiles` kept); the offices as tiles (`offices-tile`, `office-tile-<kind>`, "Your workspace" as a `ButtonLink`); `Two` of the Chronicle (`chronicle`) and the scoreboard (`scoreboard`, your row pinned first with the "you" pill, Firms folded under the name below md); Citizens (`citizens`, Kind and Since folded below md, flags as pills, the "you" pill) with the householders' script under a `More` (`householder-script` is always in the DOM now); the `FooterStrip` from the live stats. Archive per §10: "The record" with the Verdict and the epoch-ending line; a `Card` per epoch (`archive-N`, the ending as its subtitle, "statements open" as an info pill) with `archive-summary` as four `Figure`s and a `FactList`, `archive-standings` at three columns (the rank folded into the name), the closing statements with the handle in bold, the statement box on the kit; the public page shares the list. Talk per §10: no Verdict; `PageHeader` titled by the channel with a "Channels" button below md that opens a `Sheet` (`channel-sheet`) of the channels and the direct-message form; from md the channel list (the current one in the accent-soft pill) and the Direct card sit in a 14rem column beside the thread card (`thread`, `messages`, the composer with "Say it" primary and the Enter hint); embedded stays bare for the Assembly. Event: `PageHeader` with the kind and `#seq`, "Why" (`event-explains`) as a `FactList` of rules with each result as a `Num` that opens its inputs, "As recorded" (`event-payload`) wrapped rather than scrolled. `style-guard` allowlist minus the seven screens. `e2e/review-sb4.spec.ts`: skipped unless `REVIEW_OUT` is set; then it joins, says a word on the Square, and screenshots the seven screens at 390/720/1100 in both themes (the channel sheet and an open `More` at 390), printing each screen's horizontal overflow at 390. Specs changed in the same commit (ADR-0013 §6): `society` finds the event heading at level 1 (a `PageHeader`); `contracts` finds the loan among `[data-testid^="contract-"]` tiles, not table rows.
Deviations from TDD: none. From the bible and the card: the loan form is a visible card rather than a `More`, since it is the one contract a newcomer is told to make and the `contracts` spec fills it; the other four forms are the disclosures. The job board's terms line stays visible at every width rather than folding into a per-row `More`: it is one short line and nothing is hidden. Org's "Open offers" is its own card, or takes the contract card's place when you have no contract there. The scoreboard pins your row first and then the ranked top twenty, so a row of yours outside the twenty appears once, unranked. The notice board and your contracts are tiles at every width (four columns is card-per-row by §3), as SB.3 did for the open orders.
Provisional answers added to QUESTIONS.md: Q153 (what makes a society good, attn or crit when the engine has no threshold for one).
New tunables: none.
Next session should know: `StatTiles` and `ChronicleReader` now live in `components/`; `Public.tsx` (SB.5) imports them from there and is otherwise untouched. `Figure` is the glance tile for a society-wide or firm-wide number; `NeedCard` stays the tile for a need. A `Field` wraps its control in the label, so where a spec needs `getByLabel("Pay", { exact: true })` the input carries `aria-label="Pay"` and a sibling control in the same row is its own `Field`. `Two` and `Stack` take `testId`; `getByTestId("manager-workspace").locator("..")` in the orgs spec is the Org page's `Stack`, which also holds Production. Talk's direct-message form renders twice (sidebar and sheet) with two datalist ids. The review screenshots are under `target/review-sb4/` (`REVIEW_OUT=../target/review-sb4 CARGO_TARGET_DIR=target/e2e bash scripts/e2e/web.sh e2e/review-sb4.spec.ts`); a heredoc with an apostrophe inside `$(...)`-free text still broke the Git Bash `cat <<'EOF'` append, so the entry was written from a file. Chris's dev server was running on 8080 with no Vite; the e2e run built under `target/e2e` and served on 18080.
make check: web typecheck, oxlint (pre-existing warnings only), style-guard, vitest green; `make e2e-web` 13 passed, then the orgs/contracts/society/assembly/admin specs rerun after the last edits; overflow at 390 is 0 on all seven screens · new tests: 15 vitest (verdict.test.ts: orgsVerdict, orgVerdict, contractsVerdict, societyVerdict, archiveVerdict) · sim-check: n/a

## SB.5 — Assembly, BallotBuilder, Coordinator, Store, Ledger; Profile, Societies, Public, Login, Admin, Onboarding; the sweep — 2026-09-22 — main
Built: `lib/verdict.ts` gains the four verdicts the bible's §10 had no card for (Q154): `assemblyVerdict` ("2 proposals are before the assembly tonight, but you haven't cast on one of them yet"; the office you hold and your ballots cast in good; the one but is a ballot owed, then an election you are not in, then an empty seat), `coordinatorVerdict` ("You sit as coordinator through Day 12, and the Plan is published (2 of 3 targets met yesterday)"; the but is no Plan yet, then the Store short of Materials, then no free slot), `storeVerdict` ("The shelves are stocked, and you may draw 3 Food this hour; your plan asks at the next"; a bare shelf in crit) and `ledgerVerdict` ("You have given 4 of the norm's 6 hours today at the workshop. Over 2 days on the record you met the norm once."; no position in attn, with where labor is scarcest). Assembly: `PageHeader` with the quorum rule as its meta (`assembly-rule`); "Before the assembly" (`open-proposals`) holds the Verdict and a `Tile` per motion (`article`, `proposal-N`; the kind as a `Pill`, the tally, the quorum `Meter`, "Carries as it stands" / "Fails as it stands" / "Short of a quorum" toned good / crit / attn with the countdown, the three ballot buttons in a named group with `aria-pressed`, the roll, "Open the floor" as a quiet button with the embedded Talk); `Two` of "Move a proposal" (`move-proposal`, the builder) and "Offices" (`offices`, a `Tile` per office — `office-<kind>`, "you hold this" as a good pill, `holders`, the seats short in attn, `election` as a surface-2 block with an "election open" pill, the approval checkboxes at touch height, "Stand for <office>" primary / "Withdraw" secondary); "Decided this epoch" (`closed-proposals`, `closed-ledger` at Proposal · Outcome · Did on a phone with the close day and tally folded under the title, `outcome` and `effect` once each). BallotBuilder on `Field` / `Input` / `Select`: every control keeps its own `aria-label` so the specs' exact names hold; the policy fields are touch-height checkbox rows with the bound control beneath on `Field` (hours with its unit inside the box, the split as three `%` fields and the sum in crit until it makes 100); "Move it" primary. Coordinator: `PageHeader` with the term as meta (`fellows`); "Where you stand" with the Verdict and four `Figure`s (Materials in the Store, collective workplaces, targets set, working now); "The Plan" as a `Card` (`plan-editor`, `plan-signature` as the aside, Working and Yesterday folded under the workplace below md, the target as a `BareInput` named "Target at …", "Publish the Plan" primary with `publish-plan`); "The land" (`land`, `materials` as a line with the cost and the holding in ink or attn, `slots` with `land-<kind>` rows and "Open a <kind>" as an inline button, Open and Slots folded under the kind below md (the four columns were the one thing 4 px too wide for a phone), `collective-workplaces` as a `Tile` per `workplace-N` with "Close" then "Close it: N workers lose this hour" in danger and "keep it" quiet, `land-note` in good); "The rationing rule" (`rationing`, `rule-in-force` as an info pill, the rule's sentence as the subtitle, the builder limited to the rule); the refusal page (`not-a-coordinator`) as a card. Store: `PageHeader` with the rule as meta (`store-rule`); "The shelves" with the Verdict, `bare-shelf`, a `Figure` per shelf a household draws from (crit at zero, attn when more is asked than is there) and the `shelves` table (`shelf-<good>`, `stock` and `entitlement` once each; asked, pantry and tonight's share folded under the good below md); `Two` of Today (`store-today`) and Yesterday (`store-yesterday`), four columns on a phone with rationing and the share-out folded; the draw record (`draw-record`) on `Ledger`. Ledger: `PageHeader` with the norm and the monitoring as meta (`ledger-rule`); "Your line" with the Verdict in `my-line` and four `Figure`s, "Adjust your hours" / "Take a position" as a `ButtonLink`; "Everyone" (`ledger`, `met-today` as the aside; `ledger-row-me` with the "you" pill; Citizen · Hours today · Output today at every width, the rest folded under the name; the away rows under a caps divider). Profile: `PageHeader` with the email and the account links; Appearance, Biography (`Field` + "Save" primary, "Kept."), Societies (`citizenships` at Society · Honors on a phone, the handle and the action share folded), API keys (the notice as the subtitle, the table, the form under `More` "Create a key" — `new-key-more` — and the minted key as a `Tile`). Societies: a `Card` per society in `society-list` (the name as the title link, the preset name and the lab pill aside, the clock and the people as the subtitle, the countdown and Join or your handle); signed out, one card with the sentence about the game, the public pages, and "Sign in" as a primary link. Public: `PageHeader`s; the list in a card (`public-society-list`); the society page as "The numbers it keeps" (`StatTiles`) and `Two` of the Chronicle and "To act here"; the two lexicon keys a reader cannot fetch fall back to shell words (Q155). Login: one card on `Field`s, "Send me a link" primary; "Check your mail" the same way. Admin: a `Card` per society (`admin-society-N`; the state as a `Pill` `admin-state`; the clock and the people as a `FactList` with `admin-clock`; the tick length as a `Field` with "Set"; `admin-note` in good; Pause / Step one tick / Resume / Start epoch N / end epoch → "Yes, end it" in danger as a `ButtonRow`). Onboarding: each step a `PageHeader` and one card with one primary button (Handle on `Field` with the consent row at touch height, "Enter"; the Welcome Brief at text-lg with "Find work"; the board as a `Tile` per offer — `job-board` — with the pay in the display face and "Take this job", "Skip for now" quiet; the plan's defaults as a `FactList`, "Keep these and go home"). The sweep: `style-guard` loses its allowlist and the `legacyOk` escape — every file is held to the same rule; `tokens.css` loses the six legacy colour aliases (`paper`, `paper-2`, `ink-2`, `accent-2`, `warn`, `bad`), the `.num` / `.rule` / `.explain` helpers and the unlayered table gutter rule (the kit's `TH` / `TD` carry their own padding). `DiffSinceLastSeen` and `lib/markdown` were already clean and only leave the list. `e2e/review-sb5.spec.ts`: skipped unless `REVIEW_OUT` is set; then it shoots the landing, Login and the public pages signed out, joins the coordinator Commune through every onboarding step, stands, moves a policy change, shoots Assembly (the floor open), Store, Ledger, the refusal page, Profile (the key disclosure open) and Societies, waits for the seat and shoots Assembly again and the workspace, and the operator's room on the admin session — every screen at 390 / 720 / 1100 in both themes with its overflow at 390. Specs changed in the same commit (ADR-0013 §6): `society` opens the "Create a key" disclosure before filling the label. docs/style.md §10 gains cards for the four Phase 2 screens; docs/plans/style-refresh.md marks the series done.
Deviations from TDD: none. From the bible: the API-key form sits under a `More` inside the "API keys" card rather than the whole section, since the existing keys are layer 3 and stay visible; the Operator's societies are cards rather than a table (six columns is card-per-row by §3, and each row carries a form and five actions); the Assembly's offices sit beside the builder in a `Two` rather than in an aside column, so on a phone the election comes before the decided ledger. The open proposal's ballot buttons are hand-styled pressed buttons (`aria-pressed`, ink fill when cast), not a `Segmented` radiogroup, because the spec and the screen reader both name them as buttons in a group.
Provisional answers added to QUESTIONS.md: Q154 (verdicts and tones for the four screens §10 had no card for), Q155 (the lexicon is not served to a reader without a session; the public page falls back to shell words).
New tunables: none.
Next session should know: the guard has no allowlist now; a new file with an old name fails `pnpm check` on the spot. The four new verdicts read counts the engine keeps, not thresholds it holds (Q154), and should move to Q153's param when it lands. The Commune's onboarding job board is empty (labor by norm), so any script through it presses "Skip for now" as the coordinator spec does. The review screenshots are under `target/review-sb5/` (`REVIEW_OUT=../target/review-sb5 CARGO_TARGET_DIR=target/e2e bash scripts/e2e/web.sh e2e/review-sb5.spec.ts`); the run takes about four minutes because it waits for the first close to seat the coordinator. Chris's dev server was on 8080 with no Vite; the e2e built under `target/e2e` and served on 18080. The `ECONNABORTED` lines the Vite proxy prints during a run are the SSE streams closing with each page and not a failure.
make check: web typecheck, oxlint (pre-existing warnings only), style-guard with no allowlist, vitest green; `make e2e-web` 13 passed; overflow at 390 is 0 on every SB.5 screen · new tests: 8 vitest (verdict.test.ts: assemblyVerdict, coordinatorVerdict, storeVerdict, ledgerVerdict) · sim-check: n/a

## S2.11 — Second-preset visual pass and the Phase 2 exit run — 2026-09-22 — main
Built: the kit pass. `Ledger` rows take `hours` beside `cents` and `goods` ("+4 h", the norm's unit as credits are a payslip's) and the table's last header is a prop (`amountLabel`: "Drew" on the draw record, "Hours" on the record of contribution). `Figure` takes a `bar`; the Store's shelf figures carry a bare `Meter` of stock against what is asked this hour (`lib/draws.ts` `stockLevel`: bare is crit, short of what is asked is attn and the fill is the ratio, covered is full and good; `shelf-figure-<good>`). `DiffSinceLastSeen` narrates the assembly's news the digest already carried as bare names (S2.5 `concerns`): a ballot the plan cast by default ("Your plan voted abstain on proposal #3 by default"), a proposal closed with its count, an honor, a seat taken through its day, a seat emptied with the reason, an election opened, a members' disbursement folded like a transfer in money or goods. `Num` needed nothing: `showValue` says "cr" only for a `Money` value, and a goods Explain never does. Home per GDD §15 for a Commune (Q156): the Ledger line ("0.5 of 6 hours today", `ledger-tile`, from `/ledger`'s `is_me` row where `labor = norm`) and tonight's Ballot ("2 proposals open — 1 waits for your ballot", `ballots-tile`, attn while one is owed, where `governance ≠ none`) as facts beside the Store tile; the countdown reads "share-out" where a Store exists; the compensation card takes the Store icon and its draw record is headed "Drew"; the dwelling gloss says one is assigned tonight. `lib/tonight.ts` (`ballotsText`, `lineText`) and `lib/contribution.ts` (`contributionRows`: today so far and yesterday as hours rows against the norm, Q157) are pure and tested. The Ledger screen gains "Your record, day by day" (`my-record-more` / `my-record`) under "Your line". `StatTiles` where `money` is off shows the GDD's Commune society stat — Store stock, Food on the shelf at the day's end with the Wares beside it, from the aggregates' `store_stock` (`storeStockFigure`, `store-stock-figure`) — in the price index's place, on the Society screen, the public page and the archive summary. The archive's standings without money are Citizen · Honors with no rank (Q157). Onboarding's plan step drops the price ceiling and the balance rows where there is no money. The Contracts sale offer without money is priced goods for goods (`Price::Good`, "For 2 wares the lot · Paid in wares") and the wanted ad, money-only on the wire, is not offered (Q158). The Gallery gains "The Commune: the kit without money": the TopBar with no balance over the Commune's items, Num in units and in hours, both Ledgers, the shelf and quorum Meters, the Diff with a default ballot. `e2e/review-phase2.spec.ts` (skipped unless `REVIEW_OUT` is set) joins the ledger-* Commune and the first Freeport, takes a norm position, and shoots Home, Work, Plan, Organizations, Contracts, Society, Talk and Archive in both societies, the Store, Ledger and Assembly beside the Market, and the Gallery's Commune section, at 390 / 720 / 1100 in both themes, printing each page's overflow at 390 and, on a Commune page, any element that says "cr". `commune.spec.ts` asserts the two new facts, "share-out" and no "payday", the shelf bar and the hours record. `docs/playtest/phase2-visual.md`: the reviewed pairs and fourteen `[H]` marks for Phase 5. Q156–Q158.
Deviations from TDD: none from the card. "`Meter` as quorum and stock bars": the quorum bar was SB.5's; the stock bar's denominator is this hour's requests (Q154's reading), since the engine has no capacity for a shelf. "`Num` without a currency" was already so; its test is the gate's evidence. The contribution ledger is two rows because the wire carries two days (Q157).
Provisional answers added to QUESTIONS.md: Q156 (the Commune Home's two facts and "share-out"), Q157 (a contribution ledger from a wire with no history; a money-less archive's standings), Q158 (the engine accepts a money price where money is off).
New tunables: none.
Next session should know: **Phase 2 is closed.** The exit run, all on this sha: (1) `make sim-check PRESET=commune` green (five seeds, five epochs, need 100 %, no stock-out, no rejection); `make sim-all EPOCHS=5` "all five presets stable"; `cargo test -p isms-sim --release --test invariants -- --ignored` green in 37 s (conservation, determinism and the S2.4 governance invariants — a `PolicyChanged` from a proposal each epoch, every seat filled from cycle 1, no proposal open past its close — five presets, five epochs, five seeds). (2) `make e2e-web`: 13 passed on both seeds (a Freeport citizen sees no Assembly in `society.spec.ts`; `commune.spec.ts` and `assembly.spec.ts` propose, vote, draw and read the Ledger; `coordinator.spec.ts` publishes a Plan). (3) `make e2e-agents`: Freeport clean (8 scripted players, 336 turns, 0 skipped, every probe refused, no 5xx; run `e2e-1790106103-freeport`) then the Commune clean (8 players, 352 turns, 0 skipped; 8 motions moved — two honors, three norm changes each way, a resolution twice —, 58 ballots accepted, 3 candidacies, 2 Plans published, 2 workplaces opened, 16 floor posts; the chronicler's tallies show every close at 4–7 cast against a quorum of 2; every probe refused; run `e2e-1790106103-commune`). (4) `every_preset_defines_every_lexicon_key` and `commune_copy_renders_every_governance_event` green. (5) `docs/playtest/phase2-visual.md` written. The review run's first pass died at the 14th screen on the e2e server's Postgres pool timeout (the SB.5 gotcha); the rerun got through all 168 captures with overflow 0 at 390 on every screen of both societies (the Gallery page itself, a catalog, measures 150 over in its "Fit" width from the shell demo's TopBar, which predates this card). The screenshots are under `target/review-s211/`. Phase 3 has no cards yet: TDD §18.5's paragraph and GDD §18 row 3 are the brief; the kit now carries two presets and the Republic's lexicon is the third. Engine follow-ups waiting for a card: Q157 (`Standing` with contribution totals so a Commune's archive ranks by its own scoreboard) and Q158 (`offer_sale`/`PostWanted` refused with `NotInThisSociety` where money is off). The Commune's chronicle copy prints "closing at the end of day 0" (0-based cycle) — one line in `presets/copy/commune/chronicle.toml`, noted in the visual doc. A chained `make e2e-web` + review spec + `make e2e-agents` in one background Bash call took nine of the tool's ten minutes; split it next time. Tests ran under `CARGO_TARGET_DIR=target/s211` (cargo) and `target/e2e` (Playwright); Chris's dev server was on 8080 and an old Vite on `[::1]:5173`, neither in the way.
make check: web typecheck, oxlint (pre-existing warnings only), style-guard, vitest green (123 tests); the exit run above · new tests: 12 vitest (moneyless.test.tsx: Num, Ledger, DiffSinceLastSeen, Meter-in-Figure, StatTiles), 5 vitest (lib.test.ts: stockLevel, contributionRows, ballotsText, lineText); commune.spec.ts extended · sim-check: commune green; sim-all all five stable

## SJ.1 — A Jev brain for the synthetic cohort (spike) — 2026-09-24 — main
Built: a third `Brain`, `jev` (`agents/src/brain/jev/`), on TypeSafe's Jev through OpenRouter's Decisions API (`OPENROUTER_API_KEY` in the root `.env`; ADR-0014). Code reads the game and lays out the hour as slots (`candidates.ts`: `work` how many hours and how hard; `job` take one or switch when an offer pays more; `venture` for the founder, `market` for the speculator, `property` for the landlord), each option carrying its tool call with the numbers worked out by the scripted strategies' own arithmetic and each slot ending in a do-nothing option; `state.ts` builds a small deterministic state with no ids and no prose; `questions.ts` puts the persona in each `choice` question's instructions; `client.ts` is one `fetch` through the harness `Transport` (401/402 kill the brain for the run, 429/529 retry once); `index.ts` maps answers to candidates under `jev.min_confidence` / `jev.irreversible_confidence` (config.toml `[jev]`), runs them hours-first under the action cap, templates the intent, and writes a `decisions` list into the journal line. `--brain jev` puts `[jev].personas` (founder, wage-maximiser, speculator, landlord) on Jev and scripts the rest; `brainFor` refuses a society with an assembly. The report gains a "Jev decisions" table (held turns, mean confidence, choices the floors overruled, flip-flops, refusals, ms and $ a turn) and a per-slot tally. `MODELS` lists `~typesafe/jev-latest` at $0.042/MTok in, $0 out, with `thinking` now optional; the turn and cycle models are checked to be language models. `readClock` for the tick watcher now reads through an unrecorded client. Fixture `founder-1` (a one-day recorded Jev epoch, 22 turns, decision calls included) joins the replay test, which runs over both transcripts. README section; `.env.example`, `run.sh` and Makefile comments.
Deviations from TDD: none from a card (there is none: SJ.1 is a side-card, like SB.x). ADR-0014 records the brain beside ADR-0010's two and keeps ADR-0011's one-run-per-approval.
Provisional answers added to QUESTIONS.md: none.
New tunables: `agents/config.toml` `[jev]` `model`, `base_url`, `personas`, `min_confidence` (0.35), `irreversible_confidence` (0.5).
Next session should know: **Chris approved $1; two runs spent $0.04.** (1) `sj1-2day` (8 players, 4 on Jev, 5 s ticks): PASS, 184 Jev turns, 0 skipped, no error, no 5xx, ~300 ms a turn, $0.01; every Jev player took a job in hour 1, the speculator bought grain once, the founder saved without buying Materials (two-way venture choices came back at 0.12–0.21 confidence, under the floor). (2) `sj1-7day` (10 s ticks): PASS, 560 Jev turns, $0.03, no error; the Jev founder chose `buy_materials` eight hours running with its confidence climbing 0.02 → 0.32 as the balance grew (seven held back by the 0.35 floor), bid for 20 Materials at 0.46, chose `found_now` at 0.39 (held back by the irreversible floor) and at 0.51 founded a firm, day 1 hour 19, then ran it quietly for 129 hours; the scripted borrower founded too; the landlord never saw a dwelling on sale and the wage-maximiser never a better offer, so their only live slot was `work`. Every player skipped 10–42 hours, scripted ones included: the recording run and its cargo build shared the machine; not Jev's latency. Reports in `docs/playtest/runs/sj1-2day.md` and `sj1-7day.md`. **What the spike says:** the wiring, price and speed are settled (a turn is ~1.2k tokens, an epoch about three cents; the tick is no constraint); the measurement is not yet fair, because a lab Freeport gives Jev few real choices: `work` is answered `none` once the hours are set (that is the sensible reading, and it is why "held" is 91–98%), `market` appeared twice in seven days, `property` never. Before judging its judgment: seed the lab with dwellings on sale and a second employer whose offer beats the first, or give the slots more to choose between (effort under fatigue, the `few_low` choice when Food is high, selling output at two prices). The low confidence on two-way venture choices with the mass split is the calibration finding the evals warned of: read `probabilities`, not only `confidence`, and consider a per-slot floor. The recording run's rollover timed out ("no rollover within 180s") while the seven-day server was running beside it on another port [H]. Follow-ups: the Commune's slots (ballots, offices, the Plan, the Store) as a card; a daily reflection whose notes enter `state.notes` as a measured condition; `score` questions for a risk dial; a direct `api.typesafe.ai` base URL when a key exists.
make check: agents typecheck, oxlint, vitest green (94 tests) · new tests: 16 (jev.test.ts: laying out the hour, state determinism and no ids or persona, take_best → accept_offer, full_normal → set_labor as the helper sends it, all-none holds, an irreversible switch at 0.3 dropped, tier order, an option never offered and a slot never asked, nothing to decide, a 500, a refused key, the report's Jev section) and the founder-1 replay · sim-check: n/a

## SJ.2 — More real choices for the Jev brain: housing, plan, credit, an honest firm, standing and ambition — 2026-09-24 — main
Built: slots are offered only when something gives a reason to choose (`agents/src/brain/jev/candidates.ts`): `work` asks its full menu while nothing is set today and afterwards only a change with a reason (fatigue debt → `rest_more`, Food under 40 → `work_more`, a balance under a day's wage → `push_harder`, Food high and five days' wages in hand → `ease_off`, else `keep_hours`); `housing` for every persona (rent the cheapest dwelling to let while unhoused, with the rent as a share of a day's wage and the Shelter penalty in the question); `plan` for every persona, once a day unless Food is low (`stock_more_food`, `put_aside` three fifths of a fat balance, `loosen_floor` when the floor starves the pantry; the rest of the plan is carried over from `/home`); `credit` for the borrower (the cheapest loan on the board, the daily installment precomputed as a share of a day's wage, irreversible, once); the firm slot gains `fund_firm` (a quarter of the founder's own money into a treasury that cannot pay a worker for a day) and two wage tiers (`post_job` at the median, `post_job_generous` 15% over it, so firms compete for hands), and its question says how many worker-days the treasury covers. A `standing` block in the state from a `scoreboard` read each hour (`state.ts` `standingOf`: rank by net worth, self-made, trend against the first read of yesterday, the gaps to the citizen above and below, all precomputed) and one standing sentence in every question's instructions (`standingText`); `Script.scoreboard()`. Personas gain an optional `ambition` line in the frontmatter, rendered by `personaText` after temperament and risk: the founder, wage-maximiser, speculator, landlord, borrower and saver have one (the saver's says it does not care), the slacker none. `[jev].personas` is all seven of Freeport's strategic personas; the rule-prober stays scripted. `run.sh` seeds `params.initial_dwellings = 40 + PLAYERS` for a lab (Q159). README, ADR-0014 amended.
Deviations from TDD: none. Q159 is a game question the lab seed sidesteps.
Provisional answers added to QUESTIONS.md: Q159 (Freeport's 40 householders fill its 40 dwellings before any human joins; the lab seeds one more per player; the real society's answer is a card).
New tunables: `INITIAL_DWELLINGS` for `scripts/agents/run.sh`.
Next session should know: **Chris approved $1; three runs spent $0.06.** `sj2-2day` (before the dwelling fix; 7 Jev players + the fuzzer, 5 s ticks): PASS, $0.02, no error — but I edited `run.sh` while that bash process was running it, bash resumed at a shifted offset and played the cohort a second time on epoch 2, so its report covers two epochs (93 turns a player) and is not the measurement. `sj2-2day-b` (48 dwellings, one cohort): PASS, $0.02, 322 Jev turns, no error, no 5xx. **Every Jev player ended housed at Shelter 100; the scripted rule-prober, with no housing rule, ended unhoused at Shelter 8.** `housing` was chosen `rent_cheapest` 16 times, 7 rented and 9 lost the race for the same cheapest dwelling ("That offer was taken or withdrawn": the players all read the same board in the same hour). `job switch` 4 times, `stay` 2: the two firms' `post_job_generous` gave the wage-maximiser a rival offer at last. Two firms founded (founder and borrower), `fund_firm` twice, `sell_output` 39. `work` fell to `keep_hours` 249 with 9 `full_normal` and 1 `few_low`: the change menu fires (fat balances), Jev keeps the hours. `plan`: `put_aside` 9, `keep_plan` 313 — that run predates the once-a-day gate, which is in the code now. Speculator: 5 flip-flops in `market`, the one slot that flips; per-slot floors are still the open tuning question. Reports in `docs/playtest/runs/sj2-2day-b.md` (the measurement) and `sj2-2day.md` (the doubled one). Mean confidence 0.60–0.88; latency 200–325 ms a turn; ~$0.00007 a turn. Next: the Commune's slots; a seven-day run with the gate and the dwellings; and, from the housing races, a small thing worth its own line — seven players who all want the cheapest dwelling in the same hour is exactly what a real cohort would do too, so the `unknown_offer` copy ("That offer was taken or withdrawn") is going to be the most-read refusal in the game. **Never edit a script while bash is running it.**
make check: agents typecheck, oxlint, vitest green (99 tests once the founder-1 fixture is re-recorded on the new request order) · new tests: 5 (jev.test.ts: the plan slot and its carried-over fields, hours re-asked only for a reason, a roof for the unhoused, the borrower's cheapest loan once, the standing and its text, ambition with the persona and not in the state) · sim-check: n/a

## SJ.2b — The seven-day run, read for the economy — 2026-09-24 — main
Built: the report gains "The economy, day by day" (`/stats` snapshotted at each day's end into `days.jsonl` by the cohort, the epoch's last day from the closing read) and "Standings at the end" (the scoreboard at the run's end into `scoreboard.json`, the cohort's players in bold, every player's rank kept). The decisions client retries a gateway 5xx once (OpenRouter answered 520 once in 1,162 turns).
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: `sj2-7day` (7 Jev + the fuzzer, 48 dwellings, 10 s ticks): PASS, $0.05, 1,162 Jev turns, one 520, 0 skipped. **The economy is healthy and not vibrant.** Food last price 1.31 all seven days and the price index 1.14 from day 2: the legacy runner's cost-plus asks set prices and nothing the players did moved them. Firms 20 all week (18 legacy + the two founded), credit outstanding 0.00 all week (nobody lends: the legacy runner never posts credit and no persona does), needs met 98%, hardship 0, wellbeing ~90, consumption Gini 0.02–0.05. Real output rose 1425 → 2113 as the legacy firms bought Machines (26 → 44 Materials to machines). Day 5 broke the pattern: Materials produced fell 868 → 320, unemployed 0 → 9, investment share 5% → 13%, Gini 0.046 → 0.008, then a partial recovery — not diagnosed; the aggregates alone do not say whether a Foundry starved for ore or a legacy firm shed workers. **Standings:** the five players who did nothing but work and save hold ranks 1–5 (the fuzzer first at 1225, self-made +225); every householder sits at 1078.61 exactly (they pay rent, buy Wares, save 10% by script); the two founders are 38th and 40th with negative self-made, each holding one firm worth 303 and 127 credits book. **Both mines lose money:** the founder hired at the median (8.00/h, two places), asked 4 ore at 0.97 thirty-one times, and never allocated its own hours to its own workplace (the `work` slot sets labor at the contract job; a manager's own mine is not among its candidates). ~4 ore a day against up to 128 credits of wages. That is the missing slot: `work` should offer the manager's own workplace, and the firm slot should say what a day's wages cost against what a day's output sells for before Jev is asked to hire. Also: the slacker's slots were empty almost all week (34 ms a turn, $0: four hours at low effort set on day 1 and no reason ever fired), so the persona held by construction rather than choice; the speculator's `market` flipped 3 times; mean confidences 0.63–0.98, higher than the two-day runs, as the situations repeat. Reports: `docs/playtest/runs/sj2-7day.md`. Theories for the flatness, in order: (1) no lender and no dwelling seller, so two personas' markets have no other side; (2) the legacy runner's cost-plus pricing pins every price, so the players are price-takers in a fixed-price town; (3) seven players against 32 householders and 18 legacy firms is a perturbation; (4) firm economics: a fresh mine with unskilled poached labor cannot pay median wages, so the only capital formation in the town loses money by design of the recipes at start prices — worth checking against the sim's own founding curves before touching it.
make check: agents typecheck, oxlint, vitest green (102 tests) · new tests: 3 (report.test.ts: the day table, the standings; jev.test.ts: a 520 retried once) · sim-check: n/a

## SJ.3 — Founders that decide, a lender, a seller, a town of players, the day's headlines — 2026-09-24 — main
Built: the manager's own workplace in `work` (`agents/src/brain/jev/candidates.ts`): the firm slot leaves the firm it manages in memory (`Firm`: org, workplace, kind, recipe, inventory, last price) and `work` offers `work_own` once a day while the manager's hours are elsewhere — a day's output at its own rate capped by the inputs the firm holds, its value at the last price, against the wage it gives up — and `work_job` the way back; since `FoundOrg` grants no position and `SetLabor` needs one, the founder hires itself at a token wage of one cent an hour (Q160: post one place, accept it, set the hours). The firm question now says what the hired hands cost a day against yesterday's output (`last_cycle_output` × the last price) and offers `lay_off` (the dearest hand's contract ended on the org's behalf, from `/contracts`) once a full day's record shows wages outrunning output, and `buy_<input>` for what the recipe consumes from the treasury; `ventureSlotOf(kind)` serves the founder's mine and the builder's Builder. A `lend` slot for the lender (a quarter of the balance at 1% or 3% a day over five days, once a day while none of its offers is open, the roll's default count in the question; irreversible) and a `dwelling` slot for the builder (a finished, unoffered dwelling sold at Materials-plus-hours cost plus a fifth or let at the going rent, the days of rent that match the sale price precomputed). Personas `lender.md` and `builder.md` with scripted strategies (`lender`: a fixed 200-credit loan at 1% over five days whenever the balance covers it twice; `builder`: `founderOf("builder")`, inputs bought for the firm, each finished dwelling sold or let in turn); `founderOf(kind)` generalises the founder. `Script.citizens()`, `Script.contracts()`, `workerContracts`, `goingRent`, `dwellingAskPrice`. A named cast: `run.personas_for["freeport-town"]` (sixteen: two founders, two lenders, a builder, a landlord, two speculators, two wage-maximisers, two savers, two slackers, a borrower, the rule-prober), `--cast` in `main.ts` (`personasFor` moved to `src/player/cast.ts` so a test can load it), `CAST=` and `POPULATION_FLOOR=` in `run.sh` and the Makefile. The cohort's day-end snapshot reads `/chronicle` for the day just closed and keeps its headline texts in `days.jsonl`; the report prints them under the day table. `[jev].floors` (`{ market = 0.5 }`) gives one slot its own confidence floor. `[jev].personas` gains lender and builder. README, Q160, Q161.
Deviations from TDD: none. From the card: `work_own` is offered from the firm slot's memory rather than a second orgs read in `work`, so the founder-1 replay holds without re-recording; the lay-off names the dearest hand rather than any.
Provisional answers added to QUESTIONS.md: Q160 (a manager holds no position at its own workplace; the founder hires itself at a token wage), Q161 (the lab seed fills the floor before the players join, so day 1's end emigrates one householder per player; read the economy from day 2).
New tunables: `[jev].floors`; `CAST`, `POPULATION_FLOOR` for `scripts/agents/run.sh`.
Next session should know: the free gates are green: `pnpm --dir agents check` (116 tests), `make e2e-agents` on both presets (352 turns each, 0 skipped, conservation replay clean), and a scripted sixteen-player town (`RUN=sj3-town-scripted CAST=freeport-town PLAYERS=16 BRAIN=scripted EPOCH_CYCLES=2 TICK_SECONDS=1`, 688 turns, clean; report in `agents/runs/sj3-town-scripted/report.md`). **The scripted town already moves the day table:** credit outstanding 168.00 on day 1 and 126.00 on day 2 (the borrower took a lender's loan — the first non-zero credit in any run), consumption Gini 0.22 then 0.15 (0.02–0.05 all week in `sj2-7day`), 22 firms (four founded, the builder's among them), Materials to Machines 4 → 38. The headlines under the table showed the day-1 shock every lab run has carried unseen: fifteen householders "has left Freeport" at the first day's end (Q161), hence day 2's needs met 60 % and output 710. The paid runs (a two-day sixteen-player Jev run at ~$0.05, then seven days at ~$0.15, ADR-0011) wait on Chris's approval; the seven-day read list is in the card. A 1 s tick town run takes about five minutes. Nothing in the founder-1 recording founds a firm, so the replay test is untouched by the manager branch; re-record only if the non-manager venture, work, housing, job or plan text changes.
make check: agents typecheck, oxlint, vitest green (116 tests) · new tests: 14 (jev.test.ts: work_own offered once a day and its accept/set_labor; the token place posted when the board has none and output capped by inputs; the wages-against-output sentence and lay_off only after a full day when wages outran output; the dearest hand's contract ended on the firm's behalf; the builder's dwelling slot with the price and the rent; the sale and the lease posted on the firm's behalf; the lender's slot, its once-a-day gate, its absence with an offer open or too little to lend; the loan posted; a per-slot floor; persona.test.ts: the fourteen persona files, the cast of sixteen loads; report.test.ts: the headlines under the table; cohort.test.ts: headlines in days.jsonl) · sim-check: n/a

## SJ.3b — The sixteen-player town's two runs, read for the economy — 2026-09-24 — main
Built: nothing new; the two runs the card's done gate asks for, on Chris's approval ("two days, then seven if clean"). Reports in `docs/playtest/runs/sj3-2day.md` and `sj3-7day.md`.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: none (Q160, Q161 are SJ.3's).
New tunables: none.
Next session should know: **Both runs clean, $0.14 in all.** `sj3-2day` (16 Jev players, 24 householders, 10 s ticks): 736 turns, 0 skipped, $0.03, no error, no 5xx; credit outstanding 203.28 and 152.46; `work_own` acted twice ("set 8 h at my own mine"), both lenders' loans posted, the borrower's credit taken, ore asked at 1.11 after a week at 0.97. `sj3-7day`: 2,652 turns, 4 skipped, $0.11, no error, no 5xx, epoch archived and rolled over, conservation replay clean. **Against the seven-day list:** (1) *a price that differs between days* — Food 1.31 and the index 1.14 every day still; the legacy mills' cost-plus asks pin the basket; only ore moved (1.11), and Materials *output* swung 483 → 899 → 293. (2) *credit above zero* — 203.28 on day 1 falling 50 a day to 0.00 on day 5 as the one loan taken (242.00 at 1% over five days, borrower-1 from lender-1) repaid itself; `lend_cheap` was chosen 3 times in the week and `hold_money` the rest, at 0.51–0.54 confidence. (3) *a dwelling sold* — **yes, a market:** builder-1's Roofs built fourteen dwellings (base rate 0.5 an hour is four a day when the Materials are there; the [H] is answered), Jev chose `sell_dwelling` 15 times at 43.20 (ten Materials at 2.00 plus two hours at 8.00, plus a fifth) and never `lease_dwelling`; landlord-1 bought thirteen (`buy_dwelling` 13) and let twelve at 12.80 a day (`offer_lease` 12). (4) *the founders' book value* — at the end: borrower-1's Works 460.57, builder-1's Roofs 276.96, founder-2's 175.73, founder-1's 164.69; the scoreboard carries no day-by-day, so "not falling" is unread. (5) *a Gini that opens* — 0.09 on day 1, then 0.02–0.06: no wider than before; consumption is still the plan's 24 Food for everyone. (6) *day-5 breaks* — none like sj2's; the day table's own break is unemployment 2 → 13 → 15 from day 4 and the day wage 62 → 42, and the headlines say nothing about it because it is not an event: **both lenders chose `job: wait` all 166 turns** (mean confidence 0.5; `wait` is the do-nothing option, so no floor touches it) and lived on the endowment, which is why they rank 39th and 40th (583 and 505) with the loan long repaid; the founders on token self-wages pull the mean wage down. **Standings:** saver-1 1197.93 and wage-maximiser-1 1190.07 lead, the householders at 1095–1101 fill the middle, the four founders sit 32nd–37th (the firms' book value is in their net worth; the losses are wages and Materials bought), the landlord 38th at 628.47 — thirteen dwellings bought for 561.60 and **the scoreboard values dwellings and loans out at nothing**, so a landlord's and a lender's strategies read as losses by construction (an engine question for the Phase 3 archive work, with Q157). `lay_off` was never chosen and the journals cannot say whether it was offered (the questions are not journaled; a recording would); `work_own` 8 and `work_job` 5 — the founders moved between their mines and their jobs day by day. Jev: mean confidence 0.52 (lenders) to 0.93 (speculators); 23 flip-flops for the landlord (buy/lease alternating as dwellings arrived, which is the market working, not indecision); `market` under its 0.5 floor still chose `buy_*` 8 of 9 times. The lenders' waiting is the first clear persona failure the brain has produced: the persona text says "a wage keeps you fed" and the ambition says "without working an hour more than you must", and Jev read the second. Theories for the flat basket, in order, now: (1) the mills' cost-plus Food asks with no Food buyer but the plan — a player who *sells* Food or bids over the ask would move it; (2) the town's players still post nothing on the Food or Wares books; (3) 24 householders still outnumber 16 players at the mills. Follow-ups: an `unemployed_days` reason in `job` so waiting is re-asked as a change with a cost; net worth to count dwellings and loans out; a per-day firm book value in `days.jsonl` from `/orgs`; the Commune's slots (SJ.4).
make check: n/a (no code) · new tests: none · sim-check: n/a

## E-1 — The runner's markup answers the shelf — 2026-09-24 — PR (s3.1-runner-markup)
Built: `Org.shelf` (per output good: the stock at the last cycle close and a markup `step`), the `ShelfClosed` event emitted at 8m for every market org whose shelf changed, `householder::ask_markup` / `ask_price` (cost-plus at `legacy_markup + step x legacy_markup_step`, clamped to `legacy_markup_min..=legacy_markup_max`), and the manager's asks posted at that price. The step falls by one when a good's closing stock is above the close before and rises by one when the shelf closed empty after producing; a first close only reads; Society-owned orgs are skipped. The start-price floor in `cost_plus` is retired. SCRIPT.md rule 3 rewritten; every golden regenerated (the params line and the new events); `docs/tuning/freeport-02.md`.
Deviations from TDD: TDD 9.3's fixed markup and Q40's start-price floor, amended by Q162 (a QUESTIONS row, no ADR: the mechanism is the card's).
Provisional answers added to QUESTIONS.md: Q162.
New tunables: `householder.legacy_markup_step` 0.05, `legacy_markup_min` 0.0, `legacy_markup_max` 0.45.
Next session should know: **The lever moves prices, in one direction, to one place.** In the sim every legacy shelf grows from the first close, so Food falls 1.31 -> 1.26 -> 1.20 -> 1.14 (cost) by cycle 4 and stays; the index floor is 0.886 from cycle 12; the band is 0.89-1.14 (was 1.02-1.14) on Freeport, the Republic and the Commonwealth; all five presets stable. Worst-epoch unemployment rose 15-18 -> 22-30 (thinner treasuries at cost) and holds at every floor tried (0.0, 0.05, 0.10, -0.10) and at step 0.02, so the defaults stay; need 100 %, hardship 0. **Read SJ.5's price graph against a 1.14 floor, not 1.31**; a shelf that never sells out never steps up, so a rise needs a buyer that empties a shelf (a player's bid, or SJ.6's fewer householders). The one test that surprised: the cut sells at the resting bid's price (ADR-0001), so the tape shows 1.31 the hour the ask drops to 1.26; the ask, not the print, is the runner's signal. Dev databases seeded before this PR need a reseed (`Org` grew a field). Next: SJ.5 (step 2 of `docs/plans/market-liveliness.md`), its graph first.
make check: green · new tests: 2 (`a_shelf_moves_the_ask_by_a_step_within_the_band`, `a_shelf_that_closed_fuller_steps_down_and_the_ask_follows`) · sim-check: all five presets stable

## SJ.5 — Prices from the players — 2026-09-25 — main
Built: the firm slot prices (`agents/src/brain/jev/candidates.ts`): `sell_output` is now `undercut` (every sellable good asked one tick under the best ask, or under the last print when nobody asks; the shelf's days unsold in the sentence, from a once-a-day reading of the firm's inventory kept in `memory.shelf`/`memory.unsold`) or `hold_price` (5% over the last print, as before); each input the recipe consumes is `take_ask_<good>` (at the best ask, when the treasury covers it) or `bid_under_<good>` (a resting bid one tick under); the slot's facts carry each good's book (`last`, `best_bid`, `best_ask`, depths, `days_unsold`). `bookOf`, `undercutPrice`, `TICK` (Q163: one cent). A `which_workplace` slot for the founder and the borrower, once a day when the Materials and the fee are in hand: mine, mill, workshop and foundry ranked by `dayMargin` (the product's last price × base rate × 8 h, less the inputs at their last prices and a day's wage at the citizen's rate), the best first, `decide_later` last; the choice lands in `memory.workplaceKind` and `found_now` is offered only once one is chosen, naming it (`ventureSlotOf(kind, name, choose)`; the builder keeps its Builder). `job` while unemployed keeps `memory.unemployedSince` and, from the second day, says how many days have passed and what the best wage would have paid, with `wait` reworded as "another day without a wage" (day 0 is unchanged, so the founder-1 replay holds). A `comfort` slot for every persona, once a day while Comfort is under 60 and no Wares are in the pantry: `take_ask` (enough to lift Comfort to 80, at most five), `bid_under`, `go_without`. The cohort's day-end snapshot reads `/books` and keeps a `prices` row per good (last, bid, ask, depths; shares skipped) in `days.jsonl`; the report's `pricesTable` draws "Prices, day by day" (one column per good, the last price in bold when it differs from the day before) and "The books at each day's end" (best bid / best ask with depth, the ask in bold when it moved) under the economy table. README, Q163, the jev.md card row.
Deviations from TDD: none. From the card: `bid_under`/`take_ask` are named per input (`bid_under_materials`) since a Builder's slot may carry several; the workplace choice is its own slot the hour before `found_now` rather than a candidate list inside `venture`, so the founding stays an irreversible choice of one option.
Provisional answers added to QUESTIONS.md: Q163 (the market's tick is one cent).
New tunables: none (`COMFORT_LOW` 60 and `COMFORT_PER_WARES` 6 mirror the preset's `wares_comfort_below` and `comfort_per_wares`; `TICK` 1).
Next session should know: the free gates are green: `pnpm --dir agents check` (125 tests), `make e2e-agents` both presets (352 turns each, 0 skipped, conservation replay clean), and the scripted sixteen-player town (`RUN=sj5-town-scripted CAST=freeport-town PLAYERS=16 BRAIN=scripted EPOCH_CYCLES=2 TICK_SECONDS=1`, 688 turns, clean; report in `docs/playtest/runs/sj5-town-scripted.md`). **The graph draws, and it shows the books, not just the prints:** Food 1.31 both days with 551 then 872 bid at 1.31 and no ask at the day's end (the mills' shelves sell out by evening, the plan's bids rest); Materials 2.28 with 557 then 270 bid (the foundries' 14% markup bids, Q1) and no ask; ore asked at 0.92, 14 then 259 deep; Wares 4.00 → **4.12** (the one print that moved: the first sale off a workshop shelf). A two-day run cannot show E-1: the shelf needs two closes to compare, so the first step is day 3's ask; read the paid week from day 3 against the 1.14 floor, and against the *ask* column, since an undercut fills at the resting bid (ADR-0001). Wait: the paid seven-day sixteen-player Jev run (~$0.15, ADR-0011) is the card's last gate, read for a founder's output that sells after day 2, a day-VWAP that differs between days by more than the runner's step, and a lender with a job; `docs/plans/market-liveliness.md` step 2 is marked built, not done, until it runs. The scripted brains still use `sell` at 5% over and `buy` at the ask; only the Jev cast undercuts, which is what the paid week measures.
make check: agents typecheck, oxlint, vitest green (125 tests) · new tests: 9 (jev.test.ts: undercut/hold_price with the days unsold and the book facts, the undercut ask placed on the firm's behalf and the count reset when the shelf shrank, an input at the ask or a tick under from the treasury and only the bid when the treasury is thin, which_workplace ranked by margin once a day and absent without Materials, founds what it chose, job re-asked with the days and the unearned wage, comfort at the ask or under it once a day for every persona, the Wares bid placed; cohort.test.ts: the price rows in days.jsonl; report.test.ts: the price table and the books, a move in bold) · sim-check: n/a

## SJ.5b — The seven-day town on the price graph; two foundry defects — 2026-09-25 — main
Built: the card's paid run, on Chris's approval ("Approved, run the seven-day town"): `RUN=sj5-7day CAST=freeport-town PLAYERS=16 BRAIN=jev EPOCH_CYCLES=7 TICK_SECONDS=10 --max-usd 1`, report in `docs/playtest/runs/sj5-7day.md`. Then two fixes the run found in `candidates.ts`: the SJ.1-era `g !== "materials"` filter on a firm's sellable stock now spares the good the firm *makes* (a foundry could never sell its Materials); and `take_ask_<input>`/`bid_under_<input>` buy a day's worth at the base rate (a foundry's eighty ore) or what the treasury covers, at least one unit's worth, instead of "one unit's worth" (one ore an hour). Test: `a foundry (SJ.5b)`.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: **Clean, $0.11: 2,656 turns, 0 skipped, no error, no 5xx, epoch archived, conservation replay clean.** The graph works and it is E-1's, not the players': grain 0.61 → 0.53, ore 0.92 → 0.80, Materials 2.28 → 1.72, Wares 4.12 → 3.58 by day 6, Food 1.31 → 1.14 on day 7 — every good but machines moved by more than the runner's step, all on the *ask* side ("no bid / 0.53 (3404)"), and the day-VWAPs in the event log agree (ore 104.5, 93.1, 88.0, 84.0, 80.0). **But not one of the week's 8,045 trades had a player on the selling side, and no player firm posted an ask** (event log: `OrderPlaced.order.owner.org >= 18` has 19 Materials bids by the Roofs and 94 one-ore bids by borrower-1's foundry, nothing else). Why: `which_workplace` chose `open_foundry` all three times (foundry 86.40 a day at day-1 prices against mine 73.60; a sensible pick), and a foundry's product is Materials, which the SJ.1 filter struck from `sellable`, so `undercut`/`hold_price` were never offered; and `buy_<input>` bought one ore an hour, so borrower-1's foundry made 94 Materials in the week and the two founders' made none (they chose `run_quietly` 295 times over the only choice left, a one-ore bid). Both fixed above. **Against the card's list:** a founder's output that sells after day 2 — **no** (nothing to sell); a day-VWAP that differs between days by more than the step — **yes**, by the runner (the plan's "done" wants a player on the moving side, so the liveliness question stays open); a lender with a job — **yes**, both took one (`job take_best 48` across the cast; the SJ.5 sentence turned SJ.3's 166 `wait`s into 150 with two hires each). Also seen: the `comfort` slot is the players' first Wares demand (68 manual Wares bids, 280 units, `take_ask` 76 of 90) and the Wares ask fell 4.12 → 3.58 anyway; unemployment 2 → 16 on day 5 and 3 → 11 on day 7 tracks E-1's thinner treasuries (the sim saw the same); the landlord bought 18 dwellings and ranks last at 209.11, the lenders 37th–38th — E-2's valuation, unchanged. **The next paid run needs a new approval** (ADR-0011): the same seven-day town with the foundry fixes is the first run that can put a player's ask on the graph; read it for a founder's Materials asked one tick under the foundries' and a Materials day-VWAP that moves the hour they post. Step 2 of `docs/plans/market-liveliness.md` stays 🔶.
make check: agents typecheck, oxlint, vitest green (126 tests) · new tests: 1 (jev.test.ts: a foundry buys a day's ore or what the treasury covers and sells its Materials) · sim-check: n/a

## SJ.5c — The second seven-day town: a player sells — 2026-09-25 — main
Built: nothing new; the re-run with SJ.5b's foundry fixes, on Chris's approval ("Approved, run it again with the fixes"): `RUN=sj5b-7day`, same command as `sj5-7day`. Report in `docs/playtest/runs/sj5b-7day.md`.
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: **Clean, $0.11: 2,656 turns, 0 skipped, no error, no 5xx, archived, conservation replay clean. The card's three reads are all met.** (1) *A founder's output that sells after day 2* — **yes**: borrower-1's Works, a foundry, made 643 Materials and sold 376 of them — 44 on day 1 in thirteen `undercut` asks at 2.27 into the foundries' resting 2.28 bid (filled at 2.28, ADR-0001), and **332 in one `undercut` ask at 1.80 on day 5, one tick under the legacy foundries' 1.81, which sold whole** (the first player-side sale of a good in any lab run; `undercut` chosen 14 times at 0.70–0.91 confidence, `hold_price` never). (2) *A day-VWAP that differs between days by more than the runner's step* — **yes**, the same graph as `sj5-7day` (grain 0.61 → 0.53, ore 1.06 → 0.80, Materials 2.28 → 1.72, Wares 4.12 → 3.58, Food 1.14 on day 7); the player's 332 units pulled day 5's Materials VWAP to 1.802 against the runner's 1.81 — a real dent, but 0.8 cents where the runner's step is ~8.6, so the plan's stricter "done" (a player on the moving side by more than a step) is not yet met; day 7 closed with grain and ore **sold out** ("no bid / no ask"), so the first upward step in any run would have come on day 8. (3) *A lender with a job* — **yes**, both hold two. Also: borrower-1 ends 27th at 1,038.95 (self-made +38.95), **the first player firm in the black in any run**, having put every credit into its foundry (balance 0.09) and sold the output; founder-1's foundry (org 19) produced nothing all week — founded with an empty treasury, `take_ask_ore` needs the firm's money and `fund_firm` was chosen only 6 times, so read the funding rule next (a founder should keep a day's inputs in hand at founding); founder-2 never founded (`keep_saving` 166, `which_workplace` twice chose a foundry but the Materials bid never filled against the foundries' 2.28 bids — a founder bidding 10% over the last print at 2.20 sits behind the runner's 2.28); Jev preferred `take_ask_ore` (130) to `undercut` (14) for the foundry, so the shelf built to 332 before it sold — a calibration read for SJ.6. Unemployment 11–13 from day 4 as before (E-1's thinner treasuries). **Step 2 of `docs/plans/market-liveliness.md` is done; the liveliness question itself is still open** — the next lever is SJ.6 (eight householders: the players become the labour force and the runner's shelves run dry), read on this graph against `sj5b-7day`'s; then E-2 (the landlord is last again, 18 dwellings at nothing).
make check: n/a (no code) · new tests: none · sim-check: n/a

## SJ.6 — The town with eight householders — 2026-09-25 — main
Built: nothing new; the card's run, on Chris's approval ("I approve moving forward with SJ.6"): `RUN=sj6-7day POPULATION_FLOOR=24 PLAYERS=16 CAST=freeport-town BRAIN=jev EPOCH_CYCLES=7 TICK_SECONDS=10 --max-usd 1`. Report in `docs/playtest/runs/sj6-7day.md`, with `sj5b-7day`'s price graph and books appended beside its own (the card's done gate).
Deviations from TDD: none.
Provisional answers added to QUESTIONS.md: none.
New tunables: none.
Next session should know: **Clean, $0.15: 2,656 turns, 0 skipped, no error, no 5xx, archived, conservation replay clean — and the town starved.** Day 1 is forty people (the seed fills the floor of 24 before the players join, Q161) and sixteen householders leave at its end, citizens 8–23: every mill's, workshop's, machine shop's and Builder's hands, since the seed staffs firms in id order. Day 2: fifteen `PaymentMissed`, fifteen `Unassigned` (treasuries at cost after E-1 could not carry a payroll against a town of twenty-four buyers); production 3,077 → 2,520 → 983 → 923 → 737 → 780 → 662 units a day; Food sold 1,302 on day 1 then 18, 62, 55, 78, 121, 310 — **needs met 100 % → 96 % → 0 % from day 3, hardship 21 of 24 on days 4–5, median wellbeing 95 → 33**, with 241–506 Food bids resting at 1.31 every evening and no ask: the mills had no hands and no grain. The runner posted twenty job offers on day 1 and then 1, 4, 1, 0, 0, 1 — Q2's rule (no offers while inventory is high) held while the shelves it counted were the ones nobody could buy — and every offer was at the constant 8.00 an hour; **the day wage fell 54 → 45 → 55 → 60 → 44 → 36 → 33 because fewer hours were worked and paid, never because a firm bid for labour.** That is the finding the card asked for: E-1 answers the shelf, nothing answers the labour board; a runner that cannot restaff at a fixed wage idles and misses payroll. Prices: the same downward ladder as `sj5b-7day` but slower (grain 0.61 → 0.53, ore 0.92 → 0.80, Materials 3.04 (day 1's foundry bids printed at 3.04, 4.44, 4.88 — thin books) → 2.28 → 1.70); Food 1.31 all week with nothing to sell; Wares 4.12 flat; day 7's Materials print **1.70 is a player's**: `undercut` at 1.70–1.71 under the foundries' 1.72 sold 51 of the day's 52 units, and a player also sold 98 ore on day 3 (`sell_ore`, the speculators). A player set a day's price for the first time, by two cents; the plan's "more than the runner's step" still stands unmet, and cannot be met in a town where nothing sells. Standings: four householders lead (H-4 1,244), saver-2 5th, the founders and the borrower last (borrower-1 447.72, foundry fed at 1–4 ore an hour); `which_workplace` chose a foundry all three times again. **Hand-off:** (1) an engine card, **E-7 — the runner's wage answers its staffing** (raise the offer a step when a workplace closes short of hands, cut it when the shelf grew; post offers when `PaymentMissed` is not the reason for the idle), the labour-side twin of E-1, before any further lab week at a low floor; (2) **E-4 first** (`seed --expect-humans`), since Q161's emigration strips the mills of exactly the workers the town needs and no read of a low-floor town is fair until it lands; (3) E-2 as planned. Step 3 of `docs/plans/market-liveliness.md` is done; the liveliness question is now an engine question, and the next SJ waits on E-4 and E-7.
make check: n/a (no code) · new tests: none · sim-check: n/a
## E-4 — A seed that expects its players — 2026-09-25 — PR (s3.2-seed-expect-humans)
Built: `params.population.expected_humans` (0 in `_base.toml`); `start_epoch` fills householders to `floor - max(active humans, expected) - householders`, at epoch 0 and at every rollover (Q41's dormant humans count as expected); 8l untouched, so a place kept for a player who never comes is filled at the first cycle end and one human more than expected still emigrates one householder. `isms-server seed --expect-humans N` is shorthand for the override; `scripts/agents/run.sh` passes `$PLAYERS`. Every golden regenerated (the params line only); TDD section 7 table row; Q161 answered; jev.md E-4 and `docs/plans/market-liveliness.md` step 4 marked done.
Deviations from TDD: none (the TDD's fill rule counted active humans; the seed now also counts the humans it is told to expect, recorded in Q161).
Provisional answers added to QUESTIONS.md: none (Q161 answered).
New tunables: `population.expected_humans` 0.
Next session should know: **The lab town is now the town it says it is from the first hour.** `make e2e-agents`, both presets: 32 householders and 8 players = 40 from day 1 through the rollover, zero "has left" headlines (every earlier lab run had one per player at day 1's end). Runs before this (`sj3-`, `sj5-`, `sj5b-`, `sj6-7day`) still carry the day-1 wave and were read from day 2; a re-run of SJ.6's town at a floor of 24 would seed eight householders, not twenty-four, and the mills would keep their hands — but the wage still never moves, so E-7 comes before that week. Dev databases seeded before this PR need a reseed (`Params` grew a field). Next: E-7, the runner's wage answers its staffing (step 5 of the plan).
make check: green · new tests: 2 (`a_seed_that_expects_its_players_leaves_their_places_empty`, `an_expected_player_who_never_comes_is_backfilled_at_the_cycle_end`) · make e2e-agents: both presets clean


## E-7 — The runner's wage answers its staffing — 2026-09-25 — PR (s3.3-runner-wage)
Built: `Workplace.wage_step` and the `WageStepped` event at 8m (folded into `cycle_end_8m_shelves`, one pass over each market org's shelves and boards): a workplace's step falls by one when the org's shelf of its output grew and otherwise rises by one when an employment offer of the org's stood unfilled at the close, saturating at the band's edges. `householder::offer_wage` (the legacy wage x `1 + step x legacy_wage_step`, clamped to `legacy_wage_min..=legacy_wage_max`) replaces the median of open offers as the base of every legacy firm's offer, payroll estimate and input reserve. The manager withdraws an open offer it cannot honour (no place it can pay a cycle for at the board's wage, a glutted shelf, or a wage the board has moved off) and re-posts at the board's wage while a place remains. `run_round` lands a citizen's own commands before deciding its managed orgs' (a manager who takes the last place on its own firm's offer no longer withdraws an offer that is gone). The sim prints householder rejections as it prints the assembly's. SCRIPT.md rule 1 rewritten; every golden regenerated (the params line and the new events); `docs/tuning/freeport-03.md`.
Deviations from TDD: TDD 9.3's "hire at the median open offer or the legacy wage" and Q2's median, amended by Q164 (a QUESTIONS row, no ADR: the mechanism is the card's, and the median goes because boards that move would ratchet it).
Provisional answers added to QUESTIONS.md: Q164 (the board steps; the wage band; offers withdrawn when the firm cannot honour them; the ask's cost basis stays at the legacy wage for now), Q165 (the script's householder does not quit for a better wage; the option is recorded for Chris).
New tunables: `householder.legacy_wage_step` 0.05, `legacy_wage_min` 1.0, `legacy_wage_max` 2.0.
Next session should know: **All five presets stable; the low-floor gate is met the honest way.** At the forty floor nothing a target reads moved (need 100 %, index 0.89-1.14, unemployment 21-31 against 22-30); the consumption Gini rose 0.006-0.008 -> 0.010-0.029 because wages now differ between firms, which is the lever. A floor-12 Freeport epoch, which starved on main (need 73 %, output zero from cycle 1 to cycle 10: twelve hands took the twelve lowest-numbered offers, the mills got none, and a broke workshop re-hired the hand it had just failed to pay through its own stale 8.00 offer), now recovers (need 86 %, 100 % from cycle 10): every board rises from the first close, a farm whose grain piles up steps back, an idle mill climbs a step a day, and when the workshops and then the farms break, the hands they free take the best wage in town, the mills' at 11.20. **Five hungry cycles remain and no value of the step or ceiling shortens them** (sweep in the report): the clock is how long an idle-paying firm takes to break, because the script's householder never leaves a job for a better wage (Q165) — the wage decides where the freed hands go, not when. In the lab the players do compare wages (`job take_best`), so the next SJ.6-shaped week at a floor of 24 with E-4 and E-7 is the run that reads this lever: read the offers column beside the asks. Three E-1 follow-ups found on the way and left in the report: the ask's cost basis does not carry the wage (a mill paying 11.20 asks as if it paid 8.00; Q164's open half, and the way a wage would reach the price graph); E-1's shelf step runs past its band where E-7's board saturates; the shelf reading counts inventory net of the firm's own resting asks, so a seeded shelf coming back off the book reads as growth. Dev databases seeded before this PR need a reseed (`Workplace` and `Params` grew a field). Next: E-2 and E-3 (step 6 of `docs/plans/market-liveliness.md`).
make check: green · new tests: 2 (`a_board_moves_the_wage_offer_by_a_step_within_the_band`, `a_mill_nobody_chose_raises_its_wage_until_the_idle_hands_come`) · sim-check: all five presets stable
