# Append-only logs from two parallel tracks: keep both sides instead of conflicting.
docs/SESSIONS.md merge=union
# Migrations are checksummed by sqlx: keep their bytes identical on every OS.
*.sql text eol=lf
